# T-007 — ADR-0001 Supabase (relacional, RLS, push manual, backups/PITR, costo p

- **Zona / dueño:** P1 · **Issue:** #8 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-002
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
ADR-0001 Supabase (relacional, RLS, push manual, backups/PITR, costo por ambiente en USD) y ADR-0002 hosting y cron

## Archivos permitidos
- `docs/adr/**`
- `docs/tasks/T-007.md`
- `docs/tasks/log/T-007.md`
- `docs/revision-pr/**`
- `package.json` — scripts `test` y `format:check`
- `.github/workflows/ci.yml` — pasos de los jobs `unit`, `lint` y `audit`
- `.agents/hooks.json` y `.agents/scripts/agent-guard.mjs` — ruta y payload del guard
- `tools/verify-agent-guard.test.ts` — la prueba que lo respalda
- `.prettierrc` — `endOfLine`

> Habilitados por Lautaro073 el 2026-09-22, al resolver `PR57-H06` y los tres
> controles del barrido (`H18`, `H19`, `H20`). El denominador común: cada uno era
> algo declarado como control que nadie ejecutaba —la suite del DoD, el guard de
> la regla 00, `pnpm audit` y Prettier—, y en los cuatro casos el archivo donde se
> engancha caía fuera de esta lista. Es la misma forma que `PR47-A01`, `PR48-A01`
> y `PR49-A01` —el DoD pide un entregable y la lista no da dónde ponerlo—, acá con
> la variante «no da dónde engancharlo».

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [x] Revisados por las 3 personas; cada costo marcado como dato o supuesto
- [x] `pnpm typecheck && pnpm lint && pnpm test`
- [x] Sin cambios fuera de "Archivos permitidos"
- [x] Bitácora `docs/tasks/log/T-007.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
