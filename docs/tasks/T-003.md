# T-003 — `ci.yml`, `migrate.yml`, `approval-policy.yml`, protecciones y environ

- **Zona / dueño:** P1 · **Issue:** #4 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-000, T-002
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
`ci.yml`, `migrate.yml`, `approval-policy.yml`, protecciones y environments según §6

## Archivos permitidos
- `.github/workflows/**`
- `docs/tasks/T-003.md`
- `docs/tasks/log/T-003.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [ ] Un PR con error de tipos queda bloqueado; `ci.yml` falla si `db:types` deja drift de `database.types.ts` usando `SUPABASE_PROJECT_REF` (demostrado plantando un diff); CI con caché en menos de 10 minutos; una migración vacía de prueba se aplica en staging; dos merges seguidos no migran en paralelo; Actions fijadas por SHA; `approval-policy` bloquea un PR de prueba de P2 sin aprobación de Lautaro073 y uno de Lautaro073 sin informe de agy; job `bundle-budget` informa el first-load JS por ruta y avisa si supera el presupuesto de la regla 25
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-003.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
