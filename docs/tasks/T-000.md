# T-000 — Scaffold mínimo

- **Zona / dueño:** P1 · **Issue:** #1 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** —
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
Scaffold mínimo: Next.js App Router, pnpm con `packageManager`, `.nvmrc` y `engines.node`, TS strict, ESLint con fronteras (`eslint-plugin-boundaries`) y regla cliente→servidor, `server-only`, Prettier con plugin de Tailwind, Tailwind, `components.json` de shadcn/ui apuntando a `src/ui`, Vitest con cobertura, raíz de `src/` según la regla 20, `src/features/_template/`, `layout.tsx`, `providers.tsx` con el `QueryClientProvider`, `middleware.ts` vacío, `src/server/env.ts` y `src/lib/env.public.ts` validados con Zod, `.env.example`

## Archivos permitidos
- `raíz y configs`
- esqueleto de `src/**`
- `middleware.ts`
- `components.json`
- `tools/lint-fixtures/**`
- `docs/tasks/T-000.md`
- `docs/tasks/log/T-000.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [ ] typecheck, lint, test y build verdes. Lint falla con (a) un import profundo entre features, (b) un `"use client"` que importa `src/server/**` y (c) un paquete fuera de la lista aprobada, con fixtures commiteados. Importar un archivo `server-only` desde cliente rompe el build; arrancar sin una variable obligatoria falla con un mensaje claro
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-000.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
