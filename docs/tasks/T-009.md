# T-009 — Auth base

- **Zona / dueño:** P2 · **Issue:** #10 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-004, T-005, T-006
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo

Auth base: login, registro con rol, logout, sesión/rol/aal, guardas por rol, función de sesión para `middleware.ts`

## Archivos permitidos

- `src/features/auth/**`
- `src/app/(public)/login/**` y `src/app/(public)/register/**` (visto bueno P3)
- `middleware.ts`
- `docs/tasks/T-009.md`
- `docs/tasks/log/T-009.md`
- `docs/revision-pr/**`

## Fuera de alcance

- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas

- ninguna

## DoD

- [x] merchant y courier se registran con su rol; rol admin rechazado; courier no entra a `(merchant)` ni merchant a `(courier)`; tests unitarios de guardas
- [x] Consumo asíncrono de `await createClient()` (`src/server/supabase/server.ts`) ejercitado en tests unitarios de sesión/guardas (`H16`)
- [x] `pnpm typecheck && pnpm lint && pnpm test`
- [x] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-009.md` al día y PR con evidencia

## Notas para quien retome

- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- `H16` (PR #58 Ronda 3): `createClient()` en `src/server/supabase/server.ts` es `async` (`await cookies()` de Next.js 15). Todo consumidor en `src/features/auth/**` debe invocarlo con `await createClient()` y los tests de sesión/guardas deben ejercitar su comportamiento asíncrono.
