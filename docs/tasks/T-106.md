# T-106 — Migración, RLS de coordenadas (`merchants.default_pickup_lat/lng`, `de

- **Zona / dueño:** P1 · **Issue:** #16 · **Fase:** Fase 1 (Núcleo transaccional y flujos)
- **Dependencias (mergeadas en develop):** contracts-v1
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
Migración, RLS de coordenadas (`merchants.default_pickup_lat/lng`, `delivery_request_contacts.pickup/dropoff_lat/lng`, `delivery_requests.route_distance_m`), RPC `calculate_route_distance(lat1, lng1, lat2, lng2)` con Haversine × 1.30 y redondeo a 0,5 km, validación de bounding box de Aguilares (-27.4550 a -27.4100 lat, -65.6400 a -65.5950 lng) y fallback a centroides de `zones`

## Archivos permitidos
- `supabase/migrations/**`
- `supabase/tests/rpc_distance.sql`
- `supabase/tests/rls_coordinates.sql`
- `src/server/rpc/distance.ts`
- `src/types/database.types.ts`
- `docs/tasks/T-106.md`
- `docs/tasks/log/T-106.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [x] pgTAP de RLS (repartidor no aceptado recibe NULL al leer coordenadas); un repartidor aprobado no lee `notes`, `paid_until` ni `subscription_status` de `merchants` (PR56-H13); cálculo exacto con factor 1.30 y redondeo a múltiplos de 500 m; coordenadas fuera de Aguilares rechazadas con error P0001; fallback a centroides probado sin lat/lng; tipos regenerados sin diff
- [x] `pnpm typecheck && pnpm lint && pnpm test`
- [x] Sin cambios fuera de "Archivos permitidos"
- [x] Bitácora `docs/tasks/log/T-106.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- **El DoD suma tres columnas que no son coordenadas** (`PR56-H13`): hoy `merchants_select_courier` concede la fila entera del comercio a cualquier repartidor aprobado que vea una solicitud publicada, y ahí van `notes` —campo administrativo—, `paid_until` y `subscription_status`. RLS filtra filas, no columnas, y `grant select (columna)` no sirve porque el privilegio es por rol y rompería `merchants_select_self`: la salida es una vista.
- **Antes de escribir policies nuevas, leer la enumeración de las 53 existentes** por tabla y por operación, en `docs/revision-pr/pr-56/revisiones/ronda-3.md`. La revisión de T-005 tardó cuatro rondas porque barrió `select`, después `update` y recién al final `insert`; siete de sus hallazgos ya estaban en el primer commit (`AG-37`, `AG-38`).
- Queda pendiente `PR56-H22`: `profiles_update_self` no congela `created_at`.
