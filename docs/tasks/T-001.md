# T-001 — Kit de agy, CODEOWNERS, Project e issues

- **Zona / dueño:** P1 (Lautaro073) · **Issue:** #2 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-000 (PR #47)
- **Contratos a leer:** `AGENTS.md`, `.agents/rules/**`

## Objetivo
Instalar `docs/agy-kit/` en la raíz del repositorio, configurar CODEOWNERS con usuarios reales (@Lautaro073, @KiraK72), armar la estructura de soporte de agy con sus reglas, hooks (agent-guard.mjs) y skills para permitir la operación segura y paralela de las 3 personas.

## Archivos permitidos
- `AGENTS.md`
- `.agents/**`
- `.github/CODEOWNERS`
- `.github/pull_request_template.md`
- `docs/**`
- `supabase/AGENTS.md`
- `src/domain/AGENTS.md`
- `e2e/AGENTS.md`
- `tools/**`

## Fuera de alcance
- Modificación de código fuente de la aplicación (`src/features/**`, `src/server/**`, `src/ui/**`, etc.)
- Modificación de esquemas o migraciones de base de datos (`supabase/migrations/**`)

## Dependencias nuevas permitidas
- ninguna

## DoD
- [x] Pruebas automatizadas en `tools/verify-t001.test.ts` pasando al 100%.
- [x] GitHub no marca errores en CODEOWNERS; @Lautaro073 y @KiraK72 asignados.
- [x] Con agy, leer `.env.local`, `pnpm supabase db push` y `git push origin develop` quedan bloqueados (`deny`) y `pnpm test` devuelve `ask`.
- [x] Todas las 8 reglas y skills instaladas en `.agents/`.
- [x] `docs/agy-kit/` eliminado tras la instalación.
- [x] `pnpm typecheck && pnpm lint && pnpm test` en verde.
- [x] Bitácora `docs/tasks/log/T-001.md` al día.
