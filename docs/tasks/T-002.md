# T-002 — CLI de Supabase en devDependencies con versión exacta, `config.toml`, 

- **Zona / dueño:** P1 · **Issue:** #3 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-000
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
CLI de Supabase en devDependencies con versión exacta, `config.toml`, proyectos staging y producción (Free Tier), clientes `src/server/supabase/{server,browser,admin}.ts`, scripts `db:*`, sección de base en `docs/onboarding.md`

## Archivos permitidos
- `supabase/config.toml`
- `src/server/supabase/**`
- `package.json` (scripts y devDep)`
- `.env.example`
- `docs/onboarding.md`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [ ] P2 o P3 levantan la base en su máquina siguiendo solo el onboarding (vía Docker local); no hay claves en el repo; el cliente admin tiene `server-only`
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-002.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
