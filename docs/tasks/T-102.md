# T-102 — `accept_offer` atómica e idempotente

- **Zona / dueño:** P1 · **Issue:** #12 · **Fase:** Fase 1 (Núcleo transaccional y flujos)
- **Dependencias (mergeadas en develop):** T-101
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
`accept_offer` atómica e idempotente

## Archivos permitidos
- idem que T-101 + `supabase/tests/rpc_accept.sql`
- `docs/tasks/T-102.md`
- `docs/tasks/log/T-102.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [ ] 10 llamadas concurrentes → una sola ganadora; idempotencia; `ALREADY_MATCHED`; repartidor suspendido entre la oferta y la aceptación
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-102.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- **Desde T-005 el cliente no mueve estados.** La RLS de `20260922051650_rls_v1.sql` congela `offers.status`, `offers.decided_at`, `delivery_requests.status`, `accepted_offer_id` y los cinco *timestamps* de ciclo de vida, tanto en `update` como en `insert`: una oferta nace `pending`, una solicitud nace `draft`, un documento nace `submitted` y un incidente nace `open`. O sea que **estas RPC no son una comodidad, son el único camino**, y tienen que ser `security definer`. Si una de ellas intenta la transición con el rol del invocador, va a recibir `42501`. Salió de la revisión de la PR #56 (`PR56-H06`, `PR56-H11`, `PR56-H12`, `PR56-H15` a `PR56-H19`); el detalle está en `docs/revision-pr/pr-56/revisiones/ronda-4.md`.
- El invariante de este DoD —«10 llamadas concurrentes, una sola ganadora»— **depende** de que el cliente no escriba `offers.status`. Esa es exactamente la razón por la que T-005 lo congeló. Aceptar son dos escrituras que tienen que ir en la misma transacción: `offers.status = 'accepted'` y `delivery_requests.accepted_offer_id`; `is_courier_assigned_to_request` exige las dos, así que dejar una sin la otra no asigna a nadie y ocupa el cupo del índice único parcial `offers_one_accepted_per_request_idx`.
- Conviene aprovechar y **borrar la policy `offers_update_merchant`** (`PR56-H21`): con `status` y `decided_at` congelados, lo único que todavía le deja escribir al comercio es `created_at`.
