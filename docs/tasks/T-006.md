# T-006 — Estados, `DomainErrorCode`, `ActionResult`, `rpc-contracts.ts` (todas 

- **Zona / dueño:** P2 (`@Lautaro073` asignado en Issue #7) · **Issue:** #7 · **Fase:** Fase 0 (Fundaciones y contratos)
- **Dependencias (mergeadas en develop):** T-000
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
Estados, `DomainErrorCode`, `ActionResult`, `rpc-contracts.ts` (todas las RPC, incluidas las `admin_*`), schemas Zod, orden por `doc_level`, `testing/rpc-fake.ts`

## Archivos permitidos
- `src/domain/**`
- `src/server/supabase/server.ts`
- `tools/verify-build-boundaries.test.ts`
- `vitest.config.ts`
- `package.json`
- `pnpm-lock.yaml`
- `docs/tasks/T-006.md`
- `docs/tasks/log/T-006.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- Actualización de `next` y `eslint-config-next` (`>=15.5.24`) y parches de seguridad en `package.json` y `pnpm-lock.yaml` autorizada por Lautaro073 (`H14`)

## DoD
- [x] Transiciones válidas e inválidas por actor; orden de ofertas; el fake devuelve cada código de error; umbral de cobertura de ramas ≥ 90 % configurado
- [x] `pnpm typecheck && pnpm lint && pnpm test`
- [x] Sin cambios fuera de "Archivos permitidos"
- [x] Bitácora `docs/tasks/log/T-006.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- Alcance ampliado por Lautaro073 tras Ronda 1 y Ronda 2 de PR #58 para incluir `vitest.config.ts` (`H12`), `package.json`, `pnpm-lock.yaml`, `src/server/supabase/server.ts` y `tools/verify-build-boundaries.test.ts` (`H14` — compatibilidad con `await cookies()` y mensaje `needs "server-only"` en Next.js 15.5.26 + Vitest 3.2.7).
