# Bitácora T-002 — Clientes @supabase/ssr, vinculación remota y scripts db:*

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-21 02:15 — Lautaro073 (agy)
- **Hecho:** Inicio de tarea T-002 con la skill `tomar-tarea`. Verificación de dependencias previas mergeadas en develop (T-000 en f6dc070 y T-001 en 9f03018). Creación de rama `feat/T-002-supabase-clients`. Creación de suite de pruebas automatizadas del DoD en `src/server/supabase/clients.test.ts`. Ejecución de prueba demostrando 7/7 fallos en rojo (TDD fase roja).
- **Decisiones y por qué:** Las pruebas del DoD se ubican dentro de `src/server/supabase/clients.test.ts` para mantenerse estrictamente dentro de los archivos permitidos de la ficha (`src/server/supabase/**`).
- **Pruebas:** typecheck ❌/pendiente · lint ❌/pendiente · test ❌ (7 pruebas del DoD demostradas fallando en rojo en src/server/supabase/clients.test.ts) · test:db n.a.
- **Falta:** Implementar clientes src/server/supabase/{server,browser,admin}.ts con server-only en admin, configurar supabase/config.toml, actualizar package.json con CLI de supabase y scripts db:*, documentar en docs/onboarding.md y verificar tests en verde.
- **Bloqueos:** ninguno.
- **Próximo paso:** Implementar src/server/supabase/server.ts, src/server/supabase/browser.ts y src/server/supabase/admin.ts.
- **Último commit:** 53dca97

---

## 2026-09-21 02:25 — Lautaro073 (agy)
- **Hecho:** 
  1. Implementación de clientes Supabase:
     - src/server/supabase/server.ts: Cliente de servidor para Server Components y Server Actions con @supabase/ssr (createServerClient) y manejo de cookies con cookies() de next/headers.
     - src/server/supabase/browser.ts: Cliente de navegador para Client Components con @supabase/ssr (createBrowserClient).
     - src/server/supabase/admin.ts: Cliente administrativo con @supabase/supabase-js, serverEnv.SUPABASE_SERVICE_ROLE_KEY y directiva obligatoria import 'server-only'.
  2. Configuración de Supabase:
     - supabase/config.toml: Creado con project_id = "cadeapp-staging" y puertos locales/staging.
  3. package.json:
     - Agregado script "db:types".
     - Fijada devDependency supabase: 2.116.0.
  4. Suite de pruebas:
     - src/server/supabase/clients.test.ts pasando 7/7 en verde.
     - pnpm typecheck, pnpm lint, pnpm test (61/61 tests verdes en 8 archivos) y pnpm build (87.2 kB) en verde.
- **Decisiones y por qué:** El cliente admin contiene import 'server-only' para cumplir el invariante de arquitectura del producto. Los tests se mantuvieron dentro de src/server/supabase/ sin desvíos de alcance.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (61/61 tests verdes) · test:db n.a. · build ✅
- **Falta:** Revisión de PR y squash merge a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Abrir PR para T-002 y squash merge a develop.
- **Último commit:** d934021

---

## 2026-09-21 02:48 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución de hallazgos H01 y H02 con dictamen unánime de El Consejo:
     - Traslado de `browser.ts` a `src/lib/supabase/browser.ts` para permitir importación legítima desde Client Components (`'use client'`) sin violar la regla 20 ni `cadeapp/client-no-server`.
     - Inclusión obligatoria de `import 'server-only'` en `src/server/supabase/server.ts`.
     - Creación de la regla ESLint `cadeapp/server-layer-must-be-server-only` en `tools/eslint-plugin-cadeapp/index.js` y habilitación en `.eslintrc.json`.
     - Adición de fixture consumidor `tools/lint-fixtures/client-browser-supabase-consumer.tsx` (`AG-10`) y fixtures positivos/negativos de `server-only` en `tools/verify-scaffold.test.ts`.
     - Actualización de `.eslintrc.json` para autorizar `types` desde `lib` en `boundaries/element-types`.
  2. Resolución de H03 y H04:
     - Actualización de `docs/onboarding.md` (remoción de comando inexistente `pnpm test:db` y reescritura de sección 3 para entorno remoto `cadeapp-staging` sin Docker local).
     - Formalización de `supabase/config.toml` como configuración declarativa de proyecto.
  3. Resolución de H05:
     - Escaneo integral y recursivo de todo el árbol de archivos en `clients.test.ts` (excluyendo gitignored locales) validando que no existen JWTs, tokens PAT de Supabase (`sbp_`) ni secretos expuestos en el repositorio.
  4. Resolución de A01:
     - Formalización de archivos permitidos en `docs/tasks/T-002.md` y `docs/implementation-plan.md` (§4 y §8).
  5. Verificaciones en verde:
     - typecheck (0 errores), lint (0 errores), test (65/65 pasando en 8 suites), build (87.2 kB First Load JS).
- **Decisiones y por qué:** El Consejo determinó rechazar excepciones en `client-no-server` y ubicar el cliente de navegador en `src/lib/supabase/browser.ts`, manteniendo la capa `src/server/` 100% pura y blindada con la regla automatizada `server-layer-must-be-server-only`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (65/65 tests verdes) · build ✅
- **Falta:** Revisión de PR #49 Ronda 2 y squash merge a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Ejecutar ronda de revisión en PR #49.
- **Último commit:** a36b18d

---

## 2026-09-21 03:05 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución definitiva de Ronda 2 según dictamen unánime de El Consejo:
     - H04: Formalizada la inclusión de `supabase/config.toml` en la celda de "Archivos permitidos" de la fila T-002 en `docs/implementation-plan.md:276`.
     - H06: Redactado el DoD de `docs/tasks/T-002.md` y de `docs/implementation-plan.md` explicitando que la validación de drift corre en CI (`db:types`); removida la sugerencia de ejecución local sin credenciales en `docs/onboarding.md`.
     - A01: Desacoplados los registros de revisión hacia la rama `docs/revisiones` según `COMO-ENTREGAR.md`, dejando 0 archivos fuera de alcance en el diff de T-002.
  2. Verificaciones en verde:
     - typecheck (0 errores), lint (0 errores), test (65/65 pasando en 8 suites), build (87.2 kB First Load JS).
- **Decisiones y por qué:** Alineación estricta entre la ficha, el plan y la documentación para que ningún comando requiera tokens inexistentes en local y la PR no contenga artefactos fuera de su alcance.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (65/65 tests verdes) · build ✅
- **Falta:** Aprobación final y squash merge a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Squash merge de PR #49 a develop.
- **Último commit:** b0c4c4f

---

## 2026-09-21 12:05 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución de hallazgos de Ronda 3 (PR #49):
     - **H07 (Crítico):** Regenerado `pnpm-lock.yaml` tras `pnpm install` para incluir `supabase@2.116.0`; verificado `pnpm install --frozen-lockfile` (código 0) y `pnpm supabase --version` (2.116.0). Agregado `pnpm-lock.yaml` y dependencia permitida en `docs/tasks/T-002.md` y `docs/implementation-plan.md`.
     - **H08 (Alto):** Reemplazada la redirección de shell destructiva por el script seguro `tools/db-types.mjs`. Utiliza `spawnSync`, valida `status === 0`, presencia de `export type` y tamaño mínimo antes de escribir, blindando `src/types/database.types.ts` ante cualquier fallo o falta de red/credenciales.
     - **H06 (Medio):** Asignada formalmente la verificación de drift de tipos al CI en el DoD de T-003 en `docs/implementation-plan.md:277` (`ci.yml falla si db:types deja drift de database.types.ts`).
     - **H09 / H10 (Medio):** Configurado `tools/db-types.mjs` para resolver el ref del proyecto vía `SUPABASE_PROJECT_REF`, `SUPABASE_PROJECT_ID` o parseando `NEXT_PUBLIC_SUPABASE_URL`, usando `--project-id <ref>` o `--linked` según corresponda.
     - **H11 / H03 (Medio):** Actualizados `.env.example` y `docs/onboarding.md:17` para reflejar el flujo estándar remoto con `cadeapp-staging` y variables provistas por el Tech Lead, eliminando referencias residuales a Docker local y `supabase status`.
  2. Verificaciones en verde:
     - `pnpm install --frozen-lockfile` (0 errores, resolución limpia).
     - `pnpm typecheck` (0 errores).
     - `pnpm lint` (0 advertencias, 0 errores).
     - `pnpm test` (65/65 pruebas pasando en 8 suites).
     - `pnpm build` (87.2 kB First Load JS).
     - Diff de archivos estrictamente dentro de "Archivos permitidos".
- **Decisiones y por qué:** El script `tools/db-types.mjs` aísla la generación de tipos y previene cualquier riesgo de archivo en blanco o truncamiento en Windows/Linux ante fallos, mientras que `pnpm-lock.yaml` garantiza que los entornos de CI y colaboradores compartan la versión exacta de Supabase CLI.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (65/65 tests verdes) · build ✅
- **Falta:** Aprobación final de PR #49 y squash merge a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Squash merge de PR #49 a develop.
- **Último commit:** 55672a0

---

## 2026-09-21 12:20 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución de hallazgos de Ronda 4 (PR #49):
     - **H10 (Decisión Tech Lead):** Actualizado el título y objetivo de `docs/tasks/T-002.md` y la fila de T-002 en `docs/implementation-plan.md:276` para reflejar "configuración remota para cadeapp-staging vía variables de entorno" (descartando formalmente la necesidad de `supabase link` local). En `docs/implementation-plan.md:277` (T-003) se explicitó el uso de `SUPABASE_PROJECT_REF` en CI para `db:types`.
     - **H13 (Medio / Regresión):** Agregada prueba de regresión automática en `src/server/supabase/clients.test.ts` que valida que `package.json` no contenga `>` en `db:types`, y ejecuta `tools/db-types.mjs` contra un archivo destino dummy bajo fallo forzado comprobando que el archivo NO se modifica ni se trunca. En `tools/db-types.mjs` se añadió soporte para `DB_TYPES_TARGET_FILE` y regex tolerante a guiones.
     - **H12 (Cosmético):** Actualizado `.env.example` con placeholder de URL alfanumérico y consistente con su comentario (`https://placeholderprojectref.supabase.co`).
     - **H06 residual (Cosmético):** Actualizado `docs/onboarding.md:23` con redacción a futuro indicando que CI validará el drift en T-003.
  2. Verificaciones en verde:
     - `pnpm install --frozen-lockfile` (0 errores).
     - `pnpm typecheck` (0 errores).
     - `pnpm lint` (0 advertencias, 0 errores).
     - `pnpm test` (66/66 pruebas pasando en 8 suites, test H13 incluido).
     - `pnpm build` (87.2 kB First Load JS, < 180 kB).
     - Diff de archivos: 19 archivos, 0 fuera de "Archivos permitidos".
- **Decisiones y por qué:** Blindaje de regresión completo para la generación de tipos y consistencia total entre la documentación, variables de entorno de ejemplo y los planes de Fase 0.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (66/66 tests verdes) · build ✅
- **Falta:** Aprobación final y squash merge de PR #49 a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Squash merge de PR #49 a develop.
- **Último commit:** c35eb54

---

## 2026-09-21 12:35 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución de hallazgos de Ronda 5 con dictamen unánime de El Consejo (PR #49):
     - **H13 (Correctness / Regresión genuina):** En `src/server/supabase/clients.test.ts:133`, se eliminó `shell: process.platform === 'win32'` del `spawnSync('node', ...)`. Esto evita que `cmd.exe` fragmente los argumentos ante rutas con espacios (`C:\Users\El Yisus Pai\...`), ejecutando `node` nativamente vía API Win32.
     - **Demostración empírica TDD (Fase Roja):** Se demostró el caso negativo inyectando `writeFileSync(TARGET_FILE, '', 'utf-8')` en `tools/db-types.mjs`, verificando que el test detecta la pérdida de datos y falla en ROJO (`AssertionError: expected '' to be '/* original types content */'`), y pasando en VERDE genuino tras restaurar el script.
     - **H14 (Integridad / Regex project ref):** En `tools/db-types.mjs:21`, se restauró la regex estricta `^https:\/\/([a-z0-9]+)\.supabase\.co` para no aceptar nombres con guiones como si fueran project refs de infraestructura.
  2. Verificaciones en verde:
     - `pnpm install --frozen-lockfile` (0 errores).
     - `pnpm typecheck` (0 errores).
     - `pnpm lint` (0 advertencias, 0 errores).
     - `pnpm test` (66/66 pruebas pasando en 8 suites; test H13 ejecutándose en ~750ms).
     - `pnpm build` (87.2 kB First Load JS, < 180 kB).
     - Diff de archivos: 19 archivos, 0 fuera de "Archivos permitidos".
- **Decisiones y por qué:** Aplicación del dictamen de El Consejo: ejecución nativa sin shell para evitar descalces en Windows con espacios en la ruta y verificación estricta TDD en rojo.
- **Pruebas:** test en rojo demostrado ❌ (H13 simulando truncamiento) · typecheck ✅ · lint ✅ · test ✅ (66/66 tests verdes) · build ✅
- **Falta:** Aprobación final y squash merge de PR #49 a develop.
- **Bloqueos:** ninguno.
- **Próximo paso:** Squash merge de PR #49 a develop.
- **Último commit:** PENDIENTE





