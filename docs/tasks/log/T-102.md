# Bitácora T-102 — `accept_offer` atómica e idempotente

- **Issue:** #12
- **Rama:** `feat/T-102-rpc-accept-offer`
- **PR:** #64
- **Responsable:** `@Lautaro073` (P1 — Base de Datos, Dominio, Auth y Admin)

---

## Sesión 1 — 2026-09-23 (`@Lautaro073`)

### 1. Toma de tarea y verificación de precondiciones (`tomar-tarea`)
- Ficha `docs/tasks/T-102.md` verificada en `origin/develop`.
- Issue `#12` (`[T-102] accept_offer atómica e idempotente`) abierto y asignado a `@Lautaro073`.
- Dependencia `T-101` (`a3fe6a7`, PR #62) mergeada en `origin/develop`.
- Creación de rama `feat/T-102-rpc-accept-offer` desde `origin/develop`.
- Registro de `docs/contracts/CC-003.md` (`contract-change`) para sumar `'INTERNAL_ERROR'` y el comentario de precedencia canónica a `RPC_CONTRACTS.accept_offer` en `src/domain/rpc-contracts.ts`, además de preservar `matchedAt` en las llamadas idempotentes de `accept_offer` en `src/domain/testing/rpc-fake.ts`.

### 2. Fase Roja (TDD — pruebas primero y evidencia de fallo en `4bc43f8`)
- Se escribió primero `supabase/tests/rpc_accept.sql` (**34 aserciones pgTAP**) y las pruebas `6`, `7` y `8` en `src/server/rpc/offers.test.ts`.
- Salida de fallo en Fase Roja (`4bc43f8`):
  ```text
  FAIL  src/server/rpc/offers.test.ts > T-102 · RPC accept_offer atómica e idempotente > 6. DoD 10 llamadas concurrentes → una sola ganadora; idempotencia preservando matchedAt; ALREADY_MATCHED; repartidor suspendido entre la oferta y la aceptación
  TypeError: (0 , acceptOfferRpc) is not a function

  FAIL  src/server/rpc/offers.test.ts > T-102 · RPC accept_offer atómica e idempotente > 8. Contrato SQL de accept_offer: SECURITY DEFINER, search_path, locks FOR UPDATE/FOR SHARE, drop policy offers_update_merchant y coincidencia bidireccional de códigos
  AssertionError: expected false to be true // Object.is equality
  ```

### 3. Fase Verde (Implementación SQL + Wrapper Server + Tipos)
- **Migración SQL (`supabase/migrations/20260923170000_rpc_accept_offer_v1.sql`):**
  - Reemplaza la policy `offers_update_merchant` con `with check (false)` tras `drop policy if exists offers_update_merchant on public.offers;`, cerrando `PR56-H21` (impide el 100 % de las escrituras directas del comercio sobre `public.offers`, incluido `created_at`, y preserva el rechazo explícito `42501` ante intentos de `UPDATE` directo verificados en `rls_matrix.sql` tests 26/47 y `rpc_accept.sql`).
  - Implementa `public.accept_offer(p_offer_id uuid)` como `SECURITY DEFINER` con `SET search_path = public, pg_temp`.
  - Adquiere locks en orden jerárquico (`public.delivery_requests FOR UPDATE` -> `public.offers FOR UPDATE` -> `public.couriers FOR SHARE`) para serializar llamadas concurrentes sobre la misma solicitud sin deadlocks.
  - Respeta la precedencia canónica de 8 pasos documentada en `src/domain/rpc-contracts.ts` (`CC-003`) y compartida con `createFakeRpcClient`:
    1. **Actor y rol:** `UNAUTHENTICATED` (`auth.uid() is null`) -> `UNAUTHORIZED_ACTOR` (`role <> 'merchant'`)
    2. **Parámetros:** `VALIDATION_ERROR` (`p_offer_id is null`)
    3. **Existencia:** `NOT_FOUND` (oferta, solicitud o repartidor inexistentes)
    4. **Titularidad del comercio:** `UNAUTHORIZED_ACTOR` (`merchant_id <> auth.uid()`)
    5. **Idempotencia y competencia:** si `status = 'matched'` y `accepted_offer_id = p_offer_id`, retorna `ok` con `idempotent = true` y el `matched_at` original; si `status = 'matched'` con otra oferta, lanza `ALREADY_MATCHED`.
    6. **Solicitud:** rechazo perezoso si `status = 'published'` y `expires_at <= now()` (`REQUEST_EXPIRED`, sin persistir transición porque el `RAISE EXCEPTION` revierte la transacción, igual que `submit_offer`) -> si `status <> 'published'`, lanza `INVALID_STATE_TRANSITION`.
    7. **Oferta:** si `status <> 'pending'`, lanza `OFFER_NOT_PENDING`.
    8. **Repartidor:** si `status = 'suspended'`, lanza `COURIER_SUSPENDED`; si `status <> 'approved'`, lanza `COURIER_NOT_APPROVED`.
  - Ejecuta en la misma transacción: `offers.status = 'accepted'` (`decided_at = now()`), rechazo de todas las ofertas hermanas `pending` (`status = 'rejected'`, `decided_at = now()`), y transición de `delivery_requests` a `status = 'matched'`, `accepted_offer_id = p_offer_id`, `matched_at = now()` (habilitando inmediatamente `app_private.is_courier_assigned_to_request` para el repartidor ganador).
  - Endurece privilegios: `revoke all on function public.accept_offer(uuid) from public, anon, authenticated; grant execute on function public.accept_offer(uuid) to authenticated;`.
- **Wrapper Server-Only (`src/server/rpc/offers.ts`) y Tipos (`src/types/database.types.ts`):**
  - Se exporta `acceptOfferRpc` y se agrega `accept_offer` a `createOffersRpcServerClient` y `mapOfferRpcError` (incluyendo traducción de `23505` a `ALREADY_MATCHED` documentada como defensa en profundidad y fallback a `INTERNAL_ERROR`).
  - Se actualiza `Database['public']['Functions']['accept_offer']` en `src/types/database.types.ts`.

### 4. Verificación de calidad (`cerrar-sesion`)
- Se alineó el tipo `public.profile_role`, `couriers.profile_id`, `app_private.is_courier_assigned_to_request` y `offers_update_merchant` (`with check (false)`) en la migración `20260923170000_rpc_accept_offer_v1.sql` y en `supabase/tests/rpc_accept.sql`.
- `pnpm typecheck`: `0` errores.
- `pnpm lint`: `✔ No ESLint warnings or errors`.
- `pnpm test`: `18/18` suites (`192/192` tests). Cobertura: `src/server/rpc/offers.ts` **100 %**, `src/domain/rpc-contracts.ts` **100 %**, `src/domain/testing/rpc-fake.ts` **97.31 %** statements / **93.26 %** branches / **100 %** functions.

---

## Sesión 2 — Resolución de Ronda 1 (`docs/revision-pr/pr-64/revisiones/ronda-1.md`)

- **`H01` + `D01` (Bloqueante):** En `src/server/rpc/offers.test.ts` (test 8), se ancló cada regex de lock a su propia sentencia `SELECT` (`[^;]*?`, sin cruzar `;`), verificando además el orden jerárquico de índices (`delivery_requests FOR UPDATE < offers FOR UPDATE < couriers FOR SHARE`) y las mutaciones individuales `M1` (quitar `FOR UPDATE` de `offers`) y `M2` (quitar `FOR UPDATE` de `delivery_requests`).
- **`H02` + `D01` (Bloqueante):** Se reformuló el primer ítem del DoD en `docs/tasks/T-102.md` y `docs/implementation-plan.md` (y las descripciones en `rpc_accept.sql` y `offers.test.ts`) como *«Competencia de 10 ofertas → una sola ganadora y 9 ALREADY_MATCHED (serialización por locks FOR UPDATE en delivery_requests → offers y FOR SHARE en couriers verificada en la migración); idempotencia preservando matchedAt; repartidor suspendido entre la oferta y la aceptación»*.
- **`H03` + `D02` (Bloqueante):** Se eliminó el `update public.delivery_requests set status = 'expired'` previo a `raise exception 'REQUEST_EXPIRED'` en `supabase/migrations/20260923170000_rpc_accept_offer_v1.sql` (que se revertía al abortar la transacción), alineando con `submit_offer`, corrigiendo `docs/contracts/CC-003.md`, `src/domain/rpc-contracts.ts`, la bitácora y el cuerpo del PR, y sumando en `offers.test.ts` (test 8) el control negativo `expect(body).not.toMatch(/set\s+status\s*=\s*'expired'/i)`.
- **`H04` (Medio):** Se corrigieron el título (`# CC-003`) y la línea de `Aprobaciones` en `docs/contracts/CC-003.md`, y las referencias `CC-003` en `src/server/rpc/offers.ts:48`, `src/domain/rpc-contracts.ts:382`, `src/server/rpc/offers.test.ts:832` y `supabase/tests/rpc_accept.sql:445`.
- **`H05` (Bajo):** Se documentó explícitamente en `src/server/rpc/offers.ts` que la rama `23505 -> ALREADY_MATCHED` de `mapOfferRpcError` actúa como defensa en profundidad dado que la RPC captura `unique_violation` internamente y lanza `P0001`.
- **`H06` (Bajo):** Se restauraron las justificaciones entre paréntesis en `## Archivos permitidos` de `docs/tasks/T-102.md`.
