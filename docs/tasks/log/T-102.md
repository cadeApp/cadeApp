# Bitácora T-102 — `accept_offer` atómica e idempotente

- **Issue:** #12
- **Rama:** `feat/T-102-rpc-accept-offer`
- **PR:** Pendiente (Draft)
- **Responsable:** `@Lautaro073` (P1 — Base de Datos, Dominio, Auth y Admin)

---

## Sesión 1 — 2026-09-23 (`@Lautaro073`)

### 1. Toma de tarea y verificación de precondiciones (`tomar-tarea`)
- Ficha `docs/tasks/T-102.md` verificada en `origin/develop`.
- Issue `#12` (`[T-102] accept_offer atómica e idempotente`) abierto y asignado a `@Lautaro073`.
- Dependencia `T-101` (`a3fe6a7`, PR #62) mergeada en `origin/develop`.
- Creación de rama `feat/T-102-rpc-accept-offer` desde `origin/develop`.
- Registro de `docs/contracts/CC-002.md` (`contract-change`) para sumar `'INTERNAL_ERROR'` y el comentario de precedencia canónica a `RPC_CONTRACTS.accept_offer` en `src/domain/rpc-contracts.ts`, alineado con `CC-001` (`T-101`).

### 2. Fase Roja (TDD — pruebas primero y evidencia de fallo)
- Se escribe `supabase/tests/rpc_accept.sql` (suite pgTAP exhaustiva con concurrencia simulada de 10 ofertas competidoras sobre una misma solicitud -> una única ganadora `accepted` y las 9 restantes `rejected` en la misma transacción; idempotencia sobre la oferta ya aceptada devolviendo `idempotent = true` y el mismo `matchedAt`; `ALREADY_MATCHED` al intentar aceptar cualquiera de las otras 9 ofertas; repartidor `suspended`, `pending` o `rejected` entre la oferta y la aceptación; expiración perezosa `REQUEST_EXPIRED`; solicitud en otro estado `INVALID_STATE_TRANSITION`; oferta `withdrawn` -> `OFFER_NOT_PENDING`; `UNAUTHORIZED_ACTOR` para otro comercio o repartidor; `is_definer`, revocación a `anon` `42501`, y eliminación de la policy redundante `offers_update_merchant` según `PR56-H21`).
- Se agregan en `src/server/rpc/offers.test.ts` las pruebas del wrapper `acceptOfferRpc`, la tabla de precedencia canónica ante fallos simultáneos y el test de contrato contra la migración SQL de `accept_offer`.
