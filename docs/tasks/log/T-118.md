# Bitácora T-118 — Integración visual Stitch, shells y navegación canónica

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 15:22 — Lautaro073 (agy)
- **Hecho:** Tarea T-118 iniciada en worktree `cadeApp-T-118` (rama `feat/T-118-integracion-visual-stitch`). Verificada issue #85 asignada a Lautaro073. Convocados los tres asesores de El Consejo (`consejo-frontend`, `consejo-design`, `consejo-persona`) y sintetizados sus dictámenes unánimes: P01 placeholder obsoleto, 404 en rutas canónicas y enlaces internos, ausencia de layout de comercio, leak de BottomNav en onboarding de repartidor, desalineación de `redirectTo` en registro y desprotección de `/onboarding` en guards. Se redactó la suite `src/app/route-integrity.test.ts` con 27 pruebas cubriendo el DoD.
- **Decisiones y por qué:** Se ejecutan las pruebas del DoD demostrando 21 fallos en rojo antes de escribir implementación, cumpliendo la regla de TDD rojo primero y commit aislado.
- **Pruebas:** typecheck ✅ · lint ✅ · test ❌ (21 pruebas rojas intencionales en `src/app/route-integrity.test.ts`) · test:db n.a. · `pnpm vitest run src/app/route-integrity.test.ts` fallando por ausencia de P01, rutas canónicas inexistentes, `registerAction` sin onboarding y falta de layouts/shells.
- **Falta:** Commit de pruebas rojas, implementación de shells (`layout.tsx`, `merchant-nav.tsx`, ajuste `courier-nav.tsx`), implementación de P01 en `src/app/page.tsx`, `/forgot-password`, rutas de comercio canónicas (`dashboard`, `history`, `plan`, `requests/new`, `requests/[id]`, `onboarding`), rutas de repartidor canónicas (`onboarding/*`, `profile`), corrección de `guards.ts` y `actions.ts`, verificación en navegador y comprobación de accesibilidad.
- **Bloqueos:** ninguno.
- **Próximo paso:** Realizar commit de pruebas rojas y proceder a la implementación visual y de rutas canónicas.
- **Último commit:** da34d18

## 2026-09-24 16:00 — Lautaro073 (agy)
- **Hecho:** Implementación integral de T-118 completada de punta a punta. Se reemplazó el placeholder de Fase 0 por la landing P01 institucional de Aguilares en `src/app/page.tsx` con propuesta de valor, acciones duales de 52px y tarjeta educativa de flete directo. Creado `/forgot-password` con formulario y Server Action sin enumeración de usuarios. Creadas rutas canónicas de comercio (`/merchant/onboarding`, `/merchant/dashboard`, `/merchant/requests/new`, `/merchant/requests/[id]`, `/merchant/history` con `MerchantHistoryView`, `/merchant/plan` sin AFIP/ARBA). Creadas rutas canónicas de repartidor (`/courier/onboarding/{identity,vehicle,status}`, `/courier/feed`, `/courier/offers`, `/courier/profile` con `CourierProfileView` sin CBU/alias ni estrellas). Creados layouts y shells mobile-first (`max-w-[390px] mx-auto min-h-screen pb-20`) con TopBar marina (56px `#12182C`) con contraste asistido y BottomNavs específicos (`merchant-nav.tsx` y `courier-nav.tsx`) que se ocultan en onboarding. Eliminadas barras superiores duplicadas en las hojas cliente. Interceptación y redirección canónica en servidor de todas las rutas heredadas sin bucles ni renderizados duplicados. Aislamiento estricto de roles en `guards.ts` y enrutamiento en `registerAction`. Consulta con los 3 miembros de El Consejo (`consejo-frontend`, `consejo-design`, `consejo-persona`) con dictamen unánime favorable (APTO).
- **Decisiones y por qué:** No se alteraron archivos protegidos (`src/ui/**`, `src/domain/**`, contratos ni migraciones). Se resolvieron las reglas de contraste y boundaries arquitectónicos mediante imports canónicos y estilos scoped en layouts sin tocar `globals.css` ni dependencias externas. Se erradicaron de forma estricta los 7 datos fantasma de Stitch.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 errores, 0 warnings) · test ✅ (43 suites, 400/400 pruebas vitest verdes, incluyendo 27/27 en `route-integrity.test.ts`, 20/20 en verify-workflows y 6/6 en verify-adr) · build ✅ (`pnpm build` exitoso, 31 páginas estáticas/dinámicas generadas) · test:db n.a. (no se tocaron migraciones ni RPC).
- **Falta:** ninguna. Tarea terminada de punta a punta lista para revisión.
- **Bloqueos:** ninguno.
## 2026-09-24 16:50 — Lautaro073 (agy)
- **Hecho:** Corrección visual integral, responsividad y fidelidad de marca en la rama `feat/T-118-integracion-visual-stitch` (PR #87):
  1. Geometría auténtica de marca: se extrajeron los trazos vectoriales oficiales de `public/brand/logo.svg` y se integraron en `src/ui/brand-logo.tsx`.
  2. Corrección estructural en botones: en `src/ui/button.tsx` se añadió `inline-flex w-full items-center justify-center gap-2` en el contenedor interno, eliminando de raíz el apilamiento vertical de íconos sobre el texto.
  3. Responsividad plena en `/`: se erradicó el confinamiento rígido a 390px en desktop; ahora se expande fluidamente en tablet/desktop (`max-w-5xl mx-auto`), distribuyendo "¿Cómo funciona?" en 3 columnas y la sección dual inferior en 2 columnas, preservando el flujo vertical en móviles.
  4. Fidelidad de íconos en `/` y registro: se removió el badge de piloto del Hero colocando el isotipo oficial del logo; se reemplazaron los íconos de bicicleta y motos lineales genéricas por el isotipo auténtico del repartidor con mochila teal (`BrandLogo showWordmark={false}`) tanto en botones CTA como en tarjetas de onboarding.
- **Decisiones y por qué:** El usuario solicitó mantener la identidad gráfica fiel a los assets creados y corregir la experiencia en pantallas grandes sin romper los límites móviles.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (78/78 pruebas verdes en integridad, UI system y auth).
- **Falta:** Revisión visual con `/browser` desde la sesión de usuario local para pantallas `/login`, `/register` y shells de comercio/repartidor.
- **Bloqueos:** ninguno.
- **Próximo paso:** Continuar la inspección visual en `/login` mediante la instancia de agy con acceso al navegador.
- **Último commit:** 46066df

## 2026-09-24 19:45 — Lautaro073 (agy)
- **Hecho:** Cierre de los hallazgos de Ronda 1 (`PR87-H01` a `PR87-H12`) y unificación visual en todas las rutas canónicas:
  1. `H01`: `getRoleDefaultPath('admin')` apunta a destino existente (`/`); se reemplazaron enlaces rotos a `/terms` y `/privacy` por avisos explícitos de documentos en publicación (`T-311`).
  2. `H02`: `MerchantPlanPage` (`/merchant/plan`, tab **Cuenta**) consulta `subscription_status`, `paid_until` y datos de perfil reales desde `merchants` y `profiles`, o muestra `EmptyState` honesto cuando no hay registro, sin menciones a comisiones ni bloques AFIP/ARBA.
  3. `H03`: `CourierProfilePage` (`/courier/profile`) y `CourierProfileView` leen `couriers` y `courier_documents` reales sin fabricar `moto`, `approved` ni verificaciones fijas; en vehículos `walk`/`bike` se ocultan patente, licencia, seguro e incentivo documental.
  4. `H04` y `H05`: `getMerchantRequests` pagina con `limit <= 50` y cursor (`created_at`/`id` + `nextCursor`), y `formatApproxDistanceKm`/zonas conservan `null` / ausencia honesta en vez de `1,0 km` o `Centro`/`Aguilares`.
  5. `H06`: Creados `loading.tsx` (con `Skeleton` de `src/ui`) y `error.tsx` en los segmentos canónicos con datos de `(merchant)/merchant/**` y `(courier)/courier/**`.
  6. `H07` y `H08`: Eliminados `text-xs`, switch `h-7 w-12`, tabs `min-h-10`, bloques `<style>` y hex arbitrario `#25D366`, respetando piso tipográfico de 14 px y targets táctiles >= 48×48 px.
  7. `H09`, `H10`, `H11` y `H12`: Ampliada `src/app/route-integrity.test.ts` (50 pruebas cubriendo páginas y hojas renderizadas), verificado en `/browser` a 390 px y 1280 px, y restaurada la plantilla oficial del PR en `docs/revision-pr/pr-87-body.md`.
- **Decisiones y por qué:** Se integró la vista de **Cuenta** en `/merchant/plan` con el ícono `User` en `MerchantNav` tal como especifica Stitch (`C02`/`C07`/`C08`), unificando perfil del comercio, estado real del plan y cierre de sesión sin comisiones por viaje.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 423/423 tests en verde, incluyendo 50/50 en `route-integrity.test.ts`, 20/20 en `verify-workflows` y 6/6 en `verify-adr`) · test:db n.a.
- **Falta:** Segunda ronda de revisión independiente (`revisar-pr`).
- **Bloqueos:** ninguno.
- **Próximo paso:** Ejecutar Ronda 2 de `revisar-pr` sobre el nuevo SHA publicado.
- **Último commit:** d6efe14

## 2026-09-24 20:40 — Lautaro073 (agy)
- **Hecho:** Aplicación de correcciones para atender la Ronda 2 de revisión de PR #87 (`PR87-H01` a `PR87-H15`, `PR87-R01`, `PR87-R02`):
  0. Excepciones de alcance autorizadas por Lautaro073/P1 (`PR87-A01`/`A02`): documentadas explícitamente en `docs/tasks/T-118.md`, en esta bitácora y en el body real de GitHub PR #87 (`src/ui/brand-logo.tsx`, `src/ui/button.tsx`, `src/ui/top-bar.tsx`, `src/ui/ui-system.test.tsx`, `src/features/requests/index.ts`), sin revertir dichos cambios visuales ni ampliar otros archivos por analogía.
  1. `H01` / `H07` / `H09`: Incluido `src/app/(public)/layout.tsx` y la totalidad del alcance de T-118 en la auditoría de `src/app/route-integrity.test.ts`; reemplazados enlaces a `/terms` y `/privacy` por texto explícito `"en publicación · T-311"`; eliminadas todas las ocurrencias de `text-xs` en el alcance de T-118; ajustado `evaluateRouteGuard` en `src/features/auth/guards.ts` para no redirigir a `/admin` ni `/admin/mfa` inexistentes; añadidas pruebas de mutación en `route-integrity.test.ts`.
  2. `H02`: Sincronizada C08 (`/merchant/plan`, `src/features/merchants/copy.ts` y `queries.ts`) con el enum real de DB `merchant_subscription_status` (`pilot`, `active`, `expired`, `cancelled`) y pruebas unitarias para los 4 estados.
  3. `H03`: Sincronizada R08 (`/courier/profile` y `CourierProfileView`) con el enum real de DB `document_review_status` (`none`, `submitted`, `verified`, `rejected`), mapeando `submitted` a `"En revisión"`, `verified` a `"Validado por admin"` y combinando `dni_front` + `dni_back` mediante `combineDniDocumentStatus` donde `rejected` domina y solo ambos `verified` producen `"Validado por admin"`.
  4. `H06`: Propagación de errores de Supabase en queries y Server Components (`merchants/queries.ts`, `requests/queries.ts`, `courier/profile/page.tsx`, `merchant/dashboard/page.tsx`, `merchant/history/page.tsx`) lanzando excepción para activar los `error.tsx` de ruta y diferenciando falla de infraestructura de ausencia genuina de fila.
  5. `H05` / `R01` / `R02` / `H13`: Separadas las responsabilidades de C02 Dashboard (`getMerchantRequests` con métricas globales del comercio no truncadas a la página de 50 ítems y `avgRateArs` calculado sobre ofertas aceptadas reales o `null` / `"Sin datos"` cuando no hay ofertas aceptadas) y C07 Historial (`getMerchantHistoryRequests` aplicando `.eq('status', status)` en la query antes de `.limit(pageSize + 1)` y preservando `status` en `searchParams` y en el link de `nextCursor`, validando `cursorCreatedAt` y `cursorId` con Zod).
  6. `H14` y `H15`: Corregido `MerchantDashboardPage` para consultar `profile_id, business_name` en `merchants` en lugar de `id, name`; reemplazado `buttonVariants({ variant: 'primary' })` por `variant: 'default'`.
- **Decisiones y por qué:** Se mantuvieron intactos todos los archivos de `docs/revision-pr/pr-87/**` para que el revisor independiente ejecute la Ronda 3 sobre el nuevo SHA publicado.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 436/436 pruebas en verde + 20/20 workflows + 6/6 ADR) · build ✅ (`pnpm build` con 31 rutas generadas) · test:db n.a.
- **Falta:** Ejecución de Ronda 3 de `revisar-pr` sobre el nuevo SHA publicado.
- **Bloqueos:** ninguno.
- **Próximo paso:** Publicar commit en `origin/feat/T-118-integracion-visual-stitch`, actualizar el body real de PR #87 en GitHub y pasar a Ronda 3 de revisión.



