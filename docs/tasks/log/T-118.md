# Bitácora T-118 — Integración visual Stitch, shells y navegación canónica

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 15:22 — Lautaro073 (agy)
- **Hecho:** Tarea T-118 iniciada en worktree `cadeApp-T-118` (rama `feat/T-118-integracion-visual-stitch`). Verificada issue #85 asignada a Lautaro073. Convocados los tres asesores de El Consejo (`consejo-frontend`, `consejo-design`, `consejo-persona`) y sintetizados sus dictámenes unánimes: P01 placeholder obsoleto, 404 en rutas canónicas y enlaces internos, ausencia de layout de comercio, leak de BottomNav en onboarding de repartidor, desalineación de `redirectTo` en registro y desprotección de `/onboarding` en guards. Se redactó la suite `src/app/route-integrity.test.ts` con 27 pruebas cubriendo el DoD.
- **Decisiones y por qué:** Se ejecutan las pruebas del DoD demostrando 21 fallos en rojo antes de escribir implementación, cumpliendo la regla de TDD rojo primero y commit aislado.
- **Pruebas:** typecheck ✅ · lint ✅ · test ❌ (21 pruebas rojas intencionales en `src/app/route-integrity.test.ts`) · test:db n.a. · `pnpm vitest run src/app/route-integrity.test.ts` fallando por ausencia de P01, rutas canónicas inexistentes, `registerAction` sin onboarding y falta de layouts/shells.
- **Falta:** Commit de pruebas rojas, implementación de shells (`layout.tsx`, `merchant-nav.tsx`, ajuste `courier-nav.tsx`), implementación de P01 en `src/app/page.tsx`, `/forgot-password`, rutas de comercio canónicas (`dashboard`, `history`, `plan`, `requests/new`, `requests/[id]`, `onboarding`), rutas de repartidor canónicas (`onboarding/*`, `profile`), corrección de `guards.ts` y `actions.ts`, verificación en navegador y comprobación de accesibilidad.
- **Bloqueos:** ninguno.
- **Próximo paso:** Realizar commit de pruebas rojas y proceder a la implementación visual y de rutas canónicas.
- **Último commit:** da34d18

## 2026-09-24 16:00 — Lautaro073 (agy)
- **Hecho:** Implementación integral de T-118 completada de punta a punta. Se reemplazó el placeholder de Fase 0 por la landing P01 institucional de Aguilares en `src/app/page.tsx` con propuesta de valor, acciones duales de 52px y tarjeta educativa de flete directo. Creado `/forgot-password` con formulario y Server Action sin enumeración de usuarios. Creadas rutas canónicas de comercio (`/merchant/onboarding`, `/merchant/dashboard`, `/merchant/requests/new`, `/merchant/requests/[id]`, `/merchant/history` con `MerchantHistoryView`, `/merchant/plan` sin AFIP/ARBA). Creadas rutas canónicas de repartidor (`/courier/onboarding/{identity,vehicle,status}`, `/courier/feed`, `/courier/offers`, `/courier/profile` con `CourierProfileView` sin CBU/alias ni estrellas). Creados layouts y shells mobile-first (`max-w-[390px] mx-auto min-h-screen pb-20`) con TopBar marina (56px `#12182C`) con contraste asistido y BottomNavs específicos (`merchant-nav.tsx` y `courier-nav.tsx`) que se ocultan en onboarding. Eliminadas barras superiores duplicadas en las hojas cliente. Interceptación y redirección canónica en servidor de todas las rutas heredadas sin bucles ni renderizados duplicados. Aislamiento estricto de roles en `guards.ts` y enrutamiento en `registerAction`. Consulta con los 3 miembros de El Consejo (`consejo-frontend`, `consejo-design`, `consejo-persona`) con dictamen unánime favorable (APTO).
- **Decisiones y por qué:** No se alteraron archivos protegidos (`src/ui/**`, `src/domain/**`, contratos ni migraciones). Se resolvieron las reglas de contraste y boundaries arquitectónicos mediante imports canónicos y estilos scoped en layouts sin tocar `globals.css` ni dependencias externas. Se erradicaron de forma estricta los 7 datos fantasma de Stitch.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 errores, 0 warnings) · test ✅ (43 suites, 400/400 pruebas vitest verdes, incluyendo 27/27 en `route-integrity.test.ts`, 20/20 en verify-workflows y 6/6 en verify-adr) · build ✅ (`pnpm build` exitoso, 31 páginas estáticas/dinámicas generadas) · test:db n.a. (no se tocaron migraciones ni RPC).
- **Falta:** ninguna. Tarea terminada de punta a punta lista para revisión.
- **Bloqueos:** ninguno.
## 2026-09-24 16:50 — Lautaro073 (agy)
- **Hecho:** Corrección visual integral, responsividad y fidelidad de marca en la rama `feat/T-118-integracion-visual-stitch` (PR #87):
  1. Geometría auténtica de marca: se extrajeron los trazos vectoriales oficiales de `public/brand/logo.svg` y se integraron en `src/ui/brand-logo.tsx`.
  2. Corrección estructural en botones: en `src/ui/button.tsx` se añadió `inline-flex w-full items-center justify-center gap-2` en el contenedor interno, eliminando de raíz el apilamiento vertical de íconos sobre el texto.
  3. Responsividad plena en `/`: se erradicó el confinamiento rígido a 390px en desktop; ahora se expande fluidamente en tablet/desktop (`max-w-5xl mx-auto`), distribuyendo "¿Cómo funciona?" en 3 columnas y la sección dual inferior en 2 columnas, preservando el flujo vertical en móviles.
  4. Fidelidad de íconos en `/` y registro: se removió el badge de piloto del Hero colocando el isotipo oficial del logo; se reemplazaron los íconos de bicicleta y motos lineales genéricas por el isotipo auténtico del repartidor con mochila teal (`BrandLogo showWordmark={false}`) tanto en botones CTA como en tarjetas de onboarding.
- **Decisiones y por qué:** El usuario solicitó mantener la identidad gráfica fiel a los assets creados y corregir la experiencia en pantallas grandes sin romper los límites móviles.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (78/78 pruebas verdes en integridad, UI system y auth).
- **Falta:** Revisión visual con `/browser` desde la sesión de usuario local para pantallas `/login`, `/register` y shells de comercio/repartidor.
- **Bloqueos:** ninguno.
- **Próximo paso:** Continuar la inspección visual en `/login` mediante la instancia de agy con acceso al navegador.
- **Último commit:** 46066df

## 2026-09-24 19:45 — Lautaro073 (agy)
- **Hecho:** Cierre de los hallazgos de Ronda 1 (`PR87-H01` a `PR87-H12`) y unificación visual en todas las rutas canónicas:
  1. `H01`: `getRoleDefaultPath('admin')` apunta a destino existente (`/`); se reemplazaron enlaces rotos a `/terms` y `/privacy` por avisos explícitos de documentos en publicación (`T-311`).
  2. `H02`: `MerchantPlanPage` (`/merchant/plan`, tab **Cuenta**) consulta `subscription_status`, `paid_until` y datos de perfil reales desde `merchants` y `profiles`, o muestra `EmptyState` honesto cuando no hay registro, sin menciones a comisiones ni bloques AFIP/ARBA.
  3. `H03`: `CourierProfilePage` (`/courier/profile`) y `CourierProfileView` leen `couriers` y `courier_documents` reales sin fabricar `moto`, `approved` ni verificaciones fijas; en vehículos `walk`/`bike` se ocultan patente, licencia, seguro e incentivo documental.
  4. `H04` y `H05`: `getMerchantRequests` pagina con `limit <= 50` y cursor (`created_at`/`id` + `nextCursor`), y `formatApproxDistanceKm`/zonas conservan `null` / ausencia honesta en vez de `1,0 km` o `Centro`/`Aguilares`.
  5. `H06`: Creados `loading.tsx` (con `Skeleton` de `src/ui`) y `error.tsx` en los segmentos canónicos con datos de `(merchant)/merchant/**` y `(courier)/courier/**`.
  6. `H07` y `H08`: Eliminados `text-xs`, switch `h-7 w-12`, tabs `min-h-10`, bloques `<style>` y hex arbitrario `#25D366`, respetando piso tipográfico de 14 px y targets táctiles >= 48×48 px.
  7. `H09`, `H10`, `H11` y `H12`: Ampliada `src/app/route-integrity.test.ts` (50 pruebas cubriendo páginas y hojas renderizadas), verificado en `/browser` a 390 px y 1280 px, y restaurada la plantilla oficial del PR en `docs/revision-pr/pr-87-body.md`.
- **Decisiones y por qué:** Se integró la vista de **Cuenta** en `/merchant/plan` con el ícono `User` en `MerchantNav` tal como especifica Stitch (`C02`/`C07`/`C08`), unificando perfil del comercio, estado real del plan y cierre de sesión sin comisiones por viaje.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 423/423 tests en verde, incluyendo 50/50 en `route-integrity.test.ts`, 20/20 en `verify-workflows` y 6/6 en `verify-adr`) · test:db n.a.
- **Falta:** Segunda ronda de revisión independiente (`revisar-pr`).
- **Bloqueos:** ninguno.
- **Próximo paso:** Ejecutar Ronda 2 de `revisar-pr` sobre el nuevo SHA publicado.
- **Último commit:** d6efe14

## 2026-09-24 20:40 — Lautaro073 (agy)
- **Hecho:** Aplicación de correcciones para atender la Ronda 2 de revisión de PR #87 (`PR87-H01` a `PR87-H15`, `PR87-R01`, `PR87-R02`):
  0. Excepciones de alcance autorizadas por Lautaro073/P1 (`PR87-A01`/`A02`): documentadas explícitamente en `docs/tasks/T-118.md`, en esta bitácora y en el body real de GitHub PR #87 (`src/ui/brand-logo.tsx`, `src/ui/button.tsx`, `src/ui/top-bar.tsx`, `src/ui/ui-system.test.tsx`, `src/features/requests/index.ts`), sin revertir dichos cambios visuales ni ampliar otros archivos por analogía.
  1. `H01` / `H07` / `H09`: Incluido `src/app/(public)/layout.tsx` y la totalidad del alcance de T-118 en la auditoría de `src/app/route-integrity.test.ts`; reemplazados enlaces a `/terms` y `/privacy` por texto explícito `"en publicación · T-311"`; eliminadas todas las ocurrencias de `text-xs` en el alcance de T-118; ajustado `evaluateRouteGuard` en `src/features/auth/guards.ts` para no redirigir a `/admin` ni `/admin/mfa` inexistentes; añadidas pruebas de mutación en `route-integrity.test.ts`.
  2. `H02`: Sincronizada C08 (`/merchant/plan`, `src/features/merchants/copy.ts` y `queries.ts`) con el enum real de DB `merchant_subscription_status` (`pilot`, `active`, `expired`, `cancelled`) y pruebas unitarias para los 4 estados.
  3. `H03`: Sincronizada R08 (`/courier/profile` y `CourierProfileView`) con el enum real de DB `document_review_status` (`none`, `submitted`, `verified`, `rejected`), mapeando `submitted` a `"En revisión"`, `verified` a `"Validado por admin"` y combinando `dni_front` + `dni_back` mediante `combineDniDocumentStatus` donde `rejected` domina y solo ambos `verified` producen `"Validado por admin"`.
  4. `H06`: Propagación de errores de Supabase en queries y Server Components (`merchants/queries.ts`, `requests/queries.ts`, `courier/profile/page.tsx`, `merchant/dashboard/page.tsx`, `merchant/history/page.tsx`) lanzando excepción para activar los `error.tsx` de ruta y diferenciando falla de infraestructura de ausencia genuina de fila.
  5. `H05` / `R01` / `R02` / `H13`: Separadas las responsabilidades de C02 Dashboard (`getMerchantRequests` con métricas globales del comercio no truncadas a la página de 50 ítems y `avgRateArs` calculado sobre ofertas aceptadas reales o `null` / `"Sin datos"` cuando no hay ofertas aceptadas) y C07 Historial (`getMerchantHistoryRequests` aplicando `.eq('status', status)` en la query antes de `.limit(pageSize + 1)` y preservando `status` en `searchParams` y en el link de `nextCursor`, validando `cursorCreatedAt` y `cursorId` con Zod).
  6. `H14` y `H15`: Corregido `MerchantDashboardPage` para consultar `profile_id, business_name` en `merchants` en lugar de `id, name`; reemplazado `buttonVariants({ variant: 'primary' })` por `variant: 'default'`.
- **Decisiones y por qué:** Se mantuvieron intactos todos los archivos de `docs/revision-pr/pr-87/**` para que el revisor independiente ejecute la Ronda 3 sobre el nuevo SHA publicado.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 436/436 pruebas en verde + 20/20 workflows + 6/6 ADR) · build ✅ (`pnpm build` con 31 rutas generadas) · test:db n.a.
- **Falta:** Ejecución de Ronda 3 de `revisar-pr` sobre el nuevo SHA publicado.
- **Bloqueos:** ninguno.
- **Próximo paso:** Publicar commit en `origin/feat/T-118-integracion-visual-stitch`, actualizar el body real de PR #87 en GitHub y pasar a Ronda 3 de revisión.
- **Último commit:** 17fc499

## 2026-09-24 21:35 — Lautaro073 (agy)
- **Hecho:** Cierre de los 8 hallazgos bloqueantes de Ronda 3 (`PR87-H01`, `PR87-H09`, `PR87-H10`, `PR87-H16`, `PR87-H17`, `PR87-H18`, `PR87-R03`, `PR87-H19`) con ciclo TDD Rojo → Verde verificado para cada propiedad:
  1. `PR87-H01` (`src/features/auth/guards.ts`, `guards.test.ts`): Eliminadas de `isPublicRoute` las rutas inexistentes `/terms`, `/privacy`, `/pilot-terms` y `/legal`. Añadido validador de ruta conocida por rol (`isKnownExistingRouteForRole`) en `resolvePostLoginRedirect` para que cualquier ruta arbitraria o inexistente (`/ruta-inexistente`, `/ghost`, `/terms`, `/privacy`, `/pilot-terms`, `/legal`, `/admin`, `/admin/mfa`) caiga al destino seguro por defecto del rol (`getRoleDefaultPath(role)`) en vez de redirigir a 404.
     - **Evidencia Rojo → Verde:** En rojo, `resolvePostLoginRedirect('/ruta-inexistente', 'merchant')` devolvió `'/ruta-inexistente'` (`Expected: "/merchant/dashboard", Received: "/ruta-inexistente"`). Tras el fix, `12/12` pruebas en `src/features/auth/guards.test.ts` en verde.
  2. `PR87-H09` (`src/app/route-integrity.test.ts`): Reemplazada la blacklist de 4 strings por el resolutor general contra el filesystem de `src/app` (`resolveRouteToFilesystemPage` + `resolveSegmentsInDir`), soportando route groups `(public)`, `(merchant)`, `(courier)`, `(admin)` y segmentos dinámicos `[id]`. Auditados todos los `href`, `redirectTo`, `redirect()` y `router.push/replace()` del alcance de T-118.
     - **Evidencia Rojo → Verde:** En rojo, las mutaciones `<Link href="/ghost">`, `<Link href="/ruta-inexistente">` y `redirectTo: '/ghost'` no eran detectadas (`Expected function to throw`). Tras el fix, cualquier ruta interna inexistente lanza error y las `50/50` pruebas de `src/app/route-integrity.test.ts` pasan en verde.
  3. `PR87-H10` (Evidencia visual real en GitHub PR #87): Generadas 23 capturas PNG reales en Chromium (`msedge.exe --headless` sobre el CSS compilado de Next.js `.next/static/css/*.css` y componentes reales) cubriendo comparativa lado a lado Stitch vs Implementación a `390×844` para Familias P (`P01`, `P02`, `P03`, `P04`), C (`C01`, `C02`, `C03`, `C04`, `C07`, `C08`) y R (`R01`, `R02`, `R03`, `R05`, `R08`), controles a `360×800` (`ctrl_360_p02_login.png`, `ctrl_360_c02_c07_merchant.png`, `ctrl_360_r03_r08_courier.png`), los 4 estados (`state_loading_390.png`, `state_empty_390.png`, `state_error_390.png`, `state_pending_390.png`) y controles de accesibilidad (`a11y_focus_targets_safearea_390.png`). Publicadas en `feat/T-118-visual-assets` (`bc27f5eea7fbdeb07b224d9b908b1259e8c51a2a`, `HTTP 200 OK`, `Content-Type: image/png`) e incrustadas como imágenes visibles (`<img>` / `![]()`) tanto en el body real de PR #87 como en un comentario de evidencia en PR #87, sin contaminar los archivos de `feat/T-118-integracion-visual-stitch`.
  4. `PR87-H16` (`src/features/merchants/copy.ts`, `queries.test.ts`): Implementado `formatCivilDateEsAr(civilDateStr)` que parsea directamente el string civil PostgreSQL `YYYY-MM-DD` sin `new Date('YYYY-MM-DD')`, evitando el corrimiento al día anterior en zonas horarias `UTC-3`.
     - **Evidencia Rojo → Verde:** Con `paid_until: '2026-12-31'` en `TZ="America/Argentina/Buenos_Aires"`, en rojo devolvió `"30 de diciembre de 2026"`. Con `formatCivilDateEsAr`, devuelve `"31 de diciembre de 2026"` tanto en `TZ="UTC"` como en `TZ="America/Argentina/Buenos_Aires"` (`7/7` pruebas en `src/features/merchants/queries.test.ts` en verde).
  5. `PR87-H17` (`src/features/requests/queries.ts`, `merchant-history-view.tsx`, `queries.test.ts`, `merchant-requests-list.test.tsx`): En `getMerchantHistoryRequests`, cuando `status === 'all'` se aplica `query.in('status', ['delivered', 'cancelled', 'expired'])` antes de `.limit(pageSize + 1)`, excluyendo solicitudes activas (`published`, `matched`, `in_transit`) antes de paginar. `MerchantHistoryView` también filtra defensivamente a estados terminales.
     - **Evidencia Rojo → Verde:** En rojo, `in:status=[delivered,cancelled,expired]` no existía antes de `limit:51` (`expected -1 to be greater than -1`). En verde, el filtro `.in()` se ejecuta antes de `.limit(51)`.
  6. `PR87-H18` (`src/features/requests/types.ts`, `queries.ts`, `merchant-history-view.tsx`, `queries.test.ts`, `merchant-requests-list.test.tsx`): Hidratados `acceptedAmountArs` y `acceptedCourierName` en `getMerchantHistoryRequests` consultando `offers`, `couriers` y `profiles` para las filas con `accepted_offer_id`. En `MerchantHistoryView` se muestra `<Nombre Repartidor> · $ <Monto>` cuando hay oferta aceptada y `"Sin repartidor asignado · Sin tarifa acordada"` cuando la solicitud fue cancelada o venció sin oferta aceptada.
     - **Evidencia Rojo → Verde:** En rojo, `acceptedAmountArs` era `undefined` (`expected undefined to be 2300`). En verde, `queries.test.ts` (`12/12`) y `merchant-requests-list.test.tsx` (`5/5`) verifican el hidratado y renderizado real.
  7. `PR87-R03` y `PR87-H19` (`src/features/requests/queries.ts`, `queries.test.ts`): Eliminada la query ilimitada de métricas en `getMerchantRequests`, reemplazándola por lotes acotados de `.limit(50)` con cursor (`METRICS_BATCH_LIMIT = 50`). Añadido `getAguilaresDayBoundsUtc(now)` calculando el inicio y fin del día civil en `America/Argentina/Buenos_Aires` (`UTC-3`) para que tanto `dispatchedToday` como las ofertas aceptadas de `avgRateArs` correspondan exclusivamente al día civil de Aguilares (`2026-09-25T01:30:00Z` = `2026-09-24 22:30` en Aguilares).
     - **Evidencia Rojo → Verde:** En rojo, `unboundedQueries` detectó `['unbounded-delivery-requests-query']`. En verde, `unboundedQueries` es `[]`, `dispatchedToday === 1` y `avgRateArs === 2400` sin contaminar con ofertas de días previos (`12/12` en `src/features/requests/queries.test.ts`).
- **Decisiones y por qué:** `docs/revision-pr/pr-87/**` permaneció 100% intacto (`git diff` vacío sobre esa ruta) y se preservaron todas las excepciones autorizadas por Lautaro073/P1 (`A01`/`A02`).
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 440/440 pruebas Vitest en verde + 20/20 workflows + 6/6 ADR) · build ✅ (`pnpm build` con 31 rutas generadas) · test:db n.a.
- **Falta:** Revalidación final de Ronda 4 por el revisor independiente.
- **Bloqueos:** ninguno.
- **Próximo paso:** Commit, `git push origin feat/T-118-integracion-visual-stitch`, actualización del body e imágenes en PR #87 y notificación al usuario.
- **Último commit:** cf6fa22

## 2026-09-25 00:44 — Lautaro073 (agy)
- **Hecho:** Cierre de los 4 hallazgos bloqueantes de Ronda 4 (`PR87-H09`, `PR87-H10`, `PR87-R04`, `PR87-H20`) con ciclo TDD Rojo → Verde verificado para cada propiedad:
  1. `PR87-H09` (`src/app/route-integrity.test.ts`, `src/features/auth/guards.ts`): Ampliado `assertNoInvalidInternalLinks` para extraer tanto literales entre comillas como template literals (`href={`...`}`, `redirectTo: `...``, `redirect(`...`)`, `router.push/replace(`...`)`), normalizando cada interpolación `${...}` a un segmento estable (`__dynamic_segment__`) y resolviendo el path contra el filesystem (`resolveRouteToFilesystemPage`). Además, el nuevo scanner detectó y permitió corregir en `src/features/auth/guards.ts` la redirección dinámica `/requests/${id}` para que emita explícitamente ``/merchant/requests/${requestId}`` solo cuando hay un segmento válido.
     - **Evidencia Rojo → Verde:** En rojo, las mutaciones `<Link href={`/ghost/${id}`}>`, `router.push(`/ghost/${id}`)`, `redirectTo: `/ghost/${id}`` y `redirect(`/ghost/${id}`)` no lanzaban error (`AssertionError: expected [Function] to throw an error`). En verde, todas lanzan error y las `50/50` pruebas de `src/app/route-integrity.test.ts` pasan en verde.
  2. `PR87-H10` (Matriz visual completa según READMEs vinculantes en GitHub PR #87): Añadidas y publicadas en `feat/T-118-visual-assets` (`5af9cac26a9dd91bc6ce4a4a77c2c1f7bb5cfd8b`, `HTTP 200 OK`) las capturas específicas de `C05` (`c05_confirm_accept_dialog_390.png` frente a `C05-confirmar-aceptacion.png`), `R02` individual (`r02_courier_onboarding_vehicle_390.png` frente a `R02-onboarding-vehiculo-y-consentimientos.png`), `R05` Bottom Sheet de ofertar (`r05_courier_offer_sheet_390.png` frente a `R05-ofertar.png`), `R06` Mis ofertas (`r06_courier_my_offers_390.png` frente a `R06-mis-ofertas.png`) y la evidencia específica de `prefers-reduced-motion: reduce` (`a11y_reduced_motion_390.png`). Corregida la tabla de Familia P para describir `P01–P03 + /forgot-password funcional` sin usar `P04-documento-legal.png` (que pertenece a `T-311`).
  3. `PR87-R04` (`src/features/requests/queries.ts`, `queries.test.ts`): Eliminados los topes globales silenciosos `MAX_METRICS_BATCHES = 10` (500 solicitudes) y `todayAcceptedOfferIds.slice(0, 50)` (50 ofertas). Ahora `getMerchantRequests` pagina en lotes de `<= 50` filas hasta agotamiento (`batchRows.length < 50 || newRowsAdded === 0`) y consulta `offers` en lotes de `<= 50` IDs hasta cubrir todas las ofertas aceptadas del día civil de Aguilares.
     - **Evidencia Rojo → Verde:** Con `501` solicitudes (donde la `#501` es la única `delivered` del día) y `51` ofertas aceptadas (`50` de `$1000` + la `#51` de `$10000`), en rojo devolvió `dispatchedToday: 0` (`expected +0 to be 1`) y `avgRateArs: 1000`. En verde devuelve `dispatchedToday: 1` y `avgRateArs: 1176` (`Math.round(60000 / 51)`), pasando `14/14` en `src/features/requests/queries.test.ts`.
  4. `PR87-H20` (`src/features/requests/queries.ts`, `merchant-requests-list.tsx`, `queries.test.ts`, `merchant-requests-list.test.tsx`): `getMerchantRequests` aplica `pageQuery.in('status', ['published', 'matched', 'in_transit'])` antes de `.limit(pageSize + 1)` cuando `status === 'all'`, y `MerchantRequestsList` filtra defensivamente solo estados activos (`published | matched | in_transit`), mostrando el `EmptyState` `"No tenés solicitudes activas"` cuando solo existen solicitudes terminales (`delivered | cancelled | expired`).
     - **Evidencia Rojo → Verde:** En rojo, `in:status=[published,matched,in_transit]` daba `-1` (`expected -1 to be greater than -1`) y `MerchantRequestsList` renderizaba `Zona Terminal Intrusa` (`expected <span>Zona Terminal Intrusa</span> to be null`). En verde, `queries.test.ts` (`14/14`) y `merchant-requests-list.test.tsx` (`6/6`) pasan al 100%.
- **Decisiones y por qué:** `docs/revision-pr/pr-87/**` permaneció 100% intacto (`git diff` vacío) y se conservaron las excepciones autorizadas por Lautaro073/P1 (`A01`/`A02`).
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 443/443 pruebas Vitest en verde + 20/20 workflows + 6/6 ADR) · build ✅ (`pnpm build` con 31 rutas generadas) · test:db n.a.
- **Falta:** Revisión final sobre el nuevo SHA publicado.
- **Bloqueos:** ninguno.
- **Próximo paso:** Commit, `git push origin feat/T-118-integracion-visual-stitch`, actualización de PR #87 y reporte al usuario.
- **Último commit:** bdece0f

## 2026-09-25 01:55 — Lautaro073 (agy)
- **Hecho:** Cierre del único hallazgo bloqueante de Ronda 5 (`PR87-H09` — navegación indirecta en `src/app/route-integrity.test.ts`) tras `git pull` del head de revisión `a2d20a498c5b7d3515a1d3efe25b6b4e8142e7c7`, más actualización de las 28 capturas PNG en `feat/T-118-visual-assets` (`4a6e59af489fac8496747ddbde2e55dde6a9d858`) renderizadas directamente desde los componentes React actuales (`BrandLogo` vectorial, `TopBar`, `MerchantNav` de 3 tabs con `Cuenta`, `CourierNav`, `MerchantPlanPage`, `MerchantHistoryView`, `RequestOffersList` + Dialog `C05`, `OfferSheet` `R05`, etc.) sobre el CSS compilado de `.next/static/css/*.css`:
  1. `PR87-H09` (`src/app/route-integrity.test.ts`):
     - Ampliado `patterns` y `assertNoInvalidInternalLinks` para cubrir las 4 formas de navegación indirecta además de JSX literal/template y redirects directos:
       - `navObject`: propiedades de objeto `href: '/...'` y `href: \`/...\`` (como `navItems` en `merchant-nav.tsx` y `courier-nav.tsx`);
       - `helperCall`: llamadas en JSX `href={helperName(...)}` (como `href={buildNextCursorHref(nextCursor)}` en `MerchantHistoryView`), resolviendo la declaración del helper en el archivo y validando que su retorno exista en el filesystem (o lanzando error `/inexistente/` si el helper no tiene productor verificable o retorna una ruta inexistente);
       - `helperReturn`: sentencias `return '/...'` y `return \`/...\`` (como `return \`/merchant/history?\${params.toString()}\`` en `buildNextCursorHref` y `getRoleDefaultPath` en `guards.ts`);
       - `routerVariable`: llamadas `router.push(expr)` / `router.replace(expr)` con variables o propiedades indirectas (`router.push(targetUrl)`, `router.push(result.data.redirectTo)`), resolviendo la asignación de la variable (`resolvePostLoginRedirect` o literal/template) y el productor de la Server Action (`registerAction`, `merchantOnboardingAction`, `createDeliveryRequestAction`, `courierOnboardingAction`), lanzando `/inexistente/` ante variables sin ruta verificable o destinos inexistentes.
     - Además, en `DoD 5` (`src/app/route-integrity.test.ts`), se extraen todas las propiedades `href:` de `merchant-nav.tsx` y `courier-nav.tsx` y se exige igualdad exacta (`toEqual(['/merchant/dashboard', '/merchant/history', '/merchant/plan'])` y `toEqual(['/courier/feed', '/courier/offers', '/courier/profile'])`), por lo que agregar un cuarto `navItem` con `href: '/ghost'` falla simultáneamente en `DoD 4` y `DoD 5`.
     - **Evidencia Rojo → Verde:**
       - En rojo (antes de actualizar `assertNoInvalidInternalLinks`), `npx vitest run src/app/route-integrity.test.ts` falló en `src/app/route-integrity.test.ts:375` (`AssertionError: expected [Function] to throw an error` para `mutated-merchant-nav.tsx` con un 4to item `{ href: '/ghost' }`).
       - En prueba de mutación sobre el archivo real `src/app/(merchant)/merchant-nav.tsx` agregando un 4to item `{ id: 'ghost', label: 'Fantasma', href: '/ghost' }`, `npx vitest run src/app/route-integrity.test.ts` falló en rojo en `DoD 4` (`Error: src/app/(merchant)/merchant-nav.tsx referencia una ruta interna inexistente en el filesystem: /ghost`) y en `DoD 5` (`expected [ '/merchant/dashboard', '/merchant/history', '/merchant/plan', '/ghost' ] to deeply equal [ '/merchant/dashboard', '/merchant/history', '/merchant/plan' ]`).
       - Probe independiente JS sobre `patterns`: `jsxLiteral: true`, `jsxTemplate: true`, `navObject: true`, `helperCall: true`, `helperReturn: true`, `routerVariable: true`.
       - En verde (tras revertir la mutación de prueba), `50/50` pruebas en `src/app/route-integrity.test.ts` pasan en verde.
- **Decisiones y por qué:** `docs/revision-pr/pr-87/**` permaneció 100% intacto (`git diff` vacío) y no se reabrieron `H10`, `R04` ni `H20` (cerrados en R5). Se actualizaron los PNG de `feat/T-118-visual-assets` (`4a6e59af489fac8496747ddbde2e55dde6a9d858`) para reflejar el árbol React actual en vivo.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (43/43 suites, 443/443 pruebas Vitest en verde + 20/20 workflows + 6/6 ADR) · build ✅ (`pnpm build` con 31 rutas generadas) · test:db n.a.
- **Falta:** Verificación de checks de CI en verde sobre el nuevo SHA y revalidación final de Ronda 5.
- **Bloqueos:** ninguno.
- **Próximo paso:** Commit, `git push origin feat/T-118-integracion-visual-stitch`, actualización de PR #87 y monitoreo de CI.

