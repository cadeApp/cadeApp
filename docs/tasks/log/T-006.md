# Bitácora T-006 — Estados, `DomainErrorCode`, `ActionResult`, `rpc-contracts.ts`

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 20:32 — Lautaro073 (agy)

- **Hecho:**
  1. Inicio de tarea `T-006` (Issue #7 asignado a `Lautaro073`) con la skill `tomar-tarea`. Creación de la rama `feat/T-006-contracts-v1` desde `origin/develop` (`de6fbf3`).
  2. **Fase Roja (TDD):** Creación de `src/domain/domain.test.ts` cubriendo todos los puntos del DoD (transiciones válidas e inválidas de `delivery_requests` y `offers` por actor `merchant`/`courier`/`admin`/`system`, expiración perezosa, ordenamiento de ofertas por `doc_level` descendente + `created_at` ascendente y por precio, validación de piso dinámico `999`/`1000`/`1001` y `1500`, bounding box de Aguilares y Haversine × 1.30 redondeado a 500 m, contratos de las 18 RPCs y fake devolviendo todos los códigos de `DomainErrorCode`, pureza de `src/domain` y cobertura de ramas ≥ 90%). Corrida `pnpm vitest run src/domain/domain.test.ts` demostrada fallando en rojo (`FAIL src/domain/domain.test.ts — Error: Failed to resolve import "./errors" from "src/domain/domain.test.ts"`).
  3. **Diseño con El Consejo (`consejo-backend` y `consejo-frontend`):**
     - `src/domain/errors.ts`: `DOMAIN_ERROR_CODES` (`ALL_DOMAIN_ERROR_CODES`, 27 códigos), `DomainErrorCode`, `ActionResult<T, E>`, `ok()`, `err()`, `isOk()`, `isErr()`, `isDomainErrorCode()`.
     - `src/domain/schemas/index.ts`: todos los enums SQL de `20260922031435_schema_v1.sql` como schemas Zod puros (sin importar `src/types/database.types.ts` para cumplir estrictamente `boundaries/element-types` de `.eslintrc.json` donde `from: "domain"` solo permite `["domain"]`), bounding box oficial de Aguilares (`-27.4550` a `-27.4100` lat, `-65.6400` a `-65.5950` lng), par de coordenadas todo-o-nada (`aguilaresCoordPairSchema`), distancia Haversine × 1.30 redondeada a múltiplos de 500 m (`calculateHaversineRouteDistanceM`) y schema de monto de oferta con piso dinámico (`createOfferAmountArsSchema(minOfferArs)`).
     - `src/domain/states/index.ts`: máquina de estados de `delivery_requests` (`transitionRequest`, `canTransitionRequest`, `isRequestExpired`, `getEffectiveRequestStatus`), de `offers` (`transitionOffer`, `canTransitionOffer`), de repartidores (`canCourierSubmitOffer`, `canCourierBeAccepted`) y comercios (`canMerchantPublishRequest`).
     - `src/domain/priority.ts`: `computeDocLevel` (`0 | 1 | 2`), `getCourierVerifiedBadges` (solo `status === 'verified'`), `getCourierDeclaredBadges` y `sortOffersForMerchant` (`'doc_level'` y `'price'`).
     - `src/domain/rpc-contracts.ts`: contratos de entrada/salida Zod y `errorCodes` tipados para las **18 RPCs** de `docs/master-plan.md` §7 (`publish_request`, `cancel_request`, `submit_offer`, `withdraw_offer`, `accept_offer`, `mark_picked_up`, `mark_delivered`, `report_no_show`, `courier_cancel_match`, `republish_request`, `report_incident`, `set_availability`, `calculate_route_distance`, `admin_decide_courier`, `admin_suspend_courier`, `admin_verify_document`, `admin_set_subscription`, `admin_update_setting`).
     - `src/domain/testing/rpc-fake.ts`: `createFakeRpcClient` con estado en memoria, validación contra los schemas Zod de `RPC_CONTRACTS` y método `triggerErrorCode(code)` / `setForcedError(code)` capaz de devolver **cada uno de los 27 códigos de `DomainErrorCode`**.
     - `src/domain/index.ts`: re-exportación limpia de toda la API pública del dominio.
  4. **Fase Verde (TDD):** `pnpm typecheck`, `pnpm lint` y `pnpm test` en verde. Cobertura de ramas (`pnpm vitest run src/domain/domain.test.ts --coverage`):
     - `domain/errors.ts`: 100% ramas
     - `domain/index.ts`: 100% ramas
     - `domain/priority.ts`: 100% ramas
     - `domain/rpc-contracts.ts`: 100% ramas
     - `domain/schemas/index.ts`: 100% ramas
     - `domain/states/index.ts`: 98.3% ramas
     - `domain/testing/rpc-fake.ts`: 90.21% ramas
- **Decisiones y por qué:**
  - `src/domain/**` no importa `@/types/database.types` porque `.eslintrc.json` (`boundaries/element-types`) define `{ "from": "domain", "allow": ["domain"] }`. Todos los tipos y enums se definen en `src/domain/schemas/index.ts` en correspondencia 1:1 con `20260922031435_schema_v1.sql`.
  - El umbral de cobertura de ramas ≥ 90% de `src/domain/**` se verifica tanto con `@vitest/coverage-v8` como mediante una aserción automática en `src/domain/domain.test.ts` sobre `coverage/coverage-final.json`, sin necesidad de modificar archivos fuera de `Archivos permitidos`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ · test:db n.a. · prueba demostrada fallando antes: `src/domain/domain.test.ts` falló en rojo antes de implementar `src/domain/**` (`Failed to resolve import "./errors" from "src/domain/domain.test.ts"`).
- **Falta:** Abrir el PR hacia `develop` y ejecutar `revisar-pr`.
- **Bloqueos:** ninguno (nota: cuando existe `src/domain/rpc-contracts.ts`, el job `audit` de `.github/workflows/ci.yml` pasa de advisory a bloqueante y ejecuta `pnpm audit --audit-level=high` sobre `package.json`; si se desea actualizar `pnpm.overrides` en `package.json` y `thresholds` en `vitest.config.ts`, requiere autorización de Lautaro073 para incluirlos en `Archivos permitidos`).
- **Próximo paso:** Commit, push, apertura de PR #58 y ejecución de `revisar-pr`.
- **Último commit:** pendiente de crear en este cierre.
