# Bitácora T-006 — Estados, `DomainErrorCode`, `ActionResult`, `rpc-contracts.ts`

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 20:32 — Lautaro073 (agy)

- **Hecho:**
  1. Inicio de tarea `T-006` (Issue #7 asignado a `Lautaro073`) con la skill `tomar-tarea`. Creación de la rama `feat/T-006-contracts-v1` desde `origin/develop` (`de6fbf3`).
  2. **Fase Roja (TDD):** Creación de `src/domain/domain.test.ts` cubriendo todos los puntos del DoD (transiciones válidas e inválidas de `delivery_requests` y `offers` por actor `merchant`/`courier`/`admin`/`system`, expiración perezosa, ordenamiento de ofertas por `doc_level` descendente + `created_at` ascendente y por precio, validación de piso dinámico `999`/`1000`/`1001` y `1500`, bounding box de Aguilares y Haversine × 1.30 redondeado a 500 m, contratos de las 18 RPCs y fake devolviendo todos los códigos de `DomainErrorCode`, pureza de `src/domain` y cobertura de ramas ≥ 90%). Corrida `pnpm vitest run src/domain/domain.test.ts` demostrada fallando en rojo (`FAIL src/domain/domain.test.ts — Error: Failed to resolve import "./errors" from "src/domain/domain.test.ts"`).
  3. **Diseño con El Consejo (`consejo-backend` y `consejo-frontend`):**
     - `src/domain/errors.ts`: `DOMAIN_ERROR_CODES` (`ALL_DOMAIN_ERROR_CODES`, 27 códigos), `DomainErrorCode`, `ActionResult<T, E>`, `ok()`, `err()`, `isOk()`, `isErr()`, `isDomainErrorCode()`.
     - `src/domain/schemas/index.ts`: todos los enums SQL de `20260922031435_schema_v1.sql` como schemas Zod puros (sin importar `src/types/database.types.ts` para cumplir estrictamente `boundaries/element-types` de `.eslintrc.json` donde `from: "domain"` solo permite `["domain"]`), bounding box oficial de Aguilares (`-27.4550` a `-27.4100` lat, `-65.6400` a `-65.5950` lng), par de coordenadas todo-o-nada (`aguilaresCoordPairSchema`), distancia Haversine × 1.30 redondeada a múltiplos de 500 m (`calculateHaversineRouteDistanceM`) y schema de monto de oferta con piso dinámico (`createOfferAmountArsSchema(minOfferArs)`).
     - `src/domain/states/index.ts`: máquina de estados de `delivery_requests` (`transitionRequest`, `canTransitionRequest`, `isRequestExpired`, `getEffectiveRequestStatus`), de `offers` (`transitionOffer`, `canTransitionOffer`), de repartidores (`canCourierSubmitOffer`, `canCourierBeAccepted`) y comercios (`canMerchantPublishRequest`).
     - `src/domain/priority.ts`: `computeDocLevel` (`0 | 1 | 2`), `getCourierVerifiedBadges` (solo `status === 'verified'`), `getCourierDeclaredBadges` y `sortOffersForMerchant` (`'doc_level'` y `'price'`).
     - `src/domain/rpc-contracts.ts`: contratos de entrada/salida Zod y `errorCodes` tipados para las **18 RPCs** de `docs/master-plan.md` §7 (`publish_request`, `cancel_request`, `submit_offer`, `withdraw_offer`, `accept_offer`, `mark_picked_up`, `mark_delivered`, `report_no_show`, `courier_cancel_match`, `republish_request`, `report_incident`, `set_availability`, `calculate_route_distance`, `admin_decide_courier`, `admin_suspend_courier`, `admin_verify_document`, `admin_set_subscription`, `admin_update_setting`).
     - `src/domain/testing/rpc-fake.ts`: `createFakeRpcClient` con estado en memoria, validación contra los schemas Zod de `RPC_CONTRACTS` y método `triggerErrorCode(code)` / `setForcedError(code)` capaz de devolver **cada uno de los 27 códigos de `DomainErrorCode`**.
     - `src/domain/index.ts`: re-exportación limpia de toda la API pública del dominio.
  4. **Fase Verde (TDD):** `pnpm typecheck`, `pnpm lint` y `pnpm test` en verde. Cobertura de ramas (`pnpm vitest run src/domain/domain.test.ts --coverage`):
     - `domain/errors.ts`: 100% ramas
     - `domain/index.ts`: 100% ramas
     - `domain/priority.ts`: 100% ramas
     - `domain/rpc-contracts.ts`: 100% ramas
     - `domain/schemas/index.ts`: 100% ramas
     - `domain/states/index.ts`: 98.3% ramas
     - `domain/testing/rpc-fake.ts`: 90.21% ramas
- **Decisiones y por qué:**
  - `src/domain/**` no importa `@/types/database.types` porque `.eslintrc.json` (`boundaries/element-types`) define `{ "from": "domain", "allow": ["domain"] }`. Todos los tipos y enums se definen en `src/domain/schemas/index.ts` en correspondencia 1:1 con `20260922031435_schema_v1.sql`.
  - El umbral de cobertura de ramas ≥ 90% de `src/domain/**` se verifica tanto con `@vitest/coverage-v8` como mediante una aserción automática en `src/domain/domain.test.ts` sobre `coverage/coverage-final.json`, sin necesidad de modificar archivos fuera de `Archivos permitidos`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ · test:db n.a. · prueba demostrada fallando antes: `src/domain/domain.test.ts` falló en rojo antes de implementar `src/domain/**` (`Failed to resolve import "./errors" from "src/domain/domain.test.ts"`).
- **Falta:** Abrir el PR hacia `develop` y ejecutar `revisar-pr`.
- **Bloqueos:** ninguno (nota: cuando existe `src/domain/rpc-contracts.ts`, el job `audit` de `.github/workflows/ci.yml` pasa de advisory a bloqueante y ejecuta `pnpm audit --audit-level=high` sobre `package.json`; si se desea actualizar `pnpm.overrides` en `package.json` y `thresholds` en `vitest.config.ts`, requiere autorización de Lautaro073 para incluirlos en `Archivos permitidos`).
- **Próximo paso:** Apertura de PR #58 hacia `develop` con el informe de `revisar-pr` adjunto.
- **Último commit:** afe3631

## 2026-09-22 21:50 — Lautaro073 (agy)

- **Hecho:**
  1. Sincronización de `origin/feat/T-006-contracts-v1` (`a71ec25`) con el informe de Ronda 1 (`docs/revision-pr/pr-58/revisiones/ronda-1.md`) y autorización de `Lautaro073` para incluir `vitest.config.ts`, `package.json` y `pnpm-lock.yaml` en `Archivos permitidos` de `docs/tasks/T-006.md`.
  2. **Fase Roja Conductual por Clúster (`H13`) y de Umbral de Cobertura (`H12`):**
     - Se escribieron primero las pruebas conductuales en `src/domain/domain.test.ts` y se ejecutó `pnpm vitest run src/domain/domain.test.ts` contra la implementación anterior, registrando **8 fallos conductuales reales (`8 failed | 1 passed (9)`)**:
       - **Clúster 1 (`H01`, `H02`):** `transitionRequest` aceptaba `isOwnerMerchant: undefined` e `isAssignedCourier: undefined` (`expected { ok: true, ... } to deeply equal { ok: false, code: 'UNAUTHORIZED_ACTOR' }`) y devolvía `VALIDATION_ERROR` en vez de `REASON_REQUIRED` en las 4 ramas con motivo obligatorio.
       - **Clúster 2 (`H03`, `H04`, `H10`, `H11`):** `calculateHaversineRouteDistanceM(samePoint, samePoint)` devolvía `500` en lugar de `0`; `calculateRouteDistanceInputSchema` aceptaba coordenadas parciales; `adminUpdateSettingInputSchema` aceptaba `{ key: 'pilot_active', value: 1 }`; `publishRequestOutputSchema` aceptaba `publishedAt: 'not-a-date'`.
       - **Clúster 3 (`H06`, `H07`, `H08`, `H09`):** `createFakeRpcClient({})` no lanzaba error al omitir `settings`; `publish_request` con `role: null` devolvía `ok: true` en lugar de `UNAUTHENTICATED`; `publish_request({ requestId: REQ_MISSING })` devolvía `ok: true` en lugar de `NOT_FOUND`.
     - **Umbral de cobertura (`H12`):** Se configuró `coverage.thresholds` con `perFile: true` (`90%` en ramas, funciones, líneas y sentencias para `src/domain/**/*.ts`) en `vitest.config.ts` y se demostró `pnpm test:coverage` fallando con `exit code 1` (`ERROR: Coverage for branches (78.96%) does not meet "src/domain/**/*.ts" threshold (90%)`) antes de completar la cobertura de ramas.
  3. **Implementación de los 14 hallazgos bloqueantes (`H01`..`H14`):**
     - `H01` y `H02` (`src/domain/states/index.ts`): Evidencia positiva obligatoria (`input.isOwnerMerchant === true` / `input.isAssignedCourier === true`) y `input.subscriptionStatus` explícito en las 8 guardas protegidas de `transitionRequest`; retorno de `REASON_REQUIRED` en las 4 transiciones que exigen motivo (`matched->published` por `merchant`/`courier`, `matched->cancelled` por `merchant` e `in_transit->cancelled` por `admin`).
     - `H03` (`src/domain/schemas/index.ts` y `src/domain/rpc-contracts.ts`): `calculateHaversineRouteDistanceM` retorna `0` para puntos idénticos; `calculateRouteDistanceInputSchema` y `calculateRouteDistanceOutputSchema` remodelados como unión entre 4 coordenadas válidas en Aguilares (`routeDistanceM: number`) y modo sin coordenadas con barrios (`routeDistanceM: null, displayLabel: "De barrio X a barrio Y"`), rechazando coordenadas parciales y eliminando cualquier fallback ficticio de `1500` m o centroides.
     - `H04` (`src/domain/rpc-contracts.ts`): `adminUpdateSettingInputSchema` y `adminUpdateSettingOutputSchema` convertidos en `z.discriminatedUnion('key', [...])` validando el tipo y rango exacto por clave.
     - `H05` (`src/domain/rpc-contracts.ts`): `RpcClientContract` estrechado a `Promise<ActionResult<RpcOutput<K>, RpcErrorCode<K>>>`.
     - `H06`, `H07`, `H08`, `H09` (`src/domain/testing/rpc-fake.ts`): Eliminados los defaults ocultos (`1000`, `30`, `1500`) exigiendo `options.settings` explícito; verificación de `UNAUTHENTICATED` y `UNAUTHORIZED_ACTOR` en las 13 RPC no admin + `AAL2_REQUIRED` en las 5 RPC admin; mapas en memoria reales que devuelven `NOT_FOUND` ante UUID inexistente; transiciones de estado y efectos secundarios reales (`admin_suspend_courier` retira ofertas pendientes y cuenta `withdrawnOffersCount`, `accept_offer` acepta una oferta y pasa las restantes `pending` de esa solicitud a `rejected`, IDs determinísticos únicos por oferta).
     - `H10` (`src/domain/rpc-contracts.ts`): `isoTimestampSchema` (`z.string().datetime({ offset: true })`) en todos los campos temporales ISO y `civilDateSchema` (`YYYY-MM-DD` real) en `paidUntil`.
     - `H11` (`src/domain/errors.ts` y `src/domain/index.ts`): Eliminado `message?: string` de `ActionFailure` y de `err(code)`, devolviendo `{ ok: false, code }`; removido `export * from './testing/rpc-fake'` del barrel productivo `src/domain/index.ts`.
     - `H14` (`package.json` y `pnpm-lock.yaml`): Actualizados `next` y `eslint-config-next` a `14.2.35`, `vitest` y `@vitest/coverage-v8` a `3.2.7`, `pnpm.overrides` (`glob: 10.5.0`, `handlebars: 4.7.9`, `postcss: 8.5.28`, `vite: 6.4.3`) y `pnpm.auditConfig.ignoreGhsas` para avisos exclusivos de la rama 15.x sin backport a 14.x. `pnpm audit --audit-level=high` sale con `exit code 0`.
- **Pruebas:** `pnpm audit --audit-level=high` ✅ · `pnpm install --frozen-lockfile` ✅ · `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test:coverage` ✅ (`perFile: true`, 100% ramas en `errors.ts`, `index.ts`, `priority.ts`, `rpc-contracts.ts`, `schemas/index.ts`, 93.75% en `states/index.ts` y 90.08% en `testing/rpc-fake.ts`) · `pnpm test` ✅ · `pnpm build` ✅.
- **Falta:** Push del nuevo SHA a `origin/feat/T-006-contracts-v1` y solicitud de revisión de Ronda 2 en PR #58.
- **Bloqueos:** ninguno.
- **Próximo paso:** Revisión independiente de Ronda 2 sobre el nuevo SHA (sin tocar `docs/revision-pr/**`).

