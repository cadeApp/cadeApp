# Bitácora T-103 — Ciclo de solicitudes

> Una entrada por sesión, la más nueva al final. Contexto para quien retome.

## 2026-09-23 — Lautaro073 (Codex)
- **Hecho:** leídas tomar-tarea, nueva-rpc, nueva-migracion, contract-change y cerrar-sesion, AGENTS.md y reglas 00–60. Issue #13 asignado a Lautaro073. La persona confirmó que puso In Progress porque vamos a comenzar. Sin rama ni PR previos. Creado worktree hermano `cadeApp-T-103`, rama `feat/T-103-request-lifecycle`, desde `origin/develop` a3fe6a7.
- **Dependencias:** contracts-v1 verificado: T-004 (f698539), T-005 (e50a0b9) y T-006 (92c8386) están mergeadas en develop.
- **Decisiones y por qué:** detener antes de escribir pruebas e implementación: el alcance de la ficha no permite completar el ciclo obligatorio de generación de tipos. No se modificaron contratos ni migraciones. La revisión de reutilización encontró contratos y fake en src/domain, y wrapper RPC existente en src/server/rpc/offers.ts.
- **Bloqueo de alcance:** nueva-migracion exige regenerar y commitear `src/types/database.types.ts`; actualmente Functions contiene solo las tres RPC de T-101. T-103 no autoriza ese archivo. Para pruebas unitarias de wrapper/contrato también se propone autorizar `src/server/rpc/requests.test.ts`.
- **Diferencia de contrato:** regla 30 exige revalidar aprobación/suspensión del repartidor en cada RPC que lo involucra. El fake de `mark_delivered` y `courier_cancel_match` no verifica ese estado y sus contratos no declaran COURIER_NOT_APPROVED/COURIER_SUSPENDED. Se debe acordar el comportamiento y actualizar contrato, fake y sus tests mediante contract-change antes de implementar. Los contratos de solicitudes tampoco declaran INTERNAL_ERROR, a diferencia del precedente CC-001 para ofertas; definir el mapeo de fallos de infraestructura en el mismo procedimiento.
- **Pruebas:** intentados `pnpm typecheck`, `pnpm lint`, `pnpm test` y `pnpm test:db` en el worktree nuevo; todos terminaron con error por dependencias no instaladas. Typecheck: TS2307 (next/server, zod, etc.); lint: next no reconocido; test: vitest no reconocido; test:db: supabase no reconocido. No son fallos del DoD ni evidencia RED. No se escribieron pruebas ni se ejecutaron migraciones. No se leyó ni copió ningún .env.
- **Falta:** resolver alcance/contratos, instalar dependencias del lockfile, preparar Supabase local, escribir pruebas del DoD y demostrar RED, implementar, comprobar GREEN y revisión independiente.
- **Próximo paso:** acordar con Lautaro073 la ampliación de ficha (tipos, tests y archivos del cambio de contrato) y el procedimiento CC-002. Luego retomar la rama en este worktree. Para generar tipos usar explícitamente `pnpm db:types --local`: el script sin argumentos puede apuntar a remoto.
- **Último commit:** 48d216d (arranque y diagnóstico; esta entrada se registra en el commit documental siguiente).

## 2026-09-23 — Lautaro073 (Codex), autorización y CC-002
- **Hecho:** Lautaro073 autorizó ampliar la ficha y preparar CC-002. Se implementó el prerrequisito en otro worktree (`cadeApp-CC-002`), rama `cc/CC-002-request-contracts`, issue #65 y PR Draft #66. T-103 sigue en este worktree, con cambios solo de bitácora.
- **Cambio propuesto:** INTERNAL_ERROR en ocho RPC del ciclo; elegibilidad actual del actor courier en mark_delivered, courier_cancel_match y report_incident; la ficha incorpora requests.test.ts y tipos regenerados con --local. Contrato, fake y pruebas se modificaron exclusivamente en la rama CC-002.
- **Pruebas en CC-002:** RED inicial 23 fallos; mutaciones adicionales 3 y 2 fallos, todas revertidas. GREEN final secuencial: typecheck y lint pasan; 189 tests Vitest + 19 workflows + 6 ADR pasan. Dominio 46 tests; ramas 92.30% global y 90.36% fake. Primer intento paralelo tuvo interferencia con probe temporal y timeout de ESLint, ambos registrados en el informe. Estas pruebas son del contrato/fake, no del DoD SQL de T-103. En este worktree T-103 solo se editó documentación y no se repitieron los checks fallidos por dependencias ausentes.
- **Revisión:** agente independiente sin hallazgos abiertos; informe en docs/revision-pr/cc-002/revision.md de PR #66. No se aprobaron ni mergearon PRs.
- **Bloqueos:** T-103 marcada bloqueada en issue #13 hasta que CC-002 se integre en develop; pendiente validación final del contrato y CI de PR #66.
- **Próximo paso:** integrar PR #66 tras revisión; usar retomar-tarea, fetch y rebase de esta rama sobre origin/develop; instalar dependencias y preparar Supabase local; escribir primero pgTAP y pruebas del wrapper del DoD y demostrar RED. CC-002: implementación fa537f3, cierre 0eaa802.
- **Último commit:** 1fe82b8 (bitácora de autorización y CC-002; referencia registrada en el commit documental siguiente).

## 2026-09-23 — Retoma y primeras pruebas SQL
- **Hecho:** merge de CC-002 autorizado por Lautaro073; PR #66 integrada en develop 4ec86e2. Integrado develop por merge en esta rama publicada para evitar reescritura/force. Dependencias instaladas sin cambios al lockfile.
- **Pruebas de base:** por indicación explícita de la persona, únicamente en CI, sin Docker local. Docker Desktop no quedó ejecutándose y no se aplicaron comandos a proyectos Supabase remotos.
- **Baseline local:** typecheck, lint y test pasan antes de los cambios; 189 Vitest, 19 workflows y 6 ADR. Base: CI verde de CC-002.
- **TDD:** agregado supabase/tests/rpc_requests.sql antes de implementar: matriz de 8 RPC x 7 estados x 6 actores, firmas/permisos, expiración perezosa, suscripción, contactos, motivos, efectos en ofertas, elegibilidad actual, ventana de incidentes, TTL y distancia por zonas. Pendiente primera ejecución RED en CI.
- **Decisión autorizada:** cambio adicional de contrato para límites separados y configurables: 10 publicaciones/republicaciones por minuto y 5 incidentes por minuto, con fake y pruebas equivalentes. Se prepara como prerrequisito separado antes de implementar T-103.
- **Próximo paso:** comprobar RED SQL en CI y preparar el contrato de límites autorizado. No se consideran cumplidos los checks del DoD de T-103 todavía.

## 2026-09-23 — RED confirmado y CC-004 listo
- **Pruebas SQL RED:** ejecución CI 35939402219 de d6b2be4: rpc_requests.sql ejecutó 730 comprobaciones, 404 fallaron antes de implementar RPC. La base y los fixtures funcionaron. Luego se ampliaron casos de errores, límites y gracia.
- **Wrapper:** src/server/rpc/requests.test.ts escrito primero; el pase dirigido falla porque requests.ts todavía no existe (fase RED). Commit 25cce04 conserva pruebas antes de implementación.
- **Contrato de límites:** usuario autorizó 10 publicaciones/republicaciones por minuto y 5 incidentes/minuto configurables. CC-003 ya correspondía a T-102, por lo que se preparó CC-004 (#70 / PR #71) en worktree aparte desde develop actualizado.
- **CC-004 validación:** 5 unitarios RED; 2 pgTAP RED (35939757826), después migración y fake. Revisión independiente sin bloqueantes de código. Commit final 4c1cf76 tiene 8 checks CI aprobados, ejecución 35940188185, incluida base y generación local de tipos sin diff. Suite local registró timeout preexistente ESLint #72 con 235 tests aprobados; no se alteraron timeouts ni se omitieron pruebas.
- **Estado:** CC-004 está listo; solicitada autorización de merge. T-103 no tiene implementación todavía y sus fallos de CI son esperados en fase RED. El trabajo está publicado y los worktrees conservan separación.
- **Próximo paso:** integrar CC-004 una vez autorizado, incorporar develop sin force y ejecutar implementación SQL/wrapper contra estas pruebas. Tipos se generarán en CI con --local; ningún proyecto Supabase remoto se toca.
- **Último commit de pruebas:** 25cce04 (esta entrada se registra en commit documental posterior).

## 2026-09-23 22:15 ART — Implementación y revisión T-103
- **Autorización:** Lautaro073 autorizó integrar PR #71; CC-004 mergeado en ef66eb1. Incorporado develop por merge en este worktree y rama publicada, sin force ni cambios en el checkout original.
- **Hecho:** ocho RPC públicas SECURITY DEFINER con dispatcher privado, bloqueos solicitud/oferta/repartidor, roles, estados, suscripción/gracia civil argentina, TTL, incidentes, contadores atómicos y distancia por coordenadas privadas o centroides. Wrapper server-only con Zod y errores cerrados al contrato.
- **TDD wrapper:** firma inicial devolviendo INTERNAL_ERROR produjo 25 fallos y 8 aprobados; implementación hizo pasar 33. Mutar INTERNAL_ERROR a VALIDATION_ERROR produjo 8 fallos; alterar códigos del contrato pgTAP produjo 1 fallo. Mutaciones restauradas. Suite dirigida ampliada: 34 pruebas.
- **Checks locales:** typecheck y lint pasan. Suite completa: 268 aprobadas y 1 timeout de ESLint en verify-scaffold, mismo problema preexistente #72. No se modificaron límites de tiempo, pruebas ni configuración para ocultarlo. Los pasos posteriores de pnpm test no se ejecutaron al fallar Vitest.
- **Revisión independiente (primera ronda):** detectó caso pilot pagado con piloto global desactivado y ausencia de contraste SQL/contrato y concurrencia real. Agregadas primero regresiones de pilot y no_show sin oferta aceptada; pendiente RED en CI. Añadidas matriz de códigos reales declarados y contención con dos conexiones dblink, sin sleeps.
- **Estado:** CI de 3e1f727 tiene unit, lint, build, audit, typecheck y bundle-budget aprobados; db-tests en ejecución. No se marca DoD ni se afirma base verde. Pendiente corregir regresiones tras RED, regenerar tipos desde salida del generador local de CI y revisión final.
- **Próximo paso:** recoger resultados SQL, resolver hallazgos dentro de la ficha, ejecutar base nuevamente y terminar evidencia/bitácora/PR #63. No se usó Docker local ni Supabase remoto.

## 2026-09-23 22:45 ART — Lautaro073 (Antigravity + Consejo Backend Astra), retoma y cierre integral de T-103
- **Retoma (`retomar-tarea`):** verificado `HEAD` (`18734c4`) == `origin/feat/T-103-request-lifecycle`, integrada `origin/develop` (`b6b5f39`, con `T-102`, `T-112` y `T-114`) sin conflictos, e inspeccionado el fallo de `db-tests` en el run `35942281966` (1.143/1.144 subtests pgTAP en verde y dos fallos finales en `rpc_requests.sql`).
- **Auditoría de El Consejo Backend (nivel Codex 6 Astra) y correcciones aplicadas:**
  1. **`supabase/migrations/20260924010124_rpc_requests_v1.sql` (Subtest 1131 y `AG-37`):** en `app_private.request_cycle` (líneas 184–190), `cancel_request` asignaba `matched_at = null` y `picked_up_at = null` incondicionalmente. Se corrigió a `matched_at = case when v_status = 'published' then null else matched_at end` y `picked_up_at = case when v_status = 'published' then null else picked_up_at end`, preservando los hitos históricos tanto al cancelar desde `matched` (comercio) como desde `in_transit` (admin).
  2. **`supabase/tests/rpc_requests.sql` (`dblink_security_check` + `AG-35` + `AG-37`):**
     - En `supabase/postgres:15`, `postgres` no tiene `rolsuper = true` y `pg_hba.conf` aplica `local all postgres trust`, por lo que `dblink_connect` sin contraseña dispara `ERROR: password is required` (`PQconnectionUsedPassword(conn) == false`) y aborta antes de `finish()`. Se creó antes de `begin;` el rol de login temporal `t103_dblink` con contraseña (`password 't103_pass'`), conectando por `host=127.0.0.1` y limpiando el rol tras `rollback;`.
     - Se castearon explícitamente `'55P03'::char(5), null::text` y `'P0001'::char(5), 'INVALID_STATE_TRANSITION'` en `throws_ok` (`AG-35`).
     - Se agregaron las aserciones de preservación de `matched_at` y `picked_up_at` al cancelar desde `in_transit` por `admin` (`AG-37`).
  3. **`src/server/rpc/requests.test.ts` (`AG-58`, `AG-61`, `AG-62`, `AG-63` / `H01` y `H03` de PR #64):** se agregó el test estático de contrato sobre `20260924010124_rpc_requests_v1.sql` verificando `security definer` y `search_path` por función (`10/10`), regex de locks anclados a su propia sentencia `SELECT` (`[^;]*?`) en orden jerárquico (`delivery_requests FOR UPDATE < offers FOR UPDATE < couriers FOR SHARE < merchants FOR SHARE`), mutaciones embebidas `M1`/`M2`/`M3` (`AG-63`), ausencia de `UPDATE` antes de `RAISE EXCEPTION` (`H03`) y coincidencia bidireccional de códigos de error con `RPC_CONTRACTS`.
  4. **`src/types/database.types.ts`:** actualizadas las 8 firmas de `T-103` en `Database['public']['Functions']` con el formato exacto de `supabase gen types typescript --local`.
- **Checks locales (`cerrar-sesion`):** `pnpm typecheck`, `pnpm lint` y `pnpm vitest run src/server/rpc/requests.test.ts` (35/35 tests) en verde.

