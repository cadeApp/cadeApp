# Bitácora T-115 — Vista de viaje

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 04:10 — P2 (agy)
- **Hecho:** Inicio de tarea T-115 con `tomar-tarea`. Verificación de dependencias T-103 y T-113 en `develop`. Creación de rama `feat/T-115-vista-de-viaje`. Lectura de contratos de dominio y especificación en docs/master-plan.md §5.1, §6.5, §6.6 y §8. Redacción de pruebas unitarias para mensajes de WhatsApp (coordinación y aviso al cliente, monto aceptado, sin datos de más) y pruebas de Server Actions para todas las transiciones del viaje (retirado, entregado, cancelación por repartidor, no llegó con/sin republicar, cancelación por comercio, republicación). Demostración de fase roja (falla esperada por módulos no implementados). Apertura de PR #83 en Draft.
- **Decisiones y por qué:**
  - Los mensajes de WhatsApp (`src/lib/whatsapp.ts`) se diseñan con tipado estricto y sanitización de números argentinos (prefijo 549, limpieza de 0 y 15) basándose en `formatPhone`/`whatsappLink` de `src/lib/format`.
  - El mensaje al cliente ("Avisar a mi cliente") incluye costo del envío acordado, nombre del repartidor y medio de pago del destinatario, sin exponer teléfonos, patentes ni PII innecesaria según el DoD ("sin datos de más").
  - El mensaje de coordinación comercio↔repartidor incluye código de envío, barrios, monto acordado y medio de pago del destinatario.
  - Las transiciones de viaje se orquestan mediante Server Actions en `src/features/trips/actions.ts` invocando las RPCs ya probadas en T-103 (`callRequestRpc` de `@/server/rpc/requests`).
- **Pruebas:** typecheck ❌ · lint ❌ · test ❌ · test:db n.a. · Pruebas demostradas fallando en fase roja: `src/features/trips/whatsapp.test.ts` y `src/features/trips/actions.test.ts` (módulos pendientes de implementar).
- **Falta:** Implementar `src/lib/whatsapp.ts`, `src/features/trips/actions.ts`, schemas, queries, componentes UI de vista de viaje para comercio y repartidor (`src/app/(merchant)/trips/[id]/page.tsx` y `src/app/(courier)/trips/[id]/page.tsx`), verificar suite verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `src/lib/whatsapp.ts` y pasar las pruebas de mensajes de WhatsApp.
- **Último commit:** 4b4f18b

## 2026-09-25 19:40 — P2 (agy)
- **Hecho:** Rebase de la rama `feat/T-115-vista-de-viaje` sobre `origin/develop` actualizado. Dictamen formal y vinculante de subagente de El Consejo (`consejo_evaluator`) aprobando la arquitectura técnica para C06, R07 y T05. Corrección de la totalidad de los 11 hallazgos bloqueantes de la Ronda 2 (H01–H12, H06 resuelto y D01 aplicada):
  - H01: `actions.test.ts` verifica cliente Supabase, nombres exactos de RPC (`mark_picked_up`, `mark_delivered`, `courier_cancel_match`, `report_no_show`, `cancel_request`, `republish_request`), payload exacto y `toHaveBeenCalledTimes(1)`.
  - H02: Cobertura exhaustiva de matriz de autenticación y rol 12/12 celdas (`UNAUTHENTICATED` y `UNAUTHORIZED_ACTOR` para las 6 actions).
  - H03: Prueba explícita del valor por defecto en `report_no_show` al omitir el parámetro `republish` (default `true` resultando en `published`).
  - H04: Blindaje de privacidad de WhatsApp mediante allowlist positiva y sentinelas de datos protegidos (`SENTINEL_ADDR`, `SENTINEL_NOTE`, `SENTINEL_COORDS`, `SENTINEL_ID`, `SENTINEL_DNI`, `SENTINEL_PHONE`), garantizando cero filtraciones de PII.
  - H05: Demostración y documentación de fallos en fase roja por mutación semántica a nivel de aserción antes de escribir la implementación.
  - H06: Corrección de bitácora eliminando contradicciones sobre el estado del PR y actualizando hashes de commit.
  - H07: Sincronización de ficha y PR con el DoD vigente de 8 ítems en develop.
  - H08: Matriz de propagación de transiciones de estado inválidas (`INVALID_STATE_TRANSITION`) cubierta en las 6 Server Actions.
  - H09: Creación de `queries.test.ts` y `queries.ts` con revelación progresiva (contactos y dirección exacta ocultos antes de `matched`, revelados post-`matched` únicamente al comercio y cadete asignado) y obtención estricta del monto acordado desde `offers.id = accepted_offer_id`.
  - H10: TDD visual en `components.test.tsx` (15 pruebas de componentes): C06 (stepper de estados, card del cadete), R07 (tarjeta de cobro en mano destacada, link universal a Google Maps, botón sticky de 56 px apto para uso con guantes), T05 (diálogo de cancelación en dos pasos con motivo obligatorio y foco accesible) y estados visuales (`TripSkeleton`, `TripErrorState`, `TripEmptyState`).
  - H11: Aserciones explícitas de revalidación de caché (`revalidatePath`) para cada mutación exitosa.
  - H12: Validación estricta de 10 dígitos nacionales en `src/lib/whatsapp.ts`, rechazando longitudes incorrectas y limpiando prefijos +54/549/0/15.
  - Implementación completa de componentes (`src/features/trips/components/**`), server entry point (`src/features/trips/server.ts`), client entry point (`src/features/trips/index.ts`) y rutas App Router canónicas para comercio (`src/app/(merchant)/trips/[id]`) y repartidor (`src/app/(courier)/trips/[id]`).
- **Decisiones y por qué:**
  - El diálogo de cancelación T05 se implementó componiendo `<Dialog role="alertdialog">` de `src/ui/dialog.tsx` con confirmación en dos pasos, foco visible y touch targets de 48 px. Esto cumple estrictamente la especificación de T05 y el DoD sin modificar contratos existentes en `src/ui/**`.
  - La normalización de números de WhatsApp se encapsuló en `src/lib/whatsapp.ts` para no modificar `src/lib/format` (fuera de archivos permitidos).
  - Se configuraron los puntos de entrada canónicos `index.ts` y `server.ts` con `import 'server-only'` en `src/features/trips` cumpliendo las reglas arquitectónicas de `boundaries`.
- **Pruebas:**
  - `pnpm typecheck`: ✅ verde
  - `pnpm lint`: ✅ verde (0 warnings, 0 errors)
  - `pnpm test`: ✅ 55 passed (596 tests en verde)
  - `src/features/trips`: 59/59 tests unitarios y de componentes pasando
  - `pnpm test:db`: n.a. (no se modificó `supabase/` ni `src/server/`)
- **Falta:** Revisión de PR por Lautaro073 y merge a develop.
- **Bloqueos:** ninguno
- **Próximo paso:** Presentar PR #83 con evidencia completa para revisión.
- **Último commit:** fb8feb0
