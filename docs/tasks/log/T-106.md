# Bitácora T-106 — Migración, RLS de coordenadas y RPC `calculate_route_distance`

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-26 00:35 — Lautaro073 (agy)
- **Hecho:** Inicio de tarea T-106 (Issue #16 asignado a Lautaro073) con la skill `tomar-tarea`. Creación de la rama `feat/T-106-coordenadas-distancia` desde `origin/develop`. Se escriben primero las pruebas del DoD en `supabase/tests/rpc_distance.sql` y `supabase/tests/rls_coordinates.sql` demostrando su estado rojo inicial antes de la implementación.
- **Decisiones y por qué:** Cumplimiento estricto de TDD y de la skill `tomar-tarea`. Se verifica que la función RPC `calculate_route_distance` y la vista `merchant_public` aún no existen en la base de datos.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ · test:db ❌ (demostrado estado rojo inicial: RPC `calculate_route_distance` y vista `merchant_public` no existen aún en la base de datos)
- **Falta:** Crear la migración SQL con RPC `calculate_route_distance`, vista `merchant_public`, RLS de coordenadas y freeze de `created_at` en `profiles_update_self` (PR56-H22). Crear wrapper `src/server/rpc/distance.ts` y regenerar `src/types/database.types.ts`.
- **Bloqueos:** ninguno
- **Próximo paso:** Crear migración SQL en `supabase/migrations/` y wrapper en `src/server/rpc/distance.ts`.
- **Último commit:** pendiente
