# Bitácora T-101 — `submit_offer`, `withdraw_offer`, `set_availability`, `rate_limits` atómico

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-23 04:55 — Lautaro073 (agy)
- **Hecho:**
  1. Inicio de tarea `T-101` (Issue #11 asignado a `Lautaro073`) con la skill `tomar-tarea`.
  2. Verificación de dependencias: `contracts-v1` (`T-004`, `T-005` y `T-006`) + `T-008` y `T-009` mergeadas en `origin/develop` (`164d368`).
  3. Creación de la rama `feat/T-101-rpc-offers-rate-limits` desde `origin/develop` (`164d368`).
  4. **Fase Roja (TDD):** Creación de la suite pgTAP `supabase/tests/rpc_offers.sql` (32 aserciones cubriendo los escenarios del DoD: `999`, `1000` y `1001` con `min_offer_ars = 1000` y luego cambiado a `1500` en `platform_settings`; rechazo de repartidores `pending`, `rejected`, `suspended` y `unavailable`; `DUPLICATE_ACTIVE_OFFER`; expiración perezosa `REQUEST_EXPIRED`; `withdraw_offer` y re-oferta; `set_availability`; incremento y bloqueo atómico en `public.rate_limits` con `RATE_LIMITED`) antes de crear la migración `supabase/migrations/*_rpc_offers_v1.sql` ni el wrapper `src/server/rpc/offers.ts`.
  5. Apertura del PR en Draft hacia `develop` para registrar la corrida de Fase Roja en el job `db-tests` de GitHub Actions (sin ejecutar Docker local en la máquina del Tech Lead).
- **Decisiones y por qué:**
  - Cumplir estrictamente `AGENTS.md` §1.3 ("Tocá SOLO los archivos de 'Archivos permitidos'. Si necesitás otro, detenete y explicá por qué"): antes de pushear la migración de Fase Verde, se solicita a `Lautaro073` autorización para sumar `src/types/database.types.ts` (exigido por `ci.yml` `pnpm db:types --local && git diff --exit-code -- src/types/database.types.ts` al agregar funciones en `public`) y `src/server/rpc/offers.test.ts` (exigido por `vitest.config.ts` `include: ['src/**/*.test.{ts,tsx}']` para el test de contrato de `src/server/rpc/offers.ts` contra `rpc-contracts.ts` y `rpc-fake.ts`) a `Archivos permitidos` en `docs/tasks/T-101.md`.
- **Pruebas:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test` ✅ · `test:db` ❌ (Fase Roja demostrada en `supabase/tests/rpc_offers.sql`: las funciones `public.submit_offer`, `public.withdraw_offer` y `public.set_availability` aún no existen).
- **Falta:** Implementar `supabase/migrations/20260923050000_rpc_offers_v1.sql` y `src/server/rpc/offers.ts` (+ `src/types/database.types.ts` y `src/server/rpc/offers.test.ts` tras visto bueno de alcance de `Lautaro073`).
- **Bloqueos:** Autorización de `Lautaro073` para incluir `src/types/database.types.ts` y `src/server/rpc/offers.test.ts` en `Archivos permitidos` de `docs/tasks/T-101.md`.
- **Próximo paso:** Confirmar alcance con `Lautaro073` e implementar Fase Verde.
