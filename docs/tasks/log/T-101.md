# Bitácora T-101 — `submit_offer`, `withdraw_offer`, `set_availability` y `rate_limits` atómico

- **Issue:** #11
- **Rama:** `feat/T-101-rpc-offers-rate-limits`
- **PR:** #62
- **Responsable:** `@Lautaro073` (P1 — Base de Datos, Dominio, Auth y Admin)

---

## Sesión 1 — 2026-09-23 (`@Lautaro073`)

### 1. Toma de tarea y verificación de precondiciones (`tomar-tarea`)
- Ficha `docs/tasks/T-101.md` verificada en `origin/develop` (`164d368`).
- Issue `#11` abierto y asignado a `@Lautaro073`.
- Dependencias `contracts-v1` (`T-004`, `T-005`, `T-006`), `T-008` y `T-009` mergeadas en `origin/develop`.
- Creación de rama `feat/T-101-rpc-offers-rate-limits` desde `origin/develop` (`164d368`).

### 2. Fase Roja (TDD — pruebas primero y evidencia de fallo)
- Se escribió `supabase/tests/rpc_offers.sql` con **32 aserciones pgTAP** que ejercitan la totalidad del DoD:
  - Piso dinámico desde `public.platform_settings.min_offer_ars` (`999` → `OFFER_BELOW_MINIMUM`, `1000` → `ok`, `1001` → `ok` con `min_offer_ars=1000`; tras actualizar `min_offer_ars` a `1500`, `999`, `1000` y `1001` devuelven `OFFER_BELOW_MINIMUM`, y `1500` es aceptada).
  - Estados de repartidor rechazados en `submit_offer` y `set_availability`: `pending` → `COURIER_NOT_APPROVED`, `rejected` → `COURIER_NOT_APPROVED`, `suspended` → `COURIER_SUSPENDED`; `available = false` en `submit_offer` → `COURIER_UNAVAILABLE`.
  - Oferta activa duplicada sobre la misma solicitud → `DUPLICATE_ACTIVE_OFFER`.
  - Retiro de oferta con `withdraw_offer(p_offer_id)`: transición a `status = 'withdrawn'` con `decided_at` seteado; intento de retiro repetido → `OFFER_NOT_PENDING`; intento de otro repartidor → `UNAUTHORIZED_ACTOR`; re-oferta permitida tras retirar la oferta anterior.
  - Rate limit atómico sobre `public.rate_limits` (`INSERT ... ON CONFLICT (subject, action, window_start) DO UPDATE SET count = rate_limits.count + 1 RETURNING count`): ventana de `max_offers_per_min` respetada y rechazo con `RATE_LIMITED` al superar el tope en ráfaga.
- Commit de Fase Roja: `f66b773` (`test(offers): add failing pgTAP suite for submit_offer, withdraw_offer, set_availability and rate_limits [T-101]`).
- **Evidencia de Fase Roja en CI (`db-tests`, Run `35834306227` / Job `107093971376`):**
  - `supabase/tests/rpc_offers.sql .. Dubious, test returned 2 (wstat 512, 0x200) — Failed 32/32 subtests` (`42883: function public.submit_offer(...) does not exist`).
- Se escribió `src/server/rpc/offers.test.ts` y se verificó su fallo local en Fase Roja (`Failed to resolve import "@/server/rpc/offers"`).

### 3. Ampliación autorizada de `Archivos permitidos` (`@Lautaro073`)
- `@Lautaro073` autorizó (`autorizo`) sumar a `Archivos permitidos` en `docs/tasks/T-101.md`:
  - `src/server/rpc/offers.test.ts` (pruebas unitarias y de contrato Vitest contra `RPC_CONTRACTS`, `createRpcFake` y el wrapper `src/server/rpc/offers.ts`).
  - `src/types/database.types.ts` (sincronización de tipos generados de `Database['public']['Functions']` exigida por `AGENTS.md` §4 y `db-tests` en CI).
  - `public/brand/logo.svg` (restauración de `role="img" aria-label="cadeApp"` tras el commit `fix(svg)` de `164d368` en `develop`, manteniendo el peso en `4.541 B < 5 KB`).

### 4. Fase Verde (Implementación SQL + Wrapper Server + Tipos)
- **`supabase/migrations/20260923050000_rpc_offers_v1.sql`:**
  - Implementa `public.submit_offer`, `public.withdraw_offer` y `public.set_availability` con `SECURITY DEFINER` y `SET search_path = public, pg_temp`.
  - Bloqueo pesimista (`FOR UPDATE`) sobre `public.couriers`, `public.delivery_requests` y `public.offers` para serializar concurrencia.
  - Incremento atómico de `public.rate_limits` mediante `INSERT ... ON CONFLICT (subject, action, window_start) DO UPDATE SET count = public.rate_limits.count + 1 RETURNING count` antes de validar el negocio para contabilizar ráfagas concurrentes por `(courier_id, 'submit_offer', date_trunc('minute', now()))` y `(courier_id, 'withdraw_offer', date_trunc('minute', now()))`.
  - Lectura dinámica de `min_offer_ars` y `max_offers_per_min` desde `public.platform_settings`.
  - Privilegios endurecidos: `REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon; GRANT EXECUTE ON FUNCTION ... TO authenticated;`.
- **`src/server/rpc/offers.ts`:**
  - Módulo `server-only` con `submitOfferRpc`, `withdrawOfferRpc`, `setAvailabilityRpc`, `mapOfferRpcError` y `createOffersRpcServerClient` tipados contra `RPC_CONTRACTS` y `ActionResult`.
- **`src/types/database.types.ts`:**
  - Declaración de `set_availability`, `submit_offer` y `withdraw_offer` en `Database['public']['Functions']`.
- **Verificación local:**
  - `pnpm typecheck` → `0` errores.
  - `pnpm lint` → `✔ No ESLint warnings or errors`.
  - `pnpm test:coverage` → `18 passed (18)`, `160 passed (160)`, cobertura de `src/server/rpc/offers.ts` en `100%` statements / `100%` functions / `100%` lines.

### 5. Próximos pasos
- Push de Fase Verde a `origin/feat/T-101-rpc-offers-rate-limits`, verificación de los 8 checks en GitHub Actions (`db-tests`, `unit`, `lint`, `typecheck`, `build`, `bundle-budget`, `audit`, `approval-policy`) y cierre de revisión en PR #62.
