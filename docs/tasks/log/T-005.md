# Bitácora T-005 — RLS v1, storage courier-docs, matriz RLS, rls_enabled.sql

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 02:05 — Lautaro073 (agy)
- **Hecho:** Tarea T-005 tomada con skill `tomar-tarea` desde `origin/develop` (commit `f698539`, con T-004 mergeada). Rama `feat/T-005-rls-v1` creada. Se escriben las pruebas del DoD en `supabase/tests/rls_enabled.sql` y `supabase/tests/rls_matrix.sql` antes de implementar la migración para demostrar la fase roja (TDD).
- **Decisiones y por qué:** Respetar estrictamente los archivos permitidos (`supabase/migrations/**`, `supabase/tests/rls_*.sql`, `docs/tasks/T-005.md`, `docs/tasks/log/T-005.md`, `docs/revision-pr/**`). Se mantiene la política de no ejecutar Docker local en la máquina del Tech Lead.
- **Pruebas:** Fase roja inicial: se escriben las pruebas pgTAP `rls_enabled.sql` y `rls_matrix.sql`. Las aserciones demuestran el fallo frente al estado actual (tablas con `default_deny` que rechazan a todos los roles y bucket `courier-docs` inexistente).
- **Falta:** nada en el código. PR #56 listo para revisión / merge por Lautaro073.
- **Bloqueos:** ninguno.
- **Próximo paso:** Publicar informe de `revisar-pr` en el cuerpo del PR para satisfacer `approval-policy`.
- **Último commit:** fdf57db
- **Evidencia fase roja (CI Run 35689605017 / Job 106623573187):**
  - `rls_enabled.sql`: Fallaron 2/2 subtests por la presencia intencional de `_table_without_rls` (DoD: `rls_enabled.sql` falla con una tabla sin RLS demostrado).
  - `rls_matrix.sql`: Fallaron 8/15 subtests debido a `default_deny` (rechazo de merchants, couriers y admin a lecturas legítimas) y a que el bucket `courier-docs` aún no ha sido creado.
- **Evidencia fase verde (CI Run 35690759854 / Job 106627070128):**
  - `rls_enabled.sql`: ok (2/2)
  - `rls_matrix.sql`: ok (15/15)
  - `structure.sql`: ok (41/41)
  - Total pgTAP: 58/58 tests pasando.
  - CI completo: 8 de 8 jobs en verde (incluyendo `db-types` sin diff, `typecheck`, `lint`, `unit`, `build`, `audit`, `bundle-budget`).
- **Revisión independiente (revisar-pr):** Realizada con 0 bloqueantes. Histórico estructurado guardado en `docs/revision-pr/pr-56/`.

## 2026-09-22 03:00 — Lautaro073 (agy)
- **Hecho:** Se implementaron las correcciones para todos los hallazgos señalados en la Ronda 1 de revisión (`docs/revision-pr/pr-56/revisiones/ronda-1.md`):
  - **H01 (Bloqueante):** En `couriers_update_self`, se congelan `license_status`, `insurance_status`, `decided_by`, `decided_at` y `deactivated_at`, impidiendo autoverificación de documentos (que elevaría `doc_level` a 2) y falsificación del registro de aprobación.
  - **H02 (Bloqueante):** En `merchants_update_self`, se congelan `subscription_status`, `paid_until` y `notes`, impidiendo autoconcederse suscripción paga o alterar notas administrativas.
  - **H03 (Alto):** En `merchants_select_courier`, se acota la visibilidad mediante el helper `app_private.is_merchant_visible_to_courier` para que un repartidor solo vea comercios con los que tiene relación activa (solicitudes publicadas no expiradas o entregas asignadas aceptadas).
  - **H04 (Medio):** Se divide la política de `zones` en `zones_select_public` (para anon y authenticated con `using (active)`) y `zones_select_admin` (para admin), evitando llamadas a `app_private.is_admin()` desde el rol `anon`.
  - **H05 (Medio):** Se amplía `rls_matrix.sql` con tests positivos y negativos de `UPDATE` sobre `profiles`, `couriers` y `merchants`.
  - **H06 (Medio):** Se agrega `with check` estricto en `offers_update_merchant` congelando `amount_ars`, `eta_minutes`, `message`, `courier_id` y `request_id`.
  - **H07 (Bajo):** Se documenta la justificación explícita de `USING (true)` en `platform_settings`.
  - **H08 (Bajo):** Se restringe `incidents_insert_authenticated` exigiendo que el reportero sea el comercio dueño, el courier asignado o un admin.
  - **H09 (Bajo):** En storage para `courier-docs`, se exige que el perfil tenga rol `'courier'` mediante el helper `app_private.is_courier()`.
- **Decisiones y por qué:** Mantener la arquitectura de helpers `security definer` en el esquema `app_private` con search_path sanitizado (`public, pg_temp`), evitando la recursión de RLS y aislando la superficie pública de tipos de Supabase.
- **Pruebas:** `supabase/tests/rls_matrix.sql` ampliada de 15 a 28 aserciones pgTAP cubriendo aserciones positivas (`lives_ok`, `is`, `ok`) y de rechazo de políticas RLS (`throws_ok` con SQLSTATE `42501`).
- **Falta:** Ejecutar y validar en CI los 28 tests pgTAP y checks automáticos. Lista para Ronda 2 de revisión por Lautaro073.
- **Bloqueos:** ninguno.
- **Próximo paso:** Push de los cambios a `origin/feat/T-005-rls-v1` para disparar CI y someter a Ronda 2.

## 2026-09-22 03:15 — Lautaro073 (agy)
- **Hecho:** Se atiende el hallazgo bloqueante H10 de la Ronda 2 (`docs/revision-pr/pr-56/revisiones/ronda-2.md`). Se corrigen las 7 aserciones `throws_ok` en `supabase/tests/rls_matrix.sql` (tests 16, 20, 22, 23, 25, 26, 27) para usar la forma canónica de 4 argumentos con casts explícitos: `throws_ok(sql, '42501'::char(5), null::text, description)`.
- **Decisiones y por qué:** Afirmar exclusivamente el SQLSTATE `42501` (`insufficient_privilege`), que es estable entre versiones de PostgreSQL y no depende de la localización ni del nombre de tabla en el mensaje de error. Con `null::text` en el tercer argumento se evita que pgTAP interprete la descripción como mensaje esperado o compare con `=`.
- **Pruebas:** 28/28 tests en `rls_matrix.sql`, 2/2 en `rls_enabled.sql`, 41/41 en `structure.sql` (71 tests pgTAP en total).
- **Falta:** Validar CI 100% verde para dejar PR lista para aceptación en Ronda 3.
- **Bloqueos:** ninguno.
- **Próximo paso:** Push a `origin/feat/T-005-rls-v1` y verificar CI en verde.

