# Bitácora T-005 — RLS v1, storage courier-docs, matriz RLS, rls_enabled.sql

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 02:05 — Lautaro073 (agy)
- **Hecho:** Tarea T-005 tomada con skill `tomar-tarea` desde `origin/develop` (commit `f698539`, con T-004 mergeada). Rama `feat/T-005-rls-v1` creada. Se escriben las pruebas del DoD en `supabase/tests/rls_enabled.sql` y `supabase/tests/rls_matrix.sql` antes de implementar la migración para demostrar la fase roja (TDD).
- **Decisiones y por qué:** Respetar estrictamente los archivos permitidos (`supabase/migrations/**`, `supabase/tests/rls_*.sql`, `docs/tasks/T-005.md`, `docs/tasks/log/T-005.md`, `docs/revision-pr/**`). Se mantiene la política de no ejecutar Docker local en la máquina del Tech Lead.
- **Pruebas:** Fase roja inicial: se escriben las pruebas pgTAP `rls_enabled.sql` y `rls_matrix.sql`. Las aserciones demuestran el fallo frente al estado actual (tablas con `default_deny` que rechazan a todos los roles y bucket `courier-docs` inexistente).
- **Falta:** Verificar pase a verde en CI (`db-tests`), auditar con `revisar-pr` e incorporar el informe al PR.
- **Bloqueos:** ninguno.
- **Próximo paso:** Verificar la ejecución del job `db-tests` en GitHub Actions para confirmar pase a verde de pgTAP, y correr `revisar-pr`.
- **Último commit:** ca6cff5
- **Evidencia fase roja (CI Run 35689605017 / Job 106623573187):**
  - `rls_enabled.sql`: Fallaron 2/2 subtests por la presencia intencional de `_table_without_rls` (DoD: `rls_enabled.sql` falla con una tabla sin RLS demostrado).
  - `rls_matrix.sql`: Fallaron 8/15 subtests debido a `default_deny` (rechazo de merchants, couriers y admin a lecturas legítimas) y a que el bucket `courier-docs` aún no ha sido creado.
