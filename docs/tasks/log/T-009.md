# Bitácora T-009 — Auth base

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-23 00:35 — P2 (agy)

- **Hecho:**
  1. Toma de tarea T-009 con la skill `tomar-tarea`. Verificación de dependencias T-004, T-005 y T-006 mergeadas en `origin/develop`. Creación de rama `feat/T-009-auth-base` desde `origin/develop`.
  2. Apertura del Pull Request Draft #60 (`[T-009] Auth base`).
  3. Fase TDD: redacción previa de pruebas del DoD demostradas fallando en rojo (`src/features/auth/auth.test.ts` con `Failed to resolve import "./guards"`).
  4. Implementación modular de `src/features/auth/`:
     - `schemas.ts`: `loginSchema`, `registerSchema` reutilizando `signupRoleSchema` del dominio.
     - `guards.ts`: `evaluateRouteGuard`, `getRoleDefaultPath`, `isMerchantRoute`, `isCourierRoute`, `isAdminRoute`, `isPublicRoute`.
     - `queries.ts`: `getServerSession` consumiendo `await createClient()` de forma asíncrona (`H16`), leyendo perfil de rol y MFA AAL.
     - `actions.ts`: `loginAction`, `registerAction` (rechazo explícito de rol admin y validación de schemas) y `logoutAction` consumiendo asíncronamente `createClient()`.
     - `server.ts`: función `updateSession(request)` para middleware con `@supabase/ssr` `createServerClient`.
     - `copy.ts`: textos canónicos en es-AR según Stitch P02 y P03.
     - `components/login-form.tsx` y `components/register-form.tsx`: formularios accesibles con estilos de diseño P02 y P03.
     - `src/app/(public)/login/page.tsx` y `src/app/(public)/register/page.tsx`.
     - `middleware.ts`: integración de `updateSession(request)` exportado por `src/features/auth/server.ts`.
  5. Desacoplamiento de pruebas en suites especializadas respetando fronteras arquitectónicas (`feature-server-boundary`): `guards.test.ts`, `actions.test.ts` y `queries.test.ts`.
  6. Suite completa verde (116 tests), `typecheck` y `lint` sin advertencias ni errores, y `next build` compilando con 110 kB de First Load JS (muy inferior al presupuesto de 180 kB).
- **Decisiones y por qué:**
  - Se estructuró la feature `auth` siguiendo fielmente el modelo `src/features/_template/` y las reglas arquitectónicas (Regla 20).
  - Los tests se dividieron en `guards.test.ts`, `actions.test.ts` y `queries.test.ts` para cumplir estrictamente con el linter `feature-server-boundary`, asegurando que solo los módulos de servidor importen `src/server/**`.
  - El consumo asíncrono de `createClient()` (`await createClient()`) exigido por Next.js 15 y el hallazgo `H16` quedó completamente testeado y verificado en `actions.test.ts` y `queries.test.ts`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (116/116) · test:db n.a. · `src/features/auth/auth.test.ts` falló en rojo al inicio antes de implementar.
- **Falta:** Revisión de PR por Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Pasar PR #60 a "Ready for review" y solicitar revisión.
- **Último commit:** 630f981

## 2026-09-23 01:30 — P2 (agy)

- **Hecho:**
  1. Resolución integral de los 6 hallazgos bloqueantes (`PR60-H01` a `PR60-H06`) y 1 mejora (`PR60-H07`) de la Ronda 1 de revisión de PR #60 (`docs/revision-pr/pr-60/`).
  2. Demostración de fase roja (TDD / Regla 40) antes de cada arreglo:
     - `H01`: Tests unitarios en `queries.test.ts`, `actions.test.ts` y `server.test.ts` demostraron que si el perfil no existe, falla o tiene un rol corrupto, el sistema antes caía silenciosamente a `'merchant'`. Se agregaron tests esperando `null` (queries/server) y `err('UNAUTHORIZED_ACTOR')` (actions), fallando en rojo.
     - `H02`: Tests unitarios en `guards.test.ts` demostraron: a) `/couriers` clasificado erróneamente como repartidor en vez de admin; b) usuarios anónimos accediendo a rutas privadas `/trips` y `/feed`; c) admin autenticado con AAL1 cayendo en bucle de redirección en `/login/mfa`.
     - `H03`: Test unitario en `server.test.ts` demostró que al redirigir con `NextResponse.redirect`, se perdían las cookies de sesión rotadas por Supabase contenidas en `supabaseResponse`.
     - `H04`: Tests unitarios en `guards.test.ts` demostraron que `redirectTo` sin sanear permitía open redirect a dominios externos y evasión de aislamiento entre roles.
     - `H05`: Tests en `queries.test.ts` y `actions.test.ts` utilizaban `setTimeout(..., 5)` violando Regla 40; fallaron al auditar temporizadores fijos.
     - `H06`: Test unitario en `actions.test.ts` demostró que `registerAction` permitía el envío sin aceptar términos (`acceptTerms: false`).
  3. Implementación de soluciones:
     - `PR60-H01`: `queries.ts`, `actions.ts` y `server.ts` validan el rol del perfil con `profileRoleSchema.safeParse`. Si el perfil no existe, tiene error o el rol es inválido, retornan `null` / `UNAUTHORIZED_ACTOR` sin caer a `'merchant'`.
     - `PR60-H02`: Implementación de coincidencia exacta por segmento (`matchesSegment`), ampliación de `isAdminRoute` para cubrir `/couriers`, `/merchants`, `/settings`, `/incidents`, `/applicants`, `/audit`, `/login/mfa`, `/admin/mfa`. `evaluateRouteGuard` aplica default-deny para usuarios no autenticados, y permite `/login/mfa` a admins con AAL1 sin bucles.
     - `PR60-H03`: En `src/features/auth/server.ts`, al emitir redirecciones se copian todas las cookies de `supabaseResponse.cookies` hacia `redirectResponse.cookies`.
     - `PR60-H04`: Se implementó `resolvePostLoginRedirect(redirectTo, role)` en `guards.ts`, rechazando URLs absolutas, esquemas ajenos, barras dobles y rutas fuera del alcance del rol. Se tipó `loginSchema` y se validó en `src/app/(public)/login/page.tsx`.
     - `PR60-H05`: Se reemplazaron todos los `setTimeout` en tests por microtasks (`await Promise.resolve()`), eliminando esperas arbitrarias.
     - `PR60-H06`: Se añadió `acceptTerms` a `registerSchema` (`refine(val => val !== false)`), se pasa en `registerAction` desde `register-form.tsx`, se reemplazó el texto hardcodeado por `authCopy.register.errorTermsRequired`, se corrigieron 4 clases `text-xs` a `text-sm` (Regla anti-12px) y se agregaron `aria-pressed` en las tarjetas de selección de rol. En `login-form.tsx`, se amplió el botón de visibilidad de contraseña a 48x48px (`h-12 w-12`) con etiquetas accesibles según `authCopy`.
     - `PR60-H07`: Se actualizó la casilla del DoD en `docs/tasks/T-009.md`.
  4. Formateo y validaciones:
     - Prettier verificado en todos los archivos tocados.
     - `pnpm typecheck` ✅
     - `pnpm lint` ✅ (0 errores, 0 warnings)
     - `pnpm test` ✅ (126 tests pasando)
     - `pnpm build` ✅ (111 kB First Load JS).
- **Decisiones y por qué:**
  - `matchesSegment(path, prefix)`: Evita que prefijos compartidos como `/courier` hagan match con `/couriers` (que pertenece a la zona admin).
  - Preservación explícita de cookies en `updateSession`: Next.js `NextResponse.redirect` instancia una respuesta limpia; iterar sobre `supabaseResponse.cookies.getAll()` y aplicarlas garantiza que la rotación de tokens de Supabase persista en el cliente tras la redirección.
  - Tipado de `maybeSingle<{ role: unknown }>()`: Permite una inferencia limpia con TypeScript estricto delegando la validación del contrato al schema Zod `profileRoleSchema.safeParse`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (126/126) · test:db n.a. · build ✅
- **Falta:** Aprobación de Lautaro073 en PR #60.
- **Bloqueos:** ninguno
- **Próximo paso:** Lautaro073 revisa y aprueba el PR #60 para mergear a develop.
- **Último commit:** 409b509

## 2026-09-23 01:50 — P2 (agy)

- **Hecho:**
  1. Resolución de la mejora no bloqueante `PR60-H08` indicada en la Ronda 2 de revisión de PR #60:
     - En `src/features/auth/guards.ts` (`resolvePostLoginRedirect`), se añadió la condición `rawRedirectTo.includes('\\')` para rechazar barras invertidas y prevenir bypass de Open Redirect derivados de la normalización WHATWG URL (e.g. `/\evil.com` -> `//evil.com`).
  2. Demostración TDD (Regla 40):
     - **Rojo:** Se añadieron aserciones en `src/features/auth/guards.test.ts` esperando que `/\\evil.com` y `/foo\\bar` sean redirigidos a la ruta por defecto del rol. Falló en rojo con `AssertionError: expected '/\\evil.com' to be '/merchant/dashboard'`.
     - **Verde:** Tras aplicar el filtro en `guards.ts:98`, la suite completa de 12 tests en `guards.test.ts` y la suite global de 126 tests pasaron en verde.
  3. Desbloqueo de `approval-policy`:
     - El PR #60 se encontraba en estado Draft (`isDraft: true`). En GitHub, los PRs en borrador no permiten a los revisores emitir una revisión con estado `APPROVED`, razón por la cual la revisión de Lautaro073 ingresó como comentario y el workflow de CI falló con "El PR requiere aprobación vigente de Lautaro073".
     - Se convirtió el PR #60 a listo para revisión (`gh pr ready 60`).
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (126/126) · test:db n.a. · Prettier ✅
- **Falta:** Aprobación formal de Lautaro073 en GitHub (ahora habilitada tras salir de Draft) para poner en verde `approval-policy` y mergear a develop.
- **Bloqueos:** ninguno
- **Próximo paso:** Lautaro073 emite la aprobación formal en el PR #60 y se mergea a develop.
- **Último commit:** 8ff466b
