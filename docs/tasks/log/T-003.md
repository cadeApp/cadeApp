# Bitácora T-003 — CI, migraciones y política de aprobación

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-21 — Lautaro073 (agy)
- **Hecho:** T-003 tomada desde `origin/develop` tras verificar que T-000 y T-002 están mergeadas, el issue #4 está asignado a Lautaro073 y aparece en In Progress en el Project.
- **Decisiones y por qué:** Las pruebas de workflows viven en `.github/workflows/`, dentro de los archivos permitidos de la ficha.
- **Pruebas:** `node --test .github/workflows/verify-workflows.test.mjs` ❌, 8/8 pruebas fallan en rojo: faltan `ci.yml`, `migrate.yml`, `approval-policy.yml` y `approval-policy.mjs`. Es la demostración previa a implementar.
- **Falta:** Implementar workflows; comprobar CI y políticas en GitHub.
- **Bloqueos:** ninguno.
- **Próximo paso:** Implementar `.github/workflows/{ci,migrate,approval-policy}.yml` y `approval-policy.mjs`.
- **Último commit:** 157e2a2

---

## 2026-09-21 13:19 — Lautaro073 (agy)
- **Hecho:** PR Draft #51 abierto. Implementados `ci.yml`, `migrate.yml`, `approval-policy.yml`, la lógica de aprobación y el reporte de bundle. Las 8 pruebas iniciales fallaron antes de implementar; ahora pasan 11/11. CI real #35624232818 terminó en 1 min 37 s: typecheck, lint, unit, db-tests, build, bundle-budget y audit pasaron; db-types falló por configuración faltante.
- **Decisiones y por qué:** `db-types` ejecuta el script desde la rama base confiable y compara los tipos generados con el archivo propuesto; evita ejecutar código del PR con el token de Supabase. `approval-policy` usa código de la base. En `production`, el job solo corre si el push de `main` lo hizo Lautaro073; no reemplaza al revisor obligatorio de environment. Se agregó un error explícito cuando faltan variables de Actions.
- **Pruebas:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test` ✅ (71/71) · `pnpm test:coverage` ✅ · `pnpm build` ✅ · `node --test .github/workflows/verify-workflows.test.mjs` ✅ (11/11) · Prettier ✅ · `pnpm test:db` n.a. El primer lint local coincidió con una prueba que crea un archivo temporal de frontera inválida; se volvió a correr secuencialmente y pasó. Se probó un error de tipos dentro de `.github/workflows/**`, pero `pnpm typecheck` no incluye esa carpeta; se retiró el probe sin tocar archivos fuera de alcance.
- **Falta:** Configurar `SUPABASE_PROJECT_REF` y `SUPABASE_ACCESS_TOKEN` en GitHub Actions para validar drift real; probar una migración vacía autorizada en staging, dos merges consecutivos, y PRs de prueba de la política tras el merge. Protección de ramas y revisor obligatorio de `production` no están disponibles en el plan actual del repo privado; la persona indicó continuar sin esperar la protección de ramas. No se marca el DoD completo.
- **Bloqueos:** CI `db-types` recibe ambas variables vacías. GitHub devuelve 403 al consultar protección de `develop`, `staging` y `main`; `production` tiene `protection_rules: []`.
- **Próximo paso:** Una vez cargadas las variables de Actions, reejecutar el CI de PR #51 y revisar `db-types`; después coordinar la prueba de migración en staging y la política de aprobación.
- **Último commit:** 5f7c31b

---

## 2026-09-21 14:43 — Lautaro073 (agy)
- **Hecho:** Se incorporó `origin/develop` con la ronda 1 de revisión del PR #51. Se corrigieron H01 (ref propio para production y guard que rechaza el ref de staging), H02 (actor no autorizado falla dentro del job en vez de saltearlo), H03 (pruebas por job para ambos environments) y H04 (`bundle-budget` emite una advertencia sin bloquear). El checkout principal cambió de rama por trabajo simultáneo; la corrección se hizo en un worktree aislado.
- **Decisiones y por qué:** Se conservó `SUPABASE_PROJECT_REF` para staging y `db-types`; production exige `SUPABASE_PRODUCTION_PROJECT_REF`, todavía sin configurar. Se interpretó «avisa» del DoD como advertencia, no como fallo del job. No se tocó `package.json` ni otra ruta fuera de la ficha para H05.
- **Pruebas:** Antes del arreglo, las nuevas pruebas de migración y aviso fallaron (2/12). Después: `node --test .github/workflows/verify-workflows.test.mjs` ✅ (12/12), `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (71/71), Prettier ✅ y `git diff --check` ✅. CI #35633748735: 7/8 jobs pasan; `db-types` genera tipos desde staging y falla por el drift real de 12 a 650 líneas. `pnpm test:db` n.a. porque no se tocó `supabase/` ni `src/server/`.
- **Falta:** H05 requiere modificar la configuración de pruebas/lint fuera de los archivos permitidos. `db-types` ya genera tipos con las credenciales configuradas, pero detecta drift real de `src/types/database.types.ts`, fuera de T-003. Siguen pendientes la migración vacía en staging, los dos merges seguidos, la prueba de política en PRs reales y el revisor obligatorio del environment `production`, no disponible en el plan actual.
- **Bloqueos:** El PR continúa en Draft. Production no debe migrarse hasta contar con su ref propio y el control de aprobación exigido; el DoD no está completo.
- **Próximo paso:** Resolver H05 mediante una tarea que autorice `package.json`/configuración de checks y acordar el control de aprobación de production; después repetir `revisar-pr`.
- **Último commit:** 9e331ea

---

## 2026-09-21 14:55 — Lautaro073 (agy)
- **Hecho:** Se incorporó `origin/develop` con la ficha actualizada y se leyó la ronda 2 del PR #51. H06 quedó corregido: `bundle-budget` falla si no puede leer ninguna ruta y solo avisa si una ruta medida supera 180 kB.
- **Decisiones y por qué:** Se agregó una prueba de ejecución real para la salida ilegible, sin ampliar el alcance. H05 sigue pendiente porque `package.json` y la configuración de Vitest no están en los archivos permitidos.
- **Pruebas:** La prueba nueva falló antes del arreglo (12/13); después, `node --test .github/workflows/verify-workflows.test.mjs` ✅ (13/13), `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (72/72), Prettier ✅ y `git diff --check` ✅. `pnpm test:db` n.a.
- **Falta:** CI remoto del nuevo commit, H05, drift de tipos ajeno a T-003, configuración de production y demostraciones en vivo restantes del DoD.
- **Bloqueos:** El PR sigue Draft y no debe mergearse con `db-types` fallando ni sin el control de aprobación de production.
- **Próximo paso:** Revisar CI, repetir `revisar-pr` sobre el nuevo SHA y resolver H05 en una tarea con alcance para la configuración de checks.
- **Último commit:** 038f8cc

---

## 2026-09-21 22:24 — Lautaro073 (agy)
- **Hecho:** H05 corregido en la misma rama del PR #51 con autorización expresa de Lautaro073. La ficha ahora permite `package.json` solo para scripts de calidad, sin dependencias nuevas. `pnpm test` ejecuta Vitest y las pruebas de workflows; `pnpm lint` incluye los `.mjs`; `pnpm typecheck` usa un `tsconfig` propio para revisarlos con `checkJs` y JSDoc.
- **Decisiones y por qué:** El `tsconfig` vive dentro de `.github/workflows/**`, así no se amplía el alcance al `tsconfig.json` raíz. No se modificó `pnpm-lock.yaml`. Se eligió `eslint --no-ignore` porque `next lint` ignora por defecto el directorio oculto `.github`.
- **Pruebas:** Las tres pruebas nuevas fallaron antes del cambio (13/16) y pasaron después (16/16). `pnpm typecheck` detectó parámetros sin tipo en los `.mjs` antes de agregar JSDoc; luego pasó. `pnpm lint` ✅, `pnpm test` ✅ (72/72 Vitest y 16/16 workflows), Prettier ✅ y `git diff --check` ✅. `pnpm test:db` n.a. No se ejecutó un probe temporal adicional: la revisión automática rechazó el comando que lo creaba y retiraba; no dejó archivos.
- **Falta:** Verificar el CI del nuevo commit y repetir `revisar-pr`. El drift de `database.types.ts`, la configuración de production y las demostraciones en vivo del DoD siguen pendientes.
- **Bloqueos:** El PR sigue Draft y `db-types` seguirá rojo hasta que la tarea de tipos/migración sincronice el contrato.
- **Próximo paso:** Revisar el CI de este commit y pasar una nueva ronda de `revisar-pr` sobre el PR #51.
- **Último commit:** a47af09

---

## 2026-09-21 22:36 — Lautaro073 (agy)
- **Hecho:** Se demostró el bloqueo por error de tipos en GitHub Actions con un archivo temporal permitido bajo `.github/workflows/`. El commit `98d6d3b` provocó `TS2322` y el job `typecheck` falló en el [run #35676222759](https://github.com/cadeApp/cadeApp/actions/runs/35676222759). El commit `1c8d7ed` retiró el archivo; no queda en el diff del PR.
- **Decisiones y por qué:** Se usaron dos commits normales para dejar evidencia remota y retirar la prueba sin reescribir la historia compartida. Esto prueba que el job detecta el error; la protección obligatoria de rama sigue sin estar disponible para este repositorio privado.
- **Pruebas:** `pnpm typecheck` ❌ con el error temporal (`Type 'string' is not assignable to type 'number'`); después de retirarlo, `pnpm typecheck` ✅, `pnpm lint` ✅ y `pnpm test` ✅ (72/72 Vitest y 16/16 workflows). El nuevo CI #35676360659 terminó con 7/8 jobs verdes, incluido `typecheck`; solo `db-types` falló por el drift real de `database.types.ts`. `pnpm test:db` n.a. porque no se tocó `supabase/` ni `src/server/`.
- **Falta:** Completar las comprobaciones en vivo de migración vacía en staging, dos merges seguidos y política de aprobación sobre PRs reales cuando los workflows estén en la rama base. T-004 debe sincronizar `database.types.ts` para que `db-types` pase. Falta el revisor obligatorio de `production`, no disponible en el plan actual.
- **Bloqueos:** PR #51 continúa en Draft con `db-types` rojo; no se puede declarar el DoD completo ni pedir aprobación con CI verde.
- **Próximo paso:** Integrar T-004 y volver a ejecutar el CI; luego coordinar las pruebas de migración y aprobación sobre ramas base sin saltear la revisión.
- **Último commit:** 1c8d7ed

---

## 2026-09-21 23:45 — Lautaro073 (agy)
- **Hecho:** Se leyó la ronda 3 agregada en `39eaa2c`. H07 quedó corregido con `.github/workflows/.eslintrc.json` que aplica `eslint:recommended` y entorno Node a los `.mjs`; el regex de indentación YAML usa `{2}`. H10 quedó atendido en el cuerpo de la PR con la justificación del presupuesto, la evidencia de pruebas en rojo y «Dependencias nuevas: ninguna».
- **Decisiones y por qué:** H09 conserva la aprobación de otra persona en `approval-policy` mientras la protección obligatoria de rama no está disponible; la actualización del check después de una review se verificará con una PR real tras el merge. H08 exige un informe en formato literal generado por quien apruebe, según el template; no se inventó ese informe. H11 corresponde a configuración global de fin de línea fuera de la ficha. `git rebase origin/develop` rehizo commits locales aun sin novedades en develop; se restauró inmediatamente el SHA remoto `39eaa2c`, sin push forzado ni pérdida de cambios.
- **Pruebas:** Antes del arreglo, la nueva prueba de lint falló (16/17): ESLint aceptaba una variable sin usar y `debugger`. Sin la configuración nueva volvió a fallar; con ella pasó (17/17). `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (72/72 Vitest y 17/17 workflows); Prettier ✅ para los archivos tocados con `--end-of-line auto`; `git diff --check` ✅. [CI #35680512190](https://github.com/cadeApp/cadeApp/actions/runs/35680512190): 7/8 jobs verdes, solo `db-types` falla por el drift de T-004. `pnpm test:db` n.a.; no se tocó `supabase/` ni `src/server/`.
- **Falta:** T-004 debe sincronizar `database.types.ts`. Después del merge de los workflows quedan las demostraciones reales de migración vacía en staging, dos merges seguidos y política de aprobación (incluido el recheck de H09). H08 requiere el informe literal de quien apruebe esta PR.
- **Bloqueos:** PR #51 sigue Draft y `db-types` continúa rojo; protección de rama y revisor obligatorio de `production` no disponibles en el plan actual.
- **Próximo paso:** Esperar la sincronización de tipos en T-004 y repetir CI; coordinar con la persona que apruebe el informe literal y, tras el merge, las demostraciones en vivo.
- **Último commit:** 33ebe3c

---

## 2026-09-21 23:58 — Lautaro073 (agy)
- **Hecho:** Se leyó la ronda 4 incorporada en `0c4a75f`. Confirma H07, H09 y H10 cerrados, sin bloqueantes de código. Se actualizó el resumen del PR #51 para enlazar la ronda 4 y el CI del SHA revisado.
- **Decisiones y por qué:** No se cambió código: H08 es el informe literal que debe aportar quien apruebe cuando el PR esté listo, y H11 necesita una tarea de configuración del repo fuera de la ficha. `origin/develop` sigue en el merge-base `75d00cc`; se evitó repetir un rebase que reescribiría la historia compartida sin incorporar novedades.
- **Pruebas:** `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (72/72 Vitest y 17/17 workflows). [CI #35681223980](https://github.com/cadeApp/cadeApp/actions/runs/35681223980): 7/8 jobs verdes; solo `db-types` falla por el drift pendiente de T-004. `pnpm test:db` n.a.; no se tocó `supabase/` ni `src/server/`.
- **Falta:** T-004 debe sincronizar `database.types.ts`; luego hay que repetir CI. Tras el merge, verificar las demostraciones en vivo del DoD, incluido el comportamiento del check al recibir una review. Quien apruebe aportará H08 contra el SHA final.
- **Bloqueos:** PR #51 continúa Draft con `db-types` rojo; protección de rama y revisor obligatorio de `production` siguen fuera de la configuración disponible.
- **Próximo paso:** Retomar cuando T-004 sincronice tipos o haya una nueva revisión accionable; entonces comprobar CI y coordinar el informe del aprobador.
- **Último commit:** 0c4a75f
