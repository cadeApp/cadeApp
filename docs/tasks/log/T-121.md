# Bitácora T-121 — Onboarding del repartidor

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 01:35 — P3 (agy)

- **Hecho:**
  1. Toma de tarea T-121 con la skill `tomar-tarea`. Verificación de dependencias T-009 (`0e8c235`) y T-008 (`164d368`) mergeadas en `origin/develop`. Creación de la rama `feat/T-121-courier-onboarding` desde `origin/develop`.
  2. Redacción previa de pruebas del DoD según TDD (Regla 40): unit de compresión de imágenes (`src/lib/image-compression.test.ts`), persistencia y recuperación ante cortes de red en subidas (`src/features/courier-onboarding/upload.test.ts` y componentes), y bloqueo de DNI para repartidores rechazados mediante `dni_hmac` (`src/features/courier-onboarding/actions.test.ts`). Demostración de fallas en rojo antes de implementar.
- **Decisiones y por qué:**
  - Alcance estrictamente acotado a los archivos permitidos por la ficha: `src/features/courier-onboarding/**`, `src/app/(courier)/onboarding/**` (visto bueno P2), `src/lib/image-compression.ts` (visto bueno P2), `docs/tasks/T-121.md`, `docs/tasks/log/T-121.md` y `docs/revision-pr/**`.
  - Compresión en cliente (`src/lib/image-compression.ts`) implementada mediante APIs nativas de navegador (Canvas / Blob / OffscreenCanvas) sin añadir dependencias en `package.json`, cumpliendo con la regla 25 y la ficha ("Dependencias nuevas permitidas: ninguna").
  - `dni_hmac` generado en Server Action con `node:crypto` HMAC-SHA256 (`serverEnv.DNI_HMAC_SECRET`). Detección y bloqueo de DNI repetido/rechazado (`DNI_ALREADY_REGISTERED`) consultando la base vía `createAdminClient` (`server-only`) debido a que la RLS congela `dni_hmac` en updates directos del repartidor.
  - Subida individual de cada documento con estado reactivo independiente (idle, uploading, success, error) para permitir reintentos aislados ante cortes de red sin reiniciar el progreso de documentos ya subidos.
- **Pruebas:** typecheck ❌ (esperado en rojo: módulos y tipos aún no implementados) · lint ❌ · test ❌ (`src/lib/image-compression.test.ts`, `src/features/courier-onboarding/actions.test.ts` fallan por falta de implementación previa).
- **Falta:** Implementar `src/lib/image-compression.ts`, `src/features/courier-onboarding/**` (schemas, actions, queries, copy, components R01/R02/R03), rutas en `src/app/(courier)/onboarding/**`, y pasar toda la suite a verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Crear las pruebas del DoD y verificar que fallan con `pnpm test`.
- **Último commit:** (pendiente tras redactar pruebas del DoD)
