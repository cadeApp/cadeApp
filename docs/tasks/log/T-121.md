# Bitácora T-121 — Onboarding del repartidor

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 01:35 — P3 (agy)

- **Hecho:**
  1. Toma de tarea T-121 con la skill `tomar-tarea`. Verificación de dependencias T-009 (`0e8c235`) y T-008 (`164d368`) mergeadas en `origin/develop`. Creación de la rama `feat/T-121-courier-onboarding` desde `origin/develop`.
  2. Redacción previa de pruebas del DoD según TDD (Regla 40): unit de compresión de imágenes (`src/features/courier-onboarding/image-compression.test.ts`), persistencia y recuperación ante cortes de red en subidas (`src/features/courier-onboarding/upload-resilience.test.ts`), y bloqueo de DNI para repartidores rechazados mediante `dni_hmac` (`src/features/courier-onboarding/actions.test.ts`). Demostración de fallas en rojo antes de implementar.
  3. Creación del PR Draft #77 enlazando el Issue #24 con descripción estructurada y checklist completo.
- **Decisiones y por qué:**
  - Alcance estrictamente acotado a los archivos permitidos por la ficha: `src/features/courier-onboarding/**`, `src/app/(courier)/onboarding/**` (visto bueno P2), `src/lib/image-compression.ts` (visto bueno P2), `docs/tasks/T-121.md`, `docs/tasks/log/T-121.md` y `docs/revision-pr/**`.
  - Compresión en cliente (`src/lib/image-compression.ts`) implementada mediante APIs nativas de navegador (Canvas / Blob / OffscreenCanvas) sin añadir dependencias en `package.json`, cumpliendo con la regla 25 y la ficha ("Dependencias nuevas permitidas: ninguna").
  - `dni_hmac` generado en Server Action con `node:crypto` HMAC-SHA256 usando la clave secreta de servidor. Detección y bloqueo de DNI repetido/rechazado (`DNI_ALREADY_REGISTERED`) consultando la base vía `createAdminClient` (`server-only`) debido a que la RLS congela `dni_hmac` en updates directos del repartidor.
  - Subida individual de cada documento con estado reactivo independiente (idle, uploading, success, error) para permitir reintentos aislados ante cortes de red sin reiniciar el progreso de documentos ya subidos.
- **Pruebas:** typecheck ❌ (módulos y tipos aún no implementados) · lint ❌ · test ❌ (`src/features/courier-onboarding/image-compression.test.ts`, `upload-resilience.test.ts` y `actions.test.ts` fallan en rojo por falta de implementación previa).
- **Falta:** Implementar `src/lib/image-compression.ts`, `src/features/courier-onboarding/**` (schemas, actions, upload-manager, components R01/R02/R03), rutas en `src/app/(courier)/onboarding/**`, y pasar toda la suite a verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `src/lib/image-compression.ts`, `src/features/courier-onboarding/**` y pasar las pruebas a verde.
- **Último commit:** 3be2c69 (PR Draft #77)

## 2026-09-24 03:30 — P3 (agy)

- **Hecho:**
  1. Resolución integral de hallazgos de revisión PR #77 (PR77-H01 a PR77-H09) mediante subagentes especializados de El Consejo:
     - `PR77-H01` y `PR77-H05`: Implementación completa de los módulos productivos en la rama (`src/lib/image-compression.ts`, `src/features/courier-onboarding/**`, `src/app/(courier)/onboarding/**`).
     - `PR77-H02`: Corrección de errores de compilación TypeScript. Tipado estricto con `Awaited<ReturnType<typeof serverAuth.createClient>>` para funciones asíncronas de mock.
     - `PR77-H03`: Aserciones explícitas en `actions.test.ts` para verificar la inserción de consentimientos (`mockInsertConsents`) y documentos (`mockUpsertDocuments`).
     - `PR77-H04`: Directiva `// @vitest-environment jsdom` y mocks de Canvas/Image para simular compresión nativa sin dependencias externas en Vitest.
     - `PR77-H06`: Verificación estricta de consulta por columna `dni_hmac` y caso de prueba cubriendo el bloqueo de repartidores con estado `suspended`.
     - `PR77-H07`: Integración con Supabase Storage para bucket `courier-docs` con path canónico `courier/${courierId}/...` respetando la política RLS `courier_docs_insert_own_folder`.
     - `PR77-H08`: Suite completa de pruebas de componentes en `components.test.tsx` cubriendo pantallas R01 (identidad y subida de documentos), R02 (vehículo y consentimientos) y R03 (estado de revisión).
     - `PR77-H09`: Ejecución de auto-revisión con skill `revisar-pr`, reporte sin bloqueantes y actualización de checklist del DoD en PR #77.
  2. Implementación de componentes de UI y páginas Next.js App Router:
     - `/onboarding/identity` (R01): Subida de 4 documentos con compresión automática, reintentos independientes y touch targets >= 48px (72px).
     - `/onboarding/vehicle` (R02): Grid 2x2 de tipo de vehículo, patente argentina condicional para moto/auto, consentimientos obligatorios (`tos`, `privacy`, `courier_contract`).
     - `/onboarding/status` (R03): Estado en revisión y checklist reactivo de documentos enviados.
  3. Verificación de calidad: `pnpm typecheck` (0 errores), `pnpm lint` (0 errores/warnings), suites de la feature pasando al 100% (34 tests en 4 suites).
- **Decisiones y por qué:**
  - Sin dependencias externas: `src/lib/image-compression.ts` utiliza exclusivamente la Canvas API del navegador, garantizando que el bundle y `package.json` permanezcan inalterados (Regla 25).
  - Enrutamiento: Las vistas se organizaron en subrutas canónicas `/onboarding/identity`, `/onboarding/vehicle` y `/onboarding/status` bajo `src/app/(courier)/onboarding/` para evitar colisión con el onboarding de comercios (`src/app/(merchant)/onboarding/page.tsx`).
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (34/34 tests de T-121 pasando en verde; suite global 294 tests pasando).
- **Falta:** Aprobación y merge por Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Revisión final y merge a develop.

