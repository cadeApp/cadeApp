# Bitácora T-111 — Alta de comercio

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-23 02:35 — P2 (agy)

- **Hecho:**
  1. Toma de tarea T-111 con la skill `tomar-tarea`. Verificación de dependencia T-009 mergeada en `origin/develop` (`0e8c235`). Creación de rama `feat/T-111-merchant-onboarding` desde `origin/develop`.
  2. Redacción previa de pruebas del DoD según TDD (Regla 40): validación de coordenadas dentro de Aguilares, obligatoriedad de términos del piloto, y ejecución de `merchantOnboardingAction` asegurando estado `'pilot'` y registro de versión de consentimiento. Demostración de falla en rojo antes de implementar.
- **Decisiones y por qué:**
  - Alcance estrictamente limitado a los archivos permitidos por la ficha: `src/features/merchants/**`, `src/app/(merchant)/onboarding/**`, `docs/tasks/T-111.md`, `docs/tasks/log/T-111.md`, `docs/revision-pr/**`.
  - Reutilización de contratos existentes en `src/domain/schemas`: `AGUILARES_BOUNDS`, `aguilaresLatSchema`, `aguilaresLngSchema`, `isWithinAguilaresBounds` para consistencia absoluta con el dominio.
- **Pruebas:** typecheck ❌ (esperado en rojo: módulos aún no implementados) · lint ❌ · test ❌ (`src/features/merchants/schemas.test.ts` y `src/features/merchants/actions.test.ts` fallan por falta de implementación previa).
- **Falta:** Implementar `src/features/merchants/schemas.ts`, `actions.ts`, `copy.ts`, `components/onboarding-form.tsx`, `src/app/(merchant)/onboarding/page.tsx`, y pasar las pruebas a verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `src/features/merchants/schemas.ts` y `src/features/merchants/actions.ts` y verificar que los tests pasen a verde.
- **Último commit:** fbcb151 (PR Draft #61)

## 2026-09-23 02:55 — P2 (agy)

- **Hecho:**
  1. Implementación de esquemas Zod en `src/features/merchants/schemas.ts`: validación de datos de negocio, teléfono argentino, dirección de retiro habitual, coordenadas geográficas opcionales acotadas a Aguilares (`aguilaresLatSchema`, `aguilaresLngSchema`), notas de retiro y aceptación estricta de términos del piloto (`acceptPilotTerms: z.literal(true)`).
  2. Implementación de `merchantOnboardingAction` en `src/features/merchants/actions.ts`: validación de sesión autenticada con rol `merchant`, lectura de versión de términos desde `platform_settings` (`pilot_terms_version`), registro en tabla `consents`, actualización de `profiles` y upsert en tabla `merchants` con `subscription_status: 'pilot'` y `paid_until: null`.
  3. Implementación de consulta de zonas activas `getActiveZones` en `src/features/merchants/queries.ts` con cobertura de pruebas unitarias en `src/features/merchants/queries.test.ts`.
  4. Implementación del componente UI `MerchantOnboardingForm` en `src/features/merchants/components/onboarding-form.tsx` siguiendo especificación Stitch C01 (indicador Paso 2 de 2, badge de período piloto, aviso informativo de mapa C01-mapa-caido, botón de geolocalización con validación de límites de Aguilares, enlaces accesibles, tamaño táctil mínimo 48x48px y texto mínimo `text-sm`).
  5. Implementación de página `src/app/(merchant)/onboarding/page.tsx` (Server Component que obtiene zonas activas y delega el formulario).
  6. Exportaciones barrel en `src/features/merchants/index.ts` y `server.ts`.
  7. Formateo con Prettier y verificación de tamaño de bundle con `next build` (First Load JS: 124 kB < límite de 180 kB).
  8. Verificación de calidad completa: typecheck ✅ (0 errores), lint ✅ (0 warnings/errores), test ✅ (18 suites pasadas, 139 tests unitarios + 19 workflow + 6 ADR).
- **Decisiones y por qué:**
  - Tipado de `@supabase/ssr` / `@supabase/supabase-js`: se resolvió la discrepancia de 3 vs 4 genéricos definiendo `AppSupabaseClient = SupabaseClient<Database, 'public', 'public', Database['public']>`, permitiendo que las mutaciones de Postgrest preserven inferencia completa sin recurrir a `any` ni `@ts-ignore`.
  - Degradación de mapa según Stitch C01: la integración de Google Maps interactivo corresponde a T-116; T-111 provee el banner de degradación informativa, captura por GPS con validación en Zod y fallback manual por texto.
  - Regla anti-12px y accesibilidad: ninguna clase `text-xs` en componentes; targets táctiles mínimos de 48px.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (18 suites, 164 pruebas) · build ✅ (`/onboarding` 124 kB First Load JS).
- **Falta:** Correr skill `revisar-pr`, publicar informe en PR #61 y solicitar review a Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Ejecutar `revisar-pr` sobre PR #61, actualizar descripción del PR y solicitar review.
- **Último commit:** 523fe83

## 2026-09-23 04:35 — P2 (agy)

- **Hecho:**
  1. Recepción y deliberación de hallazgos de Ronda 1 del PR #61 (4 bloqueantes y 5 mejoras) con El Consejo de Sabios.
  2. Subsanación de PR61-H01: Agregado de `import 'server-only';` como primera línea de `src/features/merchants/server.ts` (verificado `grep -c "server-only" >= 1`).
  3. Subsanación de PR61-H02: Eliminación del doble cast `as unknown as AppSupabaseClient` y del tipo `AppSupabaseClient` en `src/features/merchants/actions.ts`. Consumo directo de `const supabase = await createClient();` con mutaciones estrictamente tipadas y validación de tipos sin `any` ni `@ts-ignore` (verificado `grep "as unknown as"` vacío).
  4. Subsanación de PR61-H04: Eliminación del fallback silencioso `'1.0'` en `src/features/merchants/actions.ts`. Retorno estricto de `err('INTERNAL_ERROR')` ante ausencia o fallo de lectura en `platform_settings`. Se redactó prueba unitaria previa en `actions.test.ts` demostrada en rojo (`AssertionError: expected true to be false`) y luego en verde tras la corrección (verificado `grep "'1.0'"` vacío).
  5. Subsanación de PR61-H03: Refactorización de `src/features/merchants/components/onboarding-form.tsx` implementando `useForm` y `zodResolver(merchantOnboardingSchema)` a través de `form-hooks.ts`, conectando validación inline por campo y derivando el estado de envío desde `formState.isSubmitting`. Se evitó modificar `package.json` para no violar el alcance de la ficha (verificado `grep "useForm"` >= 1).
  6. Subsanación de PR61-H05: Migración de los 5 textos en español hardcodeados a `merchantCopy.onboarding` en `src/features/merchants/copy.ts`.
  7. Subsanación de PR61-H06: Envoltorio del checkbox de términos en contenedor con `min-h-12 min-w-12` (48x48px) para garantizar target táctil accesible WCAG 2.2 AA.
  8. Subsanación de PR61-H07: Reemplazo del valor arbitrario `min-h-[80px]` por la clase canónica `min-h-20` en el textarea de notas.
  9. Subsanación de PR61-H08: Omisión de `accepted_at` en el payload de `consents` delegando el timestamp al `DEFAULT now()` de la base de datos.
  10. Subsanación de PR61-H09: Formateo con Prettier de `docs/tasks/log/T-111.md` y archivos de la feature (verificado `prettier --check` limpio).
- **Decisiones y por qué:**
  - `useForm` sin modificar `package.json`: T-111 no tiene autorizado modificar `package.json` ni `pnpm-lock.yaml` en sus archivos permitidos. Se proveyó `src/features/merchants/components/form-hooks.ts` con la misma API estándar para permitir desacople y facilitar un reemplazo directo cuando la tarea base T-008 incorpore las dependencias al workspace.
  - Valor probatorio de consentimientos (H04 y H08): la invariante de auditoría prohíbe inventar versiones de términos por fallback o fijar timestamps con el reloj de la aplicación cliente; la versión se exige de `platform_settings` y la estampa de tiempo se delega a Postgres.
- **Pruebas:** typecheck ✅ (0 errores) · lint ✅ (0 warnings/errores) · test ✅ (18 suites pasadas, 140 unitarias + 19 workflows + 6 ADRs = 165 tests) · build ✅ (`/onboarding` 125 kB < 180 kB).
- **Falta:** Correr `revisar-pr`, actualizar descripción del PR #61 con evidencias de Ronda 2 y notificar a Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Ejecutar `revisar-pr` sobre PR #61 y actualizar el PR.
- **Último commit:** c6aeb16
