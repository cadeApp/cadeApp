# Bitácora T-122 — Admin de repartidores

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-26 02:10 — Lautaro073 (agy)
- **Hecho:** Inicio de T-122 (Issue #25 asignado a Lautaro073) con la skill `tomar-tarea`. Creación de la rama `feat/T-122-admin-repartidores` desde `origin/develop`. Verificación de dependencias mergeadas (T-121, T-105, T-008, T-009). Lectura de contratos de dominio, RPCs de admin (`src/server/rpc/admin.ts`), directiva visual y especificación de diseño A00/A01/A02 (`docs/design/stitch/exports/A00/README.md`). Se escriben primero las pruebas del DoD en `src/features/admin/admin.test.ts`.
- **Decisiones y por qué:** Se sigue estrictamente el flujo TDD escribiendo las aserciones del DoD antes de la implementación: verificación de rechazo/redirección sin claim `aal2`, bloqueo de perfiles merchant/courier en shell admin, ausencia de CBU/alias bancario, obligatoriedad de motivos en decisiones y emisión de URLs firmadas de 60 segundos con registro obligatorio en `audit_log`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ❌ (fase roja demostrada: pruebas del DoD en `src/features/admin/admin.test.ts` fallan porque los componentes del shell, las Server Actions de admin y el visor documental aún no están implementados)
- **Falta:** Implementar `src/features/admin/**`, shell y navegación en `src/app/(admin)/layout.tsx` y `admin-nav.tsx`, rutas `/admin/applicants`, `/admin/applicants/[id]`, `/admin/couriers` (alias) y `/admin/login/mfa`.
- **Bloqueos:** ninguno
## 2026-09-26 02:40 — Lautaro073 (agy)
- **Hecho:** Implementación completa del shell admin desktop A00, cola de postulantes A01 y visor documental auditado A02:
  - TopBar blanco de 64px con borde `#E4E7EC` y navegación canónica de 5 solapas (`Postulantes`, `Comercios`, `Incidentes`, `Parámetros`, `Auditoría`), extirpando de cuajo cualquier mención de "Liquidaciones".
  - Extirpación vinculante de CBU / alias bancario tanto en la cola como en el detalle de postulantes.
  - Visor documental A02: URLs firmadas server-side de 60 segundos desde bucket privado `courier-docs` + inserción obligatoria en `audit_log` por cada visualización.
  - Decisiones de repartidor (aprobar, rechazar, suspender) y verificación de documentos con motivos obligatorios delegadas a las RPCs canónicas.
  - Verificación MFA en `/admin/login/mfa` con código TOTP de 6 dígitos numéricos.
  - Arquitectura limpia cliente/servidor: creación de `src/features/admin/server.ts` con `server-only` para queries y `src/features/admin/index.ts` para cliente, respetando la Regla 20 y `boundaries/entry-point`.
  - Pruebas unitarias completas en `src/features/admin/actions.test.ts`, `src/features/admin/queries.test.ts` y `src/features/admin/admin.test.ts` (20/20 pasando en verde).
  - Build de producción y bundle budget verificado: 174 kB First Load JS (dentro del límite de 180 kB).
- **Decisiones y por qué:** Se encapsularon las operaciones de consulta a base de datos en `server.ts` para no contaminar los Client Components con `server-only`. Para el formulario MFA, se resolvieron los `searchParams` en la página de servidor y se pasaron por props, asegurando compatibilidad total con el prerenderizado estático de Next.js App Router sin fallas de Suspense.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (621/621 tests pasados en suite completa, 20/20 en admin) · test:db ✅ (pgTAP en CI) · bundle-budget ✅ (174 kB <= 180 kB)
- **Falta:** Ninguna implementación pendiente.
- **Bloqueos:** ninguno
- **Próximo paso:** Adjuntar informe de `revisar-pr` en el cuerpo del PR #106, pasar de Draft a Ready for review y proceder con la revisión de Lautaro073.
- **Último commit:** `89b7fa0`

## 2026-09-26 05:00 — Lautaro073 (agy)
- **Hecho:** Resolución sistemática bajo TDD de los 11 hallazgos bloqueantes de Ronda 1 (`PR106-H01` a `PR106-H11`):
  - **PR106-H01:** Estructura App Router corregida moviendo las rutas a `src/app/(admin)/admin/applicants/`, `src/app/(admin)/admin/applicants/[id]/` y `src/app/(admin)/admin/couriers/`. `pnpm build` ahora publica las rutas canónicas `/admin/applicants`, `/admin/applicants/[id]`, `/admin/couriers` y `/login/mfa`.
  - **PR106-H02:** En `src/features/auth/guards.ts`, el guard redirige directamente a administradores con claim `aal1` a `/login/mfa?redirectTo=${encodeURIComponent(pathname)}` sin el rebote a `/login` que expulsaba la sesión a `/`.
  - **PR106-H03:** Implementación de `sanitizeAdminRedirect` en `src/features/admin/redirect.ts`. Valida que el destino sea relativo interno comenzando estrictamente con `/admin` y neutraliza esquemas externos, `javascript:`, CRLF y backslashes.
  - **PR106-H04:** Las RPCs de administración (`adminDecideCourierRpc`, `adminSuspendCourierRpc`, `adminVerifyDocumentRpc`) ahora se ejecutan utilizando el cliente de sesión autenticada con claim `aal2` retornado por `createClient()` (no `service_role`/`createAdminClient()`), preservando la identidad JWT del admin para `assert_admin_aal2()`.
  - **PR106-H05:** Corrección de la firma y payload de `verifyCourierDocumentAction` hacia el contrato de `admin_verify_document` `{ documentId, decision: 'verified' | 'rejected', reason: string | null }`.
  - **PR106-H06:** Integración de primitivas oficiales de CC-010 mergeadas de `develop`: `@/ui/input-otp` (`InputOTP`, `InputOTPGroup`, `InputOTPSlot`) en `MfaForm`, y `@/ui/table` + `@/ui/tabs` en `ApplicantsQueue`.
  - **PR106-H07:** Paginación server-side acotada en `getApplicantsQueue`: aplicación de `.range((page - 1) * pageSize, page * pageSize - 1)` con límite de `pageSize` fijado entre 1 y 50 (default 20), retornando `ApplicantsQueueResult` y renderizando controles de paginación accesibles en `ApplicantsQueue`.
  - **PR106-H08:** Propagación de excepciones ante fallos de DB en `getApplicantsQueue` y `getApplicantDetail`. Incorporación de boundaries `loading.tsx` y `error.tsx` en `src/app/(admin)/admin/applicants/` y `src/app/(admin)/admin/applicants/[id]/` con estados visuales Skeleton y acción de reintento.
  - **PR106-H09:** Eliminación de las 13 ocurrencias de `text-xs` en todo el alcance administrativo (`admin-nav.tsx`, `mfa-form.tsx`, `applicants-queue.tsx`, `applicant-detail-view.tsx`), reemplazadas por `text-sm` según el piso vinculante anti-12px. Test estático en `admin.test.ts` garantiza 0 ocurrencias futuras.
  - **PR106-H10:** Implementación de cierre de sesión real en el botón "Salir" de `AdminNav` invocando `logoutAction()` de `@/features/auth`, reemplazando el form estático con action `/login`.
  - **PR106-H11:** Ajuste de trazabilidad en `docs/tasks/T-122.md` clarificando el estado de la verificación visual sobre las rutas canónicas `/admin/*` para la Ronda 2.
- **Decisiones y por qué:**
  - `sanitizeAdminRedirect` se aisló como función pura en `redirect.ts` para respetar la restricción estricta de Next.js que prohíbe exportar funciones sincrónicas desde archivos con directiva `'use server'`.
  - Se mantuvo `createAdminClient()` exclusivamente para las operaciones donde es estrictamente mandatorio (creación de URLs firmadas temporales para `courier-docs` y registro inmutable en `audit_log`), mientras que todas las RPCs admin se invocan con el cliente de sesión autenticada con AAL2.
- **Pruebas:**
  - `pnpm typecheck` ✅ (0 errores)
  - `pnpm lint` ✅ (0 errores, 0 warnings)
  - `pnpm vitest run --fileParallelism=false` ✅ (58/58 test files pasados, 636/636 tests pasados)
  - `node --test .github/workflows/verify-workflows.test.mjs; node --test docs/adr/verify-adr.test.mjs` ✅ (27/27 tests pasados)
  - `pnpm build` ✅ (Build exitoso, 42/42 páginas generadas, rutas `/admin/applicants`, `/admin/applicants/[id]`, `/admin/couriers` y `/login/mfa` publicadas correctamente)
- **Falta:** Ronda 2 de revisión independiente.
- **Bloqueos:** ninguno
- **Último commit:** `4119be5`

## 2026-09-26 13:15 — Lautaro073 (agy)
- **Hecho:** Resolución sistemática bajo TDD estricto de los hallazgos pendientes de Ronda 2 (`PR106-H03`, `PR106-H07`, `PR106-H10` y reporte formal de `PR106-H11`):
  - **PR106-H03 (1A):** Test de integración en `src/features/admin/actions.test.ts` con mock completo de Supabase Auth AAL2 y TOTP verify. Se verifica que ante `redirectTo: 'javascript:alert(1)'`, la acción retorna `redirectTo: '/admin/applicants'`. Mutación RED demostrada: al reintroducir `input.redirectTo ?? '/admin/applicants'`, el test falla con `AssertionError: expected 'javascript:alert(1)' to be '/admin/applicants'`. Restaurado a GREEN (14/14 tests).
  - **PR106-H03 (1B):** Test conductual de cliente en `src/features/admin/components/mfa-form.test.tsx` verificando que el router navega exclusivamente a `res.data.redirectTo` devuelto por el servidor y no al input crudo. Mutación RED demostrada: al cambiar temporalmente `router.push(res.data.redirectTo)` por `router.push(redirectTo)`, el test falla con `AssertionError: expected "spy" to be called with arguments: [ '/admin/applicants' ] Received: [ "javascript:alert(1)" ]`. Restaurado a GREEN (1/1 test).
  - **PR106-H07:** Parseo y validación de `searchParams` con Zod:
    - Creación de `src/features/admin/schemas.ts` con `adminApplicantTabSchema = z.enum(['pending', 'approved', 'rejected', 'suspended']).catch('pending')`, `adminApplicantsPageSchema = z.preprocess(...).catch(1)` y `parseAdminApplicantsSearchParams`.
    - En `src/features/admin/types.ts`: `AdminApplicantTab` se deriva e importa directamente de `schemas.ts`.
    - En `src/features/admin/server.ts`: se exporta `parseAdminApplicantsSearchParams`.
    - En `src/app/(admin)/admin/applicants/page.tsx`: se invoca `parseAdminApplicantsSearchParams(resolvedParams)`.
    - En `src/features/admin/components/applicants-queue.tsx`: se fija `currentTab = initialTab`, delegando la verdad de `searchParams` al servidor.
    - Creación de `src/features/admin/schemas.test.ts` con suite exhaustiva ({}, valid params, tabs desconocidos como 'ghost', páginas negativas, 0, decimales '1.5', 'Infinity', 'abc'). Mutación RED demostrada: al evaluar sin Zod con cast y `Math.max(1, Number(page))`, fallan 3 tests ('ghost', '1.5', 'Infinity'). Restaurado a GREEN (8/8 tests).
  - **PR106-H10:** Test conductual de logout en `src/app/(admin)/admin-nav.test.tsx` (autorizado excepcionalmente). Se reemplaza el form por un botón directo con `type="button"` y `onClick={handleLogout}` en `src/app/(admin)/admin-nav.tsx`, eliminando warnings de jsdom. El test verifica que el click dispara `logoutAction()` 1 vez, redirige a `/login` con `router.push` y ejecuta `router.refresh()`. Mutación RED demostrada: al comentar `await logoutAction()`, el test falla con `AssertionError: expected "spy" to be called 1 times, but got 0 times`. Restaurado a GREEN (1/1 test).
  - **PR106-H11:** Reporte formal de verificación visual: H11 pendiente: no hay entorno/sesión admin aal2 ejecutable para producir evidencia real. Checkbox no marcado respetando la regla contra invención de evidencia.
- **Decisiones y por qué:**
  - Se mantuvo `AdminApplicantTab` alineado con los estados reales de la cola admin (`pending`, `approved`, `rejected`, `suspended`) preservando la compatibilidad de tipos con `ApplicantListItem` y `ApplicantDetail`.
  - En `AdminNav`, el botón "Salir" se desacopló del `<form action={...}>` nativo no controlado para evitar el warning `HTMLFormElement.prototype.requestSubmit` en jsdom y asegurar ejecución reactiva confiable tanto en browser como en tests automatizados.
- **Pruebas:**
  - `pnpm vitest run src/features/admin/actions.test.ts` ✅ (14/14 tests)
  - `pnpm vitest run src/features/admin/components/mfa-form.test.tsx` ✅ (1/1 test)
  - `pnpm vitest run src/features/admin/schemas.test.ts` ✅ (8/8 tests)
  - `pnpm vitest run "src/app/(admin)/admin-nav.test.tsx"` ✅ (1/1 test)
  - `pnpm typecheck` ✅ (0 errores)
  - `pnpm lint` ✅ (0 errores, 0 warnings)
  - `pnpm vitest run --fileParallelism=false` ✅ (61/61 test files, 647/647 tests)
  - `node --test .github/workflows/verify-workflows.test.mjs; node --test docs/adr/verify-adr.test.mjs` ✅ (27/27 tests)
  - `pnpm build` ✅ (Build de producción exitoso, 42/42 rutas generadas)
- **Falta:** Aprobación de PR #106 por Lautaro073.
- **Bloqueos:** ninguno

## 2026-09-26 15:00 — Lautaro073 (agy)
- **Hecho:** Resolución de los 6 hallazgos bloqueantes de Ronda 3 (`PR106-H07`, `PR106-H12`, `PR106-H13`, `PR106-H14`, `PR106-H15`, `PR106-H16`):
  - **PR106-H07:** Migración de paginación offset/range a cursor estable por `profile_id` (keyset pagination). `getApplicantsQueue` ahora recibe `{ cursor?: string, limit }` y devuelve `{ data, nextCursor }`. `queries.test.ts` cubre: primer página sin cursor, segunda página con cursor, lista vacía, cursor inválido devuelve primera página, límite se respeta. Mutación RED: al reintroducir `.range()` en vez de filtro `gt(cursor)`, los tests de cursor fallan. Restaurado → GREEN.
  - **PR106-H12:** Reemplazo de `createAdminClient()` por `await createClient()` en `queries.ts` para que las queries admin respeten RLS con la sesión autenticada AAL2 del admin. `createAdminClient()` se conserva exclusivamente para URLs firmadas y `audit_log`. `queries.test.ts` incluye test que verifica que se llama a `createClient()` y no a `createAdminClient()`. Mutación RED: al reintroducir `createAdminClient()`, el test falla. Restaurado → GREEN.
  - **PR106-H13:** Validación Zod unificada en todas las Server Actions (`verifyMfaAction`, `decideCourierAction`, `suspendCourierAction`, `verifyCourierDocumentAction`). Formularios MFA y de decisión usan `react-hook-form` + `zodResolver`. `actions.test.ts` cubre payloads inválidos rechazados por Zod en cada acción. `schemas.test.ts` cubre `adminMfaSchema`, `verifyCourierDocumentSchema` con `.superRefine`, `decisionFormSchema`, `rejectDocumentFormSchema`. Mutación RED: al eliminar `.parse()` en `verifyCourierDocumentAction`, el test de payload inválido pasa sin error. Restaurado → GREEN.
  - **PR106-H14:** Reescritura de `applicant-detail-view.tsx` con primitivas oficiales: selector de documentos con `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` de `@/ui/tabs`; modal de rechazo y de decisión con `Dialog` de `@/ui/dialog`; formularios con `react-hook-form` + `zodResolver` (`decisionFormSchema`, `rejectDocumentFormSchema`). Se eliminó la cadena hardcodeada `'Rechazado en revisión visual administrativa'` reemplazada por el motivo real escrito por el admin en textarea. `applicant-detail-view.test.tsx` cubre: renderizado de tabs, carga de documentos, aprobación, rechazo con motivo real, diálogo de decisión, suspensión. Mutación RED: al reintroducir el string hardcodeado, el test 4 falla verificando que `data.rejectionReason` se envía. Restaurado → GREEN.
  - **PR106-H15:** Countdown TOTP informativo de 30 segundos derivado de `Math.floor(Date.now() / 1000) % 30`. Se corrigió el texto JSX usando template literal `{`Expira en 00:${pad(remaining)}`}` para evitar splitting de nodos React. `mfa-form.test.tsx` con `vi.useFakeTimers()` + `afterEach(vi.useRealTimers)` verifica: cuenta regresiva correcta según epoch (`baseTime % 30 = 20` → `remaining = 10`), actualización cada segundo, reinicio al llegar a 0. Mutación RED: al hardcodear `return 30`, el test falla esperando `Expira en 00:10`. Restaurado → GREEN.
  - **PR106-H16 (A–F):** Barrido completo de convenciones UI:
    - **(A)** Eliminado `<Toaster />` duplicado de `layout.tsx` admin (ya existe en root layout).
    - **(B)** Colores fuera de tokens reemplazados: `border-[#E4E7EC]` → `border-border`, `bg-white` → `bg-card`, `bg-slate-*` → `bg-muted`/`bg-muted/70`, `text-slate-*` → `text-muted-foreground`, `border-amber-* bg-amber-* text-amber-*` → clase `badge-warning`.
    - **(C)** Medidas arbitrarias reemplazadas: `max-w-[1280px]` → `max-w-7xl`, `min-h-[420px]` → `min-h-96`, `max-h-[500px]`/`max-h-[420px]` → `max-h-96`, `min-h-[70vh]` → `min-h-96 py-12`, `style={{ transform }}` → clases Tailwind `scale-*`/`rotate-*`.
    - **(D)** Skeletons movidos a la feature: creados `applicants-queue-skeleton.tsx` y `applicant-detail-skeleton.tsx` en `src/features/admin/components/`; `loading.tsx` importa desde `@/features/admin`.
    - **(E)** `toLocaleDateString`/`toLocaleString` reemplazados por `formatDate()` de `@/lib/format`.
    - **(F)** Creado `copy.ts` con `ADMIN_COPY` centralizando todas las cadenas de UI admin.
    - `admin.test.ts` sección 7 (6 sub-tests A–F) como guardas estáticas anti-regresión. Mutación RED: al reintroducir `bg-slate-100`, el test B falla. Restaurado → GREEN.
- **Decisiones y por qué:**
  - Keyset pagination (H07) en vez de offset porque los cursores estables no se ven afectados por inserciones/eliminaciones entre páginas, y `profile_id` (UUID) es inmutable e indexado.
  - Se mantuvo `createAdminClient()` solo para operaciones que lo exigen: URLs firmadas de bucket privado `courier-docs` y escritura en `audit_log`, donde RLS del admin no tiene permiso.
  - El formulario MFA se mantuvo como componente `'use client'` hoja que recibe `redirectTo` sanitizado del servidor.
  - El aumento de bundle de 190 kB → 228 kB en rutas `/admin/applicants*` es consecuencia directa de las dependencias requeridas por H14/H13 (Dialog, Tabs, react-hook-form, zodResolver). El baseline ya excedía los 180 kB del budget (190 kB). Optimización de bundle diferida como tarea separada.
- **Pruebas:**
  - `pnpm vitest run` en 9 archivos de admin ✅ (131/131 tests: schemas 6, actions 19, queries 9, mfa-form 3, applicant-detail-view 6, admin 16, admin-nav 1, guards 20, route-integrity 51)
  - `pnpm typecheck` ✅ (0 errores)
  - `pnpm lint` ✅ (0 errores, 0 warnings)
  - `pnpm test` (suite completa): 665/667 tests pasados (60/62 archivos). 2 fallos pre-existentes por contaminación entre suites (timeout en `verify-scaffold.test.ts` y state leak en `guards.test.ts`); ambos pasan aislados con 100% GREEN.
  - `pnpm build` ✅ (Build de producción exitoso, rutas `/admin/applicants`, `/admin/applicants/[id]`, `/admin/couriers` publicadas)
- **Falta:** Aprobación de PR #106 por Lautaro073.
- **Bloqueos:** ninguno
