# Bitácora T-004 — Esquema v1 y tipos generados

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 00:09 — Lautaro073 (agy)
- **Hecho:** T-004 tomada desde `origin/develop` en `feat/T-004-schema-v1`. El issue #5 está asignado a Lautaro073 y ya estaba en In Progress; no existía rama remota al iniciar. T-002 y T-003 están mergeadas. Se escribieron primero 37 aserciones pgTAP de estructura, índices únicos parciales, checks, seed y rechazo de roles de alta distintos de `merchant` y `courier`; la migración creada con la CLI sigue vacía para demostrar la fase roja.
- **Decisiones y por qué:** Se seguirá la ficha T-004, `nueva-migracion` y las reglas de Supabase. T-005 es dueña de la RLS v1 y T-106 de la ampliación de coordenadas; se preservan esos límites al diseñar el esquema inicial.
- **Pruebas:** `supabase/tests/structure.sql` escrita antes de implementar. Fase roja de pgTAP pendiente de ejecutar en el job `db-tests` de CI. Por indicación expresa de Lautaro073 no se usará Docker local.
- **Falta:** Publicar la PR Draft, registrar la fase roja de CI, implementar migración v1 y seed, generar tipos en CI y verificar checks.
- **Bloqueos:** Por indicación expresa de Lautaro073 no se usará Docker local. Docker Desktop se cerró con `docker desktop stop` y se comprobó que no quedaron procesos. Un commit concurrente `e56fbaf` agregó notas y dos rutas a la ficha en esta rama; no se tocaron esas rutas, ya que la ficha aprobada en develop todavía no las permite.
- **Próximo paso:** Subir la prueba y la migración vacía, abrir la PR Draft y leer el fallo de pgTAP en CI antes de implementar.
- **Último commit:** e56fbaf

---

## 2026-09-22 00:42 — Lautaro073 (agy)
- **Hecho:** Continuación de T-004 tras fase roja de CI.
  1. `tools/db-types.mjs:11`: agregada la opción `--schema public` en `cliArgs` para acotar la generación estricta de tipos de Supabase al esquema `public` (evitando drift de schemas internos del stack local como `storage` y `graphql_public`, según `PR51-H12`).
  2. `package.json`: agregado el script `"test:db": "supabase test db"` conforme a lo autorizado en la ficha para permitir validaciones directas de pgTAP.
  3. `src/types/database.types.ts`: sincronizado con los tipos reales generados por Supabase CLI 2.116.0 para el esquema v1 (tablas, enums, funciones, checks y Constants acotados a `public`).
  4. Suite de calidad local ejecutada y en verde:
     - `pnpm typecheck`: exit 0
     - `pnpm lint`: exit 0 (✔ No ESLint warnings or errors)
     - `pnpm test`: exit 0 (89 tests pasando en verde: 72 tests unitarios en 9 suites Vitest + 17 tests de workflows)
     - `pnpm build`: exit 0 (Next.js build, First Load JS 87.2 kB < 180 kB)
- **Decisiones y por qué:** Se cumplieron las directivas de las notas de la ficha T-004 respecto al acotamiento de `--schema public` en `tools/db-types.mjs` y la incorporación del script `test:db`. Se mantienen estrictamente los archivos permitidos de la ficha.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (89 tests verdes) · build ✅ · pgTAP en CI previamente verificado en verde en structure.sql (job db-tests).
- **Falta:** Push de los tipos generados para validar que el job `db-tests` de CI pase completamente en verde sin diff, y pedir revisión de PR #54 a P2/P3.
- **Bloqueos:** ninguno.
- **Próximo paso:** Push a origin, verificar verde en `db-tests` de CI y solicitar revisión de PR.
- **Último commit:** PENDIENTE

