# Bitácora T-113 — Mis solicitudes, ofertas en tiempo real y aceptar oferta

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 01:32 — asako669 (agy)
- **Hecho:** Inicio de tarea `T-113` (Issue #19 asignado a `asako669`) con la skill `tomar-tarea`. Verificación de dependencias (`T-112` y `T-102` confirmadas mergeadas en `origin/develop`). Creación de la rama `feat/T-113-requests-offers` desde `origin/develop`. Redacción previa de pruebas del DoD según TDD (Regla 40) en `src/features/offers/actions.test.ts` y `src/features/requests/components/request-offers.test.tsx`.
- **Decisiones y por qué:**
  - `acceptOfferAction` se ubica en `src/features/offers/actions.ts` (reexportada por `src/features/offers/server.ts` y utilizable por `src/features/requests/components/`), completando las acciones de ofertas (`submitOfferAction`, `withdrawOfferAction`, `acceptOfferAction`).
  - Pruebas del DoD cubren: rechazo sin autenticación, rechazo de actor no comerciante (`UNAUTHORIZED_ACTOR`), validación Zod de `offerId`, captura de `ALREADY_MATCHED`, `INVALID_STATE`, éxito con `ok` e invalidación de rutas; ausencia total de estrellas/reviews/calificaciones según Supuesto S4 y auditoría Stitch; ordenamiento por `doc_level` e insignias verificadas; modal de confirmación C05 con desglose de tarifa y medio de pago; y reactividad en vivo (Realtime) donde la oferta nueva aparece sin recargar la página.
- **Pruebas:** typecheck ⏳ · lint ⏳ · test ❌ (Fase Roja demostrada: `acceptOfferAction is not a function` en `src/features/offers/actions.test.ts` y falta de módulo `request-offers-list` en `src/features/requests/components/request-offers.test.tsx`) · test:db n.a.
- **Falta:** Implementar schemas de aceptación y consultas de solicitudes de comercio, `acceptOfferAction` en `src/features/offers/actions.ts`, queries en `src/features/requests/queries.ts`, hook de tiempo real en `src/features/requests/hooks/`, componentes UI (C02, C04, C05) y rutas en `src/app/(merchant)/requests/**`.
- **Bloqueos:** ninguno
- **Próximo paso:** Resolver hallazgos de Ronda 1 con El Consejo.
- **Último commit:** `14235ff`

## 2026-09-24 02:55 — asako669 (agy)
- **Hecho:**
  1. Deliberación y resolución técnica de hallazgos de revisión de Ronda 1 del PR #76 con El Consejo (`consejo-backend` y `consejo-frontend & consejo-qa`):
     - **PR76-H01:** Implementación completa del código de producción de T-113: Server Action `acceptOfferAction` en `src/features/offers/actions.ts` (con reexportación en `src/features/offers/index.ts` y `server.ts`), pantallas C02 (`src/app/(merchant)/requests/page.tsx`), C04 (`src/app/(merchant)/requests/[id]/page.tsx`), loading skeletons que imitan la forma final (`loading.tsx`), y componentes accesibles `MerchantRequestsList` y `RequestOffersList`.
     - **PR76-H02:** Alineación estricta del mock de retorno de `accept_offer` en `actions.test.ts` con el contrato canónico `acceptOfferOutputSchema` (`{ status: 'matched', requestId, acceptedOfferId, matchedAt, idempotent }`).
     - **PR76-H03:** Corrección del código de error mockeado en `actions.test.ts` a `INVALID_STATE_TRANSITION` (en lugar del inexistente `INVALID_STATE`).
     - **PR76-H04:** Aserción explícita de `expect(revalidatePath).toHaveBeenCalledWith('/merchant/requests')` agregada a la prueba de éxito en `actions.test.ts`.
     - **PR76-H05:** Sustitución del prop espurio `onRegisterRealtime` en `request-offers.test.tsx` por el mockeo de la suscripción real de Supabase Realtime (`channel('offers-' + requestId)`, evento `postgres_changes`, filtro `request_id=eq.${requestId}` y cleanup en desmonte).
     - **PR76-H06:** Eliminación de matchers de Jest-DOM y adopción de matchers nativos de Vitest (`toBeDefined()`, `toBeNull()`, `toContain()` sobre `.textContent`) y adición de directiva `// @vitest-environment jsdom`.
     - **PR76-H07:** Cobertura ampliada en `actions.test.ts` con pruebas parametrizadas para errores canónicos (`REQUEST_EXPIRED`, `NOT_FOUND`, `OFFER_NOT_PENDING`, `COURIER_SUSPENDED`, `COURIER_NOT_APPROVED`).
     - **PR76-H08:** Suite unitaria completa para pantalla C02 en `src/features/requests/components/merchant-requests-list.test.tsx` (métricas, enlaces accesibles, badges y empty state).
  2. Cumplimiento de invariantes de diseño y producto: Anti-12px (D16) en todos los textos con piso `text-sm`, purga total de estrellas y calificaciones según Supuesto S4, revelación progresiva D3 en modal C05 y estricto respeto de fronteras arquitectónicas (Regla 20).
- **Decisiones y por qué:**
  - `acceptOfferAction` se expone desde `@/features/offers` (`index.ts`) y `@/features/offers/server` (`server.ts`) para evitar imports profundos violando `boundaries/entry-point`.
  - La suscripción Realtime vive en el hook `useRequestOffers` con desuscripción en el desmontaje y actualización de estado en memoria al recibir `INSERT`/`UPDATE`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (32 suites en verde; 279 pruebas pasando en src) · test:db n.a. · `tools/verify-scaffold.test.ts` (14/14 pasando aislada; timeout de inicialización de ESLint en Windows en paralelo masivo).
- **Falta:** Revisión independiente con skill `revisar-pr` sobre el nuevo commit y actualización de descripción del PR.
- **Bloqueos:** ninguno
- **Próximo paso:** Subir commits a `origin/feat/T-113-requests-offers` y actualizar cuerpo del PR #76.
- **Último commit:** pendiente
