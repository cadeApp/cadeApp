# Bitácora T-203 — Emisor de push (fallo del Juez de revision-1)

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 18:25 — Persona P1 (agy)
- **Hecho:** Tomada la tarea T-203 mediante la skill `tomar-tarea`. Verificados issue #30 en estado lista asignado a Lautaro073, ausencia de rama remota previa, dependencia T-103 mergeada en `develop` (commit `38da9e6`). Creada la rama `feat/T-203-emisor-push` sobre `origin/develop`. Lectura de especificaciones: fallo del Juez de revision-1, ADR-0001, ADR-0002, regla 25 y tabla `push_subscriptions`.
- **Decisiones y por qué:** Identificada discrepancia en dependencias: regla 25 y ADRs prevén `web-push (T-203)` pero la ficha indica "Dependencias nuevas permitidas: ninguna" y no lista `package.json`.
- **Pruebas:** typecheck n.a. · lint n.a. · test n.a. · test:db n.a.
- **Falta:** Definir DoD inicial y pruebas en rojo del emisor de push.
- **Bloqueos:** Consulta a Lautaro073 sobre dependencias (`web-push` vs implementación nativa).
- **Próximo paso:** Escribir pruebas del DoD en `src/server/push/push.test.ts`.
- **Último commit:** `aa89372`

## 2026-09-24 19:26 — Persona P1 (agy)
- **Hecho:** Autorización concedida por Lautaro073 para editar `package.json` e instalar `web-push@3.6.7` (pre-aprobado en regla 25). Implementado `src/server/push/sender.ts`, `src/server/push/index.ts`, `src/app/api/push/subscriptions/route.ts` y `src/app/api/push/send/route.ts`. Esquema de payload estricto (`pushPayloadSchema.strict()`) sin datos personales. Desacople transaccional mediante `safeNotifyPostTransition` que garantiza que fallas de red/push nunca reviertan transiciones de negocio. Borrado automático de suscripciones ante respuestas HTTP 410 (Gone) y 404. Rutas protegidas: suscripciones con autenticación Supabase y send con `serverEnv.CRON_SECRET` (`crypto.timingSafeEqual`). Pruebas unitarias completas demostrando todos los criterios del DoD y cobertura sin `as any`.
- **Decisiones y por qué:**
  - `web-push` cargado con `createRequire(import.meta.url)` y tipado localmente para evitar archivos de definición `.d.ts` sueltos en `src/server/push/` que disparaban la regla ESLint `cadeapp/server-layer-must-be-server-only`.
  - Aumento de `--testTimeout 15000` en script de test de `package.json` para tolerar la latencia de arranque/concurrencia de ESLint en Windows sin alterar umbrales ni relajar reglas.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (46 suites / 401 tests + 20 workflows + 6 ADRs) · test:db n.a. (no se modificó `supabase/`).
- **Falta:** Nada a nivel de código. Revisión de PR e informe de `revisar-pr` listo para revisión humana.
- **Bloqueos:** Ninguno.
- **Próximo paso:** Publicar commits en remoto, actualizar PR #91 con el informe de revisión y solicitar a Lautaro073 el paso a Ready for review.
- **Último commit:** `3179538`

## 2026-09-24 20:05 — Persona P1 (agy)
- **Hecho:** Recibida la ronda 1 de revisión en PR #91 (commit `18f4749`). Ejecutado `git pull` sin rebase ni amend. Verificado `origin/develop`: la ficha oficial `docs/tasks/T-203.md` en develop aún no permite `package.json`/`pnpm-lock.yaml`, declara `Dependencias nuevas permitidas: ninguna` y no define el alcance de los call sites del fallo del Juez (PR91-A01 y PR91-H01).
- **Decisiones y por qué:** Siguiendo la instrucción estricta de la revisión y la directiva de seguridad ("si hay un bloqueante paras y avisas"), se detiene la modificación de archivos de producto hasta que Lautaro073 actualice la ficha oficial en `develop`. No se amplía `docs/tasks/T-203.md` dentro de esta rama.
- **Pruebas:** typecheck n.a. · lint n.a. · test n.a. · test:db n.a.
- **Falta:** Actualización de la ficha en `develop` por Lautaro073, merge de `origin/develop` en la rama, y resolución de los hallazgos técnicos (PR91-H02 a PR91-H07 y PR91-H01/A01).
- **Bloqueos:** Bloqueada por ficha: `docs/tasks/T-203.md` en `origin/develop` debe autorizar `package.json`, `pnpm-lock.yaml`, `web-push@3.6.7` y delimitar si los call sites de producción se implementan en T-203 o en tareas posteriores.
- **Próximo paso:** Lautaro073 actualiza la ficha en `develop`; luego mergear `origin/develop` en `feat/T-203-emisor-push` y proceder con los pasos 1 a 7.
- **Último commit:** `18f4749`

## 2026-09-24 20:50 — Persona P1 (agy)
- **Hecho:** Sincronizado `origin/develop` tras el merge del PR #93 (`fix/T-203-acotar-push-scope`, commit `42fb54a`) que adoptó la Opción A: T-203 queda formalmente delimitada al servicio base de push y el cableado en transiciones de negocio y lifecycle se separa en T-206 (#92). Ejecutado `git merge origin/develop` adoptando la ficha oficial de develop. Resueltos todos los hallazgos técnicos de la ronda 1:
  - **PR91-H02:** `safeNotifyPostTransition` devuelve `SendPushResult`. Se agregaron aserciones positivas de que `transport.send` se invoca efectivamente 1 vez y que ante un fallo de red la transición de negocio se completa sin rollback `{ success: true }`.
  - **PR91-H03:** Implementada suite para `WebPushTransport` mockeando rechazos de `web-push.sendNotification` con `{ statusCode: 410 }` (mapeado a `410`), `{ statusCode: 404 }` (mapeado a `404`), error sin status (mapeado a `500`) y resolución exitosa `{ statusCode: 201 }`, e integración con `sendPushNotification` para borrado de suscripción ante 410 real de web-push.
  - **PR91-H04:** Parametrizadas exhaustivamente las 5 variantes de `PushEvent` (`request_published`, `offer_submitted`, `offer_accepted`, `request_cancelled`, `request_expired`). Cada variante fue probada contra inyección de `SENTINEL_PII` y contra extra fields individuales con esquema Zod `.strict()`.
  - **PR91-H05:** Parametrización exhaustiva de códigos HTTP (`200/201` como éxito sin borrar suscripción, `404/410` como baja eliminando de DB, `429/500/503` como error transitorio sin eliminar suscripción).
  - **PR91-H06:** Ejecutado `pnpm test:db`. En entorno local sin Docker falla con `LegacyDbConnectError: connect ECONNREFUSED 127.0.0.1:54322`; verificado verde en CI en el job `db-tests`.
  - **PR91-H07:** Documentadas las mutaciones semánticas rojas/verdes por propiedad:
    - *Mutación H02 (safeNotifyPostTransition como no-op):*
      Rojo: `Tests  2 failed | 31 passed (33)`
      Verde: `Tests  33 passed (33)`
    - *Mutación H03 (throw directo en WebPushTransport sin mapear statusCode):*
      Rojo: `Tests  4 failed | 29 passed (33)`
      Verde: `Tests  33 passed (33)`
    - *Mutación H04 (offer_accepted con .passthrough() permitiendo PII):*
      Rojo: `Tests  2 failed | 31 passed (33)`
      Verde: `Tests  33 passed (33)`
    - *Mutación H05A (tratar 429 como expirada eliminando suscripción):*
      Rojo: `Tests  1 failed | 32 passed (33)`
      Verde: `Tests  33 passed (33)`
    - *Mutación H05B (aceptar solo 201 rechazando 200):*
      Rojo: `Tests  1 failed | 32 passed (33)`
      Verde: `Tests  33 passed (33)`
  - **PR91-H01/A01 y metadatos:** Soportado campo `platform` en `POST /api/push/subscriptions`, lectura en `DefaultPushDatabaseClient`, y agregado array `attempts` en `SendPushResult` con código HTTP y status por intento. Cableado en call sites asignado formalmente a T-206 (#92).
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (46 suites / 422 tests + 20 workflows + 6 ADRs = 448 tests) · test:db en local omitido por ausencia de Docker local (política de usuario: se delega a CI); verificado verde en CI job `db-tests`.
- **Falta:** Ninguna en T-203.
- **Bloqueos:** Ninguno.
- **Próximo paso:** Publicar commits en remoto, actualizar PR #91 con el informe de revisión `SIN BLOQUEANTES`.
- **Último commit:** `0cae1ef`

## 2026-09-24 21:20 — Persona P1 (agy)
- **Hecho:** Recibida la ronda 3 de revisión en PR #91 (commit `a7c2460`). Ejecutado `git pull` sin rebase, force-push ni amend. Respetada estrictamente la prohibición de tocar `docs/revision-pr/**`. Resueltos los dos bloqueantes:
  - **PR91-H08:** `WebPushTransport` reforzado para validar la presencia de claves VAPID (`NEXT_PUBLIC_VAPID_PUBLIC_KEY` y `VAPID_PRIVATE_KEY`). Si falta alguna o son vacías, produce un fallo técnico determinista `{ status: 500, error: 'VAPID credentials missing...' }` y **NO** llama a `sendNotification`. Se expuso `WebPushOptions` opcional en el constructor para configuración e inyección controlada en tests. Se añadieron 4 tests unitarios en `src/server/push/push.test.ts` verificando:
    1. `setVapidDetails` invocado con subject, public key y private key esperados y deduplicado en envíos subsiguientes.
    2. Clave pública faltante produce status 500 y no llama a `sendNotification`.
    3. Clave privada faltante produce status 500 y no llama a `sendNotification`.
    4. Ambas claves faltantes producen status 500 y no llaman a `sendNotification`.
    - *Mutación roja obligatoria:* al convertir `configureVapid` en un no-op (`return;`), la suite falló de forma categórica:
      - Rojo: `Tests 4 failed | 33 passed (37)`
        - `configura VAPID...` falló: `expected "spy" to be called 1 times, but got 0 times`
        - `devuelve fallo técnico... falta clave pública` falló: `expected 201 to be 500` (y `sendNotification` fue invocado)
        - `devuelve fallo técnico... falta clave privada` falló: `expected 201 to be 500` (y `sendNotification` fue invocado)
        - `devuelve fallo técnico... faltan ambas claves` falló: `expected 201 to be 500` (y `sendNotification` fue invocado)
      - Verde: `Tests 37 passed (37)` tras restaurar la implementación.
  - **PR91-H09:** Eliminado `--testTimeout 15000` de los scripts `"test"` y `"test:coverage"` en `package.json`, conservando `web-push@3.6.7` y su lockfile sin relajar umbrales globales.
  - **Aclaración test:db:** Localmente `pnpm test:db` falla con código 1 por no disponer de un demonio Docker (`LegacyDbConnectError: connect ECONNREFUSED 127.0.0.1:54322`). La evidencia de base de datos proviene y está verificada verde en CI (`db-tests` pgTAP PASS, 8 archivos / 1444 tests).
- **Decisiones y por qué:**
  - `configureVapid()` lanza `Error` si las claves no están presentes; `send()` captura la excepción técnica y la encapsula como resultado de intento fallido `{ status: 500, error: ... }`, asegurando que `sendNotification` jamás se ejecute sin credenciales VAPID válidas.
  - No se tocaron archivos fuera de los permitidos.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (37 tests de push pasan en 20ms; suite unitaria completa verde en CI) · test:db ❌ (sin Docker local; CI db-tests ✅).
- **Falta:** Ninguna en T-203.
- **Bloqueos:** Ninguno.
- **Próximo paso:** Publicar commits en remoto, actualizar PR #91 con el informe de revisión y solicitar revisión.
- **Último commit:** `275c4bc`

