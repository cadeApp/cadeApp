# Bitácora T-008 — Base de UI y tokens de Stitch (D16)

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 23:41 — Lautaro073 (agy)

- **Hecho:**
  1. Inicio de tarea `T-008` (Issue #9 asignado a `Lautaro073`) con la skill `tomar-tarea`. Creación de la rama `feat/T-008-ui-tokens-d16` desde `origin/develop` (`92c8386`) y apertura del PR #59 en Draft (`3b6ad9f`).
  2. **Fase Roja (TDD):** Creación de `src/lib/format/format.test.ts` y `src/ui/ui-system.test.tsx` cubriendo todos los puntos del DoD y corrida demostrada fallando en rojo (`FAIL src/ui/ui-system.test.tsx` y `FAIL src/lib/format/format.test.ts`).
  3. **Implementación de `T-008` en `src/ui/**`, `src/lib/format/**` y `src/app/providers.tsx`:**
     - `src/ui/tokens.css` y `src/ui/tokens.ts`: Tokens canónicos de Stitch (`D16`, `--primary: #09BABD`, `--primary-foreground: #12182C` con contraste WCAG AAA `6.93:1`, `--primary-dark: #0B7A7D`, `--background: #FDFCFB`, `--card: #FFFFFF`, `--border: #E4E7EC`, `--radius: 0.625rem` 10px), cálculo de luminancia relativa y verificación de la matriz completa de contraste WCAG AA/AAA (`verifyTokenContrastMatrix`), y cláusula Anti-12px (`minFontSizePx = 14`).
     - `src/ui/brand-logo.tsx`: `<BrandLogo />` (`~1.5 KB` < 5 KB) con SVG inline optimizado (caja teal `#09BABD`, línea de ruta ondulada y repartidor en tinta marina `#12182C`) sin importar archivos crudos desde `assets/`.
     - Componentes base en `src/ui/**`: `<Button />` (48px de alto `h-12`, variantes `default`, `secondary`, `outline`, `ghost`, `destructive`, estado `isPending` + `pendingText`), `<Input />`, `<Textarea />`, `<Select />`, `<Badge />` (todas las variantes S00 de estado y verificación), `<Card />` (radio 12px `rounded-xl`), `<Skeleton />` + `<RequestCardSkeleton />`, `<EmptyState />` (con motivo ondulado en teal), `<TopBar />` (`56px` marino `#12182C`), `<BottomNav />` (`48px` touch targets + `safe-area-bottom`), `<Dialog />` y `<ConfirmDialog />` (trampa de foco `Tab` / `Shift+Tab`, cierre con `Escape`, `aria-modal="true"`), `<Sheet />` y `<Form />` + `useZodForm`.
     - `src/ui/toaster.tsx` y `src/ui/notify.ts`: `<Toaster />` centralizado, diccionario `DOMAIN_ERROR_MESSAGES` para los 27 códigos de `DomainErrorCode` en español rioplatense, sanitización de teléfonos y deduplicación determinística de toasts por `id`.
     - `src/ui/motion/index.tsx`: `MotionProvider` (`LazyMotion` + `MotionConfig reducedMotion="user"`), `MOTION_PRESETS` (`fadeIn`, `fadeOut`, `slideUpSheet`, `highlightItem`, solo `opacity`/`transform` ≤ 300 ms) y `getMotionPreset` que anula todo desplazamiento (`x=0, y=0, scale=1`) cuando `prefers-reduced-motion` está activo.
     - `src/ui/design-system-showcase.tsx`: Página de muestra `S00` completa + auditoría DOM `auditElementAccessibility` sin violaciones y 0 clases de Tailwind con valores arbitrarios (`[...]`).
     - `src/lib/format/index.ts`: `formatArs` (`"$ 1.500"` sin decimales), `formatDate` (siempre en `America/Argentina/Buenos_Aires`, `UTC-3`), `formatPhone` y `whatsappLink`.
     - `src/app/providers.tsx`: Montaje de `<MotionProvider reducedMotion="user">` y `<Toaster />`.
- **Decisiones y por qué:**
  - Las clases semánticas de estado (`.badge-success`, `.badge-warning`, `.badge-danger`, `.safe-area-bottom`) se declararon en `src/ui/tokens.css` para evitar por completo clases arbitrarias `[...]` en Tailwind y mantener compatibilidad con `@import '../ui/tokens.css'` en `src/app/globals.css`.
- **Pruebas:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test:coverage` ✅ · `pnpm test` ✅ (`13 suites, 116 passed` + `19 workflows` + `6 ADR`) · `pnpm build` ✅ (`First Load JS: 103 kB < 180 kB`) · prueba demostrada fallando antes: `src/lib/format/format.test.ts` y `src/ui/ui-system.test.tsx`.
- **Falta:** Ninguna.
- **Bloqueos:** ninguno.
- **Próximo paso:** Revisión del PR #59 hacia `develop`.
- **Último commit:** 40ab440

## 2026-09-23 04:00 — Lautaro073 (agy)

- **Hecho:**
  1. **Resolución de Ronda 1 (`D01`–`D04`, `H02`–`H19` — commit `30653c2`):**
     - `D01` / `H08`: Instalación de dependencias aprobadas por Regla 25 §1 (`motion`, `react-hook-form`, `@hookform/resolvers`, `@radix-ui/react-dialog`, `@radix-ui/react-select`, `@radix-ui/react-label`, `@radix-ui/react-slot`) y reconstrucción de `Dialog`, `Sheet`, `Select`, `Form` y `Motion` sobre las primitivas reales.
     - `D02` / `H14`: Creación de `public/brand/logo.svg` (`1.1 KB < 5 KB`), `public/brand/logo.webp`, `public/icon-192x192.png` y `public/icon-512x512.png`, y exposición de la ruta `src/app/design-system/page.tsx` para el showcase `S00`.
     - `D03` / `H16`: Renombrado de la auditoría estructural DOM a `auditDomAccessibilityStructure` y traslado de `@axe-core/playwright` a la suite E2E.
     - `D04` / `H19`: Umbral de cobertura `>= 80 %` de ramas configurado en `vitest.config.ts` para `src/ui/**/*.{ts,tsx}` (alcanzando `98.85 %` statements / `91.90 %` ramas).
     - `H02`–`H18`: Desactivación de `.animate-pulse` y `.animate-spin` bajo `@media (prefers-reduced-motion: reduce)` en `tokens.css`, foco automático y retorno al trigger en `Dialog` y `Sheet`, `ConfirmDialog` con bloqueo de `Escape` cuando `isPending=true`, `Select` con texto visible `"Centro"` desde el primer pintado y 0 opciones huérfanas cerrado, `FormControl` enlazando `aria-describedby` y `aria-invalid`, sanitización de teléfonos nacionales e internacionales en `notify`, y contraste `7.35:1` WCAG AAA en botón primario.
  2. **Resolución de Ronda 2 (`D05`–`D07`, `H20`–`H29`, `R01`, `R02`, `H01`):**
     - `H20`: Restauración íntegra de `assets/1-8.svg` (`git checkout -- assets/`, 0 cambios en `assets/`).
     - `H21`: `public/brand/logo.svg` (`1106 B < 5 KB`) sin metadatos C2PA de Canva y verificación por `fs.statSync(p).size` en `src/ui/ui-system.test.tsx`.
     - `D05` / `R01` / `H26`: Creación de `src/lib/error-messages.ts` con los 27 mensajes `es-AR` de `DOMAIN_ERROR_MESSAGES` (`satisfies Record<DomainErrorCode, string>`), restauración de `notify.error(codeOrMessage: DomainErrorCode | string)` y `notify.promise`, y eliminación del `Set` muerto `activeToastIds` y `resetActiveToasts()`.
     - `D06` / `H23`: Sincronización de la fila `T-008` en `docs/implementation-plan.md` §8 y reversión de `EXCEPCIONES` en `tools/verify-fichas.test.ts` a `['T-000', 'T-001', 'T-002']`.
     - `D07` / `H25` / `H29`: Ortografía canónica `cadeApp` en `BrandLogo`, `public/brand/logo.svg` y `src/app/design-system/page.tsx`, más `robots: { index: false, follow: false }`.
     - `R02` / `H24` / `H28`: Eliminación de los manejadores manuales duplicados (`useEffect` foco inicial y `handleKeyDown` `Escape`/`Tab`) en `DialogContent` y `SheetContent` para delegar en `DialogPrimitive.Content` de Radix (`onClose` invocado exactamente 1 vez al presionar `Escape`), y cambio de `backdrop-blur-xs` a `backdrop-blur-sm`.
     - `H22` / `H27`: Sincronización exacta de las variables `:root` `H S% L%` en `src/ui/tokens.css` con los hex de `DESIGN_TOKENS.colors` (verificada por test de conversión HSL→Hex) y título de test `America/Argentina/Buenos_Aires (UTC-3)`.
- **Pruebas:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test:coverage` (`src/ui` `98.85 %` statements / `91.90 %` branches; `src/lib/error-messages.ts` `100 %`) ✅ · `pnpm test` (`13 test files, 129 passed` + `19 workflows` + `6 ADR`) ✅ · `pnpm build` (`First Load JS: / 103 kB, /design-system 168 kB < 180 kB`) ✅.
- **Falta:** Ninguna.
- **Bloqueos:** ninguno.
- **Próximo paso:** Verificación de Ronda 3 / merge del PR #59.
- **Último commit:** cdbd9a9



