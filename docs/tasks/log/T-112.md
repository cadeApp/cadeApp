# Bitácora T-112 — Crear solicitud (barrios, direcciones, pin de entrega opcional en mapa)

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-23 18:15 — P2 (agy)

- **Hecho:**
  1. Toma de tarea T-112 con la skill `tomar-tarea`. Verificación de dependencias satisfechas: `T-009` mergeada en `origin/develop` (`0e8c235`) y contratos de RPC en `src/domain/rpc-contracts.ts` / `src/domain/testing/rpc-fake.ts`. Creación de rama `feat/T-112-create-request` desde `origin/develop`.
  2. Redacción previa de pruebas del DoD según TDD (Regla 40): validación estricta con Zod en `src/features/requests/schemas.test.ts` (coordenadas en bounding box de Aguilares, obligatoriedad de pares de coordenadas o fallback nulo, consentimiento del destinatario, validación de chips de efectivo `cash_change_amount` cuando `needsChange: true`, tipos de paquetes en enum `['sobre', 'chico', 'mediano', 'grande']`), y pruebas unitarias de `createDeliveryRequestAction` en `src/features/requests/actions.test.ts` (rechazo de sesión no autenticada, rol no comerciante, bloqueo por falta de piloto o suscripción activa `canMerchantPublishRequest`, inserción en `delivery_requests` y `delivery_request_contacts`, cálculo server-side de distancia con Haversine y fallback a centroides de `zones`).
  3. Demostración de falla en rojo (fase Red de TDD): `vitest run` ejecutado; fallan exactamente `actions.test.ts` y `schemas.test.ts` por falta de implementación de los módulos.
- **Decisiones y por qué:**
  - Alcance estrictamente limitado a los archivos permitidos por la ficha: `src/features/requests/**`, `src/app/(merchant)/requests/new/**`, `docs/tasks/T-112.md`, `docs/tasks/log/T-112.md`, `docs/revision-pr/**`.
  - Reutilización de contratos existentes en `src/domain/schemas`: `AGUILARES_BOUNDS`, `aguilaresLatSchema`, `aguilaresLngSchema`, `isWithinAguilaresBounds`, `calculateHaversineRouteDistanceM`, y `canMerchantPublishRequest` de `src/domain/states`.
- **Pruebas:** typecheck ❌ (esperado en rojo: módulos aún no implementados) · lint ❌ · test ❌ (`src/features/requests/actions.test.ts` y `src/features/requests/schemas.test.ts` fallan por falta de implementación previa).
- **Falta:** Implementar `src/features/requests/schemas.ts`, `actions.ts`, `queries.ts`, `copy.ts`, `components/create-request-form.tsx`, `src/app/(merchant)/requests/new/page.tsx`, y pasar las pruebas a verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `src/features/requests/schemas.ts` y `src/features/requests/actions.ts` y verificar que los tests pasen a verde.
- **Último commit:** chore(T-112): start task [T-112]
