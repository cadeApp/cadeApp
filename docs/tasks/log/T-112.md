# Bitácora T-112 — Crear solicitud (barrios, direcciones, pin de entrega opcional en mapa)

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-23 18:15 — P2 (agy)

- **Hecho:**
  1. Toma de tarea T-112 con la skill `tomar-tarea`. Verificación de dependencias satisfechas: `T-009` mergeada en `origin/develop` (`0e8c235`) y contratos de RPC en `src/domain/rpc-contracts.ts` / `src/domain/testing/rpc-fake.ts`. Creación de rama `feat/T-112-create-request` desde `origin/develop`.
  2. Redacción previa de pruebas del DoD según TDD (Regla 40): validación estricta con Zod en `src/features/requests/schemas.test.ts` (coordenadas en bounding box de Aguilares, obligatoriedad de pares de coordenadas o fallback nulo, consentimiento del destinatario, validación de chips de efectivo `cash_change_amount` cuando `needsChange: true`, tipos de paquetes en enum `['sobre', 'chico', 'mediano', 'grande']`), y pruebas unitarias de `createDeliveryRequestAction` en `src/features/requests/actions.test.ts` (rechazo de sesión no autenticada, rol no comerciante, bloqueo por falta de piloto o suscripción activa `canMerchantPublishRequest`, inserción en `delivery_requests` y `delivery_request_contacts`, cálculo server-side de distancia con Haversine y fallback a centroides de `zones`).
  3. Demostración de falla en rojo (fase Red de TDD): `vitest run` ejecutado; fallan exactamente `actions.test.ts` y `schemas.test.ts` por falta de implementación de los módulos.
- **Decisiones y por qué:**
  - Alcance estrictamente limitado a los archivos permitidos por la ficha: `src/features/requests/**`, `src/app/(merchant)/requests/new/**`, `docs/tasks/T-112.md`, `docs/tasks/log/T-112.md`, `docs/revision-pr/**`.
  - Reutilización de contratos existentes en `src/domain/schemas`: `AGUILARES_BOUNDS`, `aguilaresLatSchema`, `aguilaresLngSchema`, `isWithinAguilaresBounds`, `calculateHaversineRouteDistanceM`, y `canMerchantPublishRequest` de `src/domain/states`.
- **Pruebas:** typecheck ❌ (esperado en rojo: módulos aún no implementados) · lint ❌ · test ❌ (`src/features/requests/actions.test.ts` y `src/features/requests/schemas.test.ts` fallan por falta de implementación previa).
- **Falta:** Implementar `src/features/requests/schemas.ts`, `actions.ts`, `queries.ts`, `copy.ts`, `components/create-request-form.tsx`, `src/app/(merchant)/requests/new/page.tsx`, y pasar las pruebas a verde.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `src/features/requests/schemas.ts` y `src/features/requests/actions.ts` y verificar que los tests pasen a verde.
- **Último commit:** 0a144bc

## 2026-09-23 18:25 — P2 (agy)

- **Hecho:**
  1. Implementación de esquemas Zod en `src/features/requests/schemas.ts`: validación de direcciones de retiro y entrega, barrios, coordenadas geográficas opcionales validadas dentro del bounding box de Aguilares (`aguilaresLatSchema`, `aguilaresLngSchema`, pares consistentes), datos del destinatario con declaración de consentimiento obligatoria (`recipientConsentDeclared: z.literal(true)`), tipo de paquete tipificado en `['sobre', 'chico', 'mediano', 'grande']`, medio de pago del destinatario en `['cash', 'transfer', 'to_agree']`, y validación condicional de cambio en efectivo con `cashChangeAmount` positivo cuando `needsChange: true`.
  2. Implementación de Server Action `createDeliveryRequestAction` en `src/features/requests/actions.ts`:
     - Verificación de sesión de autenticación y rol `merchant`.
     - Verificación estricta de suscripción activa o período piloto mediante `canMerchantPublishRequest` (`subscription_status`, `pilot_active`, `paid_until`, `subscription_grace_days`).
     - Cálculo server-side de distancia invocando fórmula esférica Haversine × 1.30 redondeada a múltiplos de 500 m; fallback automático a centroides de `zones` si no se proporcionan coordenadas exactas.
     - Inserción en `delivery_requests` con estado `draft`, `route_distance_m`, `approx_distance_m`, y guardado de `cash_change_amount`.
     - Inserción en `delivery_request_contacts` bajo RLS estricta (direcciones, coordenadas opcionales, destinatario y declaración).
  3. Implementación de consultas server-only `getActiveZones` y `getMerchantDefaultPickup` en `src/features/requests/queries.ts` con cobertura de pruebas en `src/features/requests/queries.test.ts`.
  4. Implementación del componente UI `CreateRequestForm` en `src/features/requests/components/create-request-form.tsx` con soporte Stitch/shadcn/ui: punto de retiro precargado editable, botón de geolocalización en Aguilares, selección de paquete, medio de pago, selector de cambio en efectivo con chips rápidos "Paga con: $ 2.000 / $ 5.000 / $ 10.000", declaración de consentimiento y accesibilidad WCAG 2.2 AA (mínimo 48px touch targets, sin clases `text-xs`).
  5. Implementación de `CreateRequestSkeleton` en `src/features/requests/components/create-request-skeleton.tsx`.
  6. Implementación de página Server Component en `src/app/(merchant)/requests/new/page.tsx` y segmento `loading.tsx`.
  7. Exportaciones barrel en `src/features/requests/index.ts` y `server.ts`.
  8. Verificación de calidad completa:
     - `pnpm typecheck` ✅ (0 errores de TypeScript).
     - `pnpm lint` ✅ (0 warnings/errores).
     - `pnpm test` ✅ (25 suites pasadas, 227 tests unitarios + workflow + ADRs).
     - `pnpm build` ✅ (`/requests/new` First Load JS: 142 kB, dentro del presupuesto de 180 kB).
- **Decisiones y por qué:**
  - Tipado de Supabase: se empleó `AppSupabaseClient` y `ZoneRow` para consultas con `select` explícito evitando tipos `never` y preservando inferencia estricta sin recurrir a `any` ni `@ts-ignore`.
  - Fallback de cálculo de distancia: si el usuario no fija pines de coordenadas, se consultan los centroides de las zonas (`zones.centroid_lat`, `zones.centroid_lng`) y se calcula la distancia Haversine urbana entre ellos, guardando el resultado en `route_distance_m` y `approx_distance_m`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (25 suites, 227 tests) · build ✅ (`/requests/new` 142 kB First Load JS).
- **Falta:** Correr skill `revisar-pr` sobre PR #67, publicar informe y solicitar review.
- **Bloqueos:** ninguno
- **Próximo paso:** Ejecutar `revisar-pr` sobre PR #67 y actualizar la descripción del PR.
- **Último commit:** 7f10c87

## 2026-09-23 19:30 — P2 (agy)

- **Hecho:**
  1. Resolución de hallazgos de revisión Ronda 1 (PR #67):
     - **PR67-H01 (BLOQUEANTE):** Se reemplazó el token inexistente `text-success` en `src/features/requests/components/create-request-form.tsx:304,377` por el token válido del sistema de diseño `text-primary`. Se añadió prueba de regresión unitaria en `src/features/requests/components/create-request-form.test.tsx`.
       - _Evidencia Rojo:_ `AssertionError: expected 'text-success flex items-center gap-1.5 text-xs font-semibold' to contain 'text-primary'`.
       - _Evidencia Verde:_ 5 pruebas pasadas en `create-request-form.test.tsx` (356 ms).
       - _Verificación específica:_ `git grep text-success src/features/requests/components/create-request-form.tsx` → 0 resultados.
     - **PR67-H02 (MEJORA):** Se migró el formulario de 18 `React.useState` a `useForm` de `react-hook-form` con `zodResolver(createDeliveryRequestSchema)` (`@hookform/resolvers/zod`), cumpliendo la convención de AGENTS.md §3. Se integraron `register`, `handleSubmit`, `setValue` para campos controlados, y mensajes de error inline accesibles con `role="alert"`.
     - **PR67-H03 (MEJORA):** Se eliminó el cast `as unknown as AppSupabaseClient` y el alias de tipo en `src/features/requests/actions.ts` y `queries.ts`, consumiendo el cliente de `createClient()` de forma directa con tipos de fila explícitos (`select<string, ZoneCentroidRow>`) e inserciones con `as never` alineadas a `merchants/actions.ts`.
       - _Verificación específica:_ `git grep "as unknown as AppSupabaseClient" src/features/requests/` → 0 resultados.
     - **PR67-H04 (MEJORA):** Se formatearon `docs/tasks/T-112.md` y `docs/tasks/log/T-112.md` con Prettier.
       - _Verificación específica:_ `pnpm prettier --check docs/tasks/T-112.md docs/tasks/log/T-112.md` → All matched files use Prettier code style!
     - **PR67-H05 (MEJORA):** Evaluación técnica con El Consejo sobre `src/ui/select.tsx` vs `<select>` nativo estilizado. Se determinó conservar `<select>` nativo estilizado integrado con RHF (`register('pickupZoneId')`, `register('dropoffZoneId')`) debido a que: 1) `src/ui/select.tsx:90` contiene un bug de singleton en `document.querySelector('[data-cade-select-root="true"]')` que falla al haber múltiples Selects en pantalla; 2) `src/ui/select.tsx:237` posee el token inexistente `text-success`; 3) no se pueden alterar archivos fuera de la ficha sin un contract-change; 4) en PWA móvil, los select nativos ofrecen una experiencia accesible y ergonómica óptima.
  2. Verificación de calidad completa:
     - `pnpm typecheck` ✅ (0 errores de TypeScript).
     - `pnpm lint` ✅ (0 warnings/errores).
     - `pnpm test` ✅ (25 suites pasadas, 228 pruebas unitarias + workflow + ADRs).
     - `pnpm build` ✅ (`/requests/new` First Load JS: 156 kB, dentro del presupuesto de 180 kB).
- **Decisiones y por qué:**
  - Se mantiene `<select>` nativo estilizado por razones técnicas objetivas (aislamiento de bugs de `src/ui/select.tsx`, cumplimiento de frontera de archivos de T-112 y mejor UX táctil en móviles).
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (25 suites, 228 tests) · build ✅ (`/requests/new` 156 kB First Load JS).
- **Falta:** Correr skill `revisar-pr` sobre PR #67 y actualizar la descripción del PR.
- **Bloqueos:** ninguno
- **Próximo paso:** Ejecutar `revisar-pr` sobre PR #67 y actualizar el PR con el informe de Ronda 2.
- **Último commit:** c014a95
