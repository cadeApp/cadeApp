# Bitácora T-204 — Datos en vivo con TanStack Query

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-24 04:05 — P2 (asako669) (agy)
- **Hecho:** Inicio de tarea T-204 con skill `tomar-tarea`. Verificación de dependencias T-113 y T-114 mergeadas en `origin/develop`. Creación de rama `feat/T-204-realtime-tanstack`. Ficha oficial `docs/tasks/T-204.md` sincronizada con `docs/implementation-plan.md` §8 e Issue #31. Creación de tests del DoD mostrando su fallo inicial (TDD fase roja). Apertura de PR #82 en Draft.
- **Decisiones y por qué:** TanStack Query se utiliza para gestionar el estado del servidor con `refetchOnWindowFocus` y `refetchOnReconnect`. Supabase Realtime se limita a invalidar queries vía canal por pantalla con debounce, sin mutar manualmente la caché en cliente. Polling de 30s activo solo con ventana en foco.
- **Pruebas:** typecheck ❌ · lint ❌ · test ❌ · test:db n.a. · Fallo demostrado: tests de `useRealtimeInvalidation`, `useRequestOffers`, `useAvailableRequests` y `useTrip` fallan por inexistencia/desalineación de implementación TDD (4 suites fallando en fase roja).
- **Falta:** Implementación de query keys, `useRealtimeInvalidation` y hooks reactivos en requests, offers y trips.
- **Bloqueos:** ninguno
- **Próximo paso:** Implementar `query-keys.ts` e invalidación en tiempo real.
- **Último commit:** 75c50da (chore(T-204): start task [T-204])

## 2026-09-25 20:20 — P2 (asako669) (agy)
- **Hecho:**
  - Pull de commit `242b55c` (Ronda 2 de revisión de PR #82) y merge de `origin/develop` (`ac4587f`).
  - PR82-H14: Ampliación de la ficha `docs/tasks/T-204.md` con decisión D01 exclusivamente para `src/features/offers/components/courier-feed.tsx` y `src/features/offers/courier-panel.test.tsx`.
  - PR82-H09: `CourierFeed` conectado al hook vivo `useAvailableRequests` con initialData; `useAvailableRequests` implementa consulta viva a Supabase (`delivery_requests` publicados).
  - PR82-H10: Eliminado el refetch no-op en `useTrip` (D02); `useTrip` ahora consulta `delivery_requests` y devuelve null ante error, nunca `initialTrip`.
  - PR82-H11: Implementado acumulador multi-key en `useRealtimeInvalidation` (`pendingKeysRef`); ante ráfagas multi-key invalida todas las queryKeys distintas una vez por clave.
  - PR82-H12: Reconfiguración reactiva de suscripciones en `useRealtimeInvalidation` mediante `serializedConfig` en las dependencias de `useEffect`.
  - PR82-H07: Comprobación de polling de 30 s activo solo en primer plano y detenido en background con fake timers.
  - PR82-H13: Notificaciones `onOfferAdded` y `onOfferUpdated` extraídas del render a `useEffect` posterior al render sin side-effects síncronos ni duplicados.
- **Evidencia Rojo / Verde (PR82-H15):**
  - PR82-H09 (CourierFeed hook vivo):
    - Rojo: `FAIL src/features/offers/courier-panel.test.tsx > T-114 DoD: Courier panel UI, privacidad y reglas de negocio > PR82-H09: CourierFeed consume useAvailableRequests y ante foco/invalidación muestra solicitudes vivas` -> `TestingLibraryElementError: Unable to find an element with the text: /Barrio Sur/i`
    - Verde: `✓ src/features/offers/courier-panel.test.tsx (8 tests) 252ms`
  - PR82-H10 (useTrip sin no-op):
    - Rojo: `FAIL src/features/trips/hooks/use-trip.test.tsx > T-204 DoD: useTrip (Active Trip TanStack Query & Realtime) > PR82-H10: refetch no es un no-op y consulta la fuente viva de datos en vez de devolver initialTrip` -> `AssertionError: expected 'matched' to be 'in_transit'`
    - Verde: `✓ src/features/trips/hooks/use-trip.test.tsx (5 tests) 252ms`
  - PR82-H11 (debounce multi-key):
    - Rojo: `FAIL src/lib/hooks/use-realtime-invalidation.test.tsx > T-204 DoD: useRealtimeInvalidation > PR82-H11: debounce multi-key acumula queryKeys distintas e invalida todas al vencer la ventana` -> `AssertionError: expected "invalidateQueries" to be called 2 times, but got 1 times`
    - Verde: `✓ src/lib/hooks/use-realtime-invalidation.test.tsx (5 tests) 25ms`
  - PR82-H12 (reconfiguración suscripción):
    - Rojo: `FAIL src/lib/hooks/use-realtime-invalidation.test.tsx > T-204 DoD: useRealtimeInvalidation > PR82-H12: reconfigura la suscripción cuando cambian filtro/key con el mismo channelName` -> `AssertionError: expected last "spy" call to have been called with [ 'postgres_changes', ObjectContaining { "filter": "request_id=eq.req-B" } ]`
    - Verde: `✓ src/lib/hooks/use-realtime-invalidation.test.tsx (5 tests) 25ms`
  - PR82-H07 (polling 30s background disabled):
    - Rojo: `FAIL src/features/offers/hooks/use-available-requests.test.tsx > T-204 DoD: useAvailableRequests (Courier Feed TanStack Query & Realtime) > PR82-H07: polling periódico de 30 s activo solo en primer plano y detenido en background` -> `AssertionError: expected "spy" to be called 2 times, but got 3 times`
    - Verde: `✓ src/features/offers/hooks/use-available-requests.test.tsx (5 tests) 180ms`
  - PR82-H13 (callbacks en useEffect):
    - Rojo: `FAIL src/features/requests/hooks/use-request-offers.test.tsx > T-204 DoD: useRequestOffers con TanStack Query y Realtime > PR82-H13: onOfferAdded y onOfferUpdated se ejecutan en useEffect posterior al render (no durante el render)` -> `AssertionError: expected [ 'start-render', 'callback', 'end-render', 'callback' ] to deeply equal [ 'start-render', 'end-render', 'callback' ]`
    - Verde: `✓ src/features/requests/hooks/use-request-offers.test.tsx (5 tests) 234ms`
- **Pruebas:**
  - `pnpm typecheck`: ✅ (exit code 0)
  - `pnpm lint`: ✅ `✔ No ESLint warnings or errors` (0 warnings, 0 errors)
  - `pnpm test`: ✅ 60/60 suites pasadas, 641/641 tests pasados; workflows 21/21; ADRs 6/6.
  - `tools/verify-fichas.test.ts`: ✅ 6/6 tests pasados.
- **Falta:** revisión independiente de Ronda 3 por Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Pedir a Lautaro073 la revisión independiente de Ronda 3 en PR #82.
- **Último commit:** 45d3d5d (feat(realtime): resolve PR 82 round 2 findings and wire live feed [T-204])

## 2026-09-26 06:15 — P2 (asako669) (agy)
- **Hecho:**
  - Resolución de los 3 bloqueantes pendientes de la Ronda 3 de revisión de PR #82 (`docs/revision-pr/pr-82/revisiones/ronda-3.md`).
  - PR82-H09 (Parcial): En `useAvailableRequests`, se implementó la consulta a Supabase browser para obtener las ofertas pendientes del usuario autenticado actual (`supabase.auth.getUser()`) en las solicitudes recuperadas (`supabase.from('offers').select('request_id, amount_ars').eq('courier_id', user.id).in('request_id', requestIds).eq('status', 'pending')`), mapeando `hasMyOffer: true` y `myOfferAmountArs` con el monto correspondiente (idéntico a la implementación canónica de servidor en `src/features/offers/queries.ts`). Se eliminó además el fallback silencioso que retornaba `[...initialRequests]` ante fallos de conexión o cliente nulo, retornando lista vacía `[]`.
  - PR82-H10 (Parcial): En `useTrip`, al resolver la consulta viva a `delivery_requests`, se mezcla el objeto `initialTrip` existente con los datos actualizados (`id` y `status`) preservando la forma completa del tipo `T` (`pickupAddress`, `dropoffAddress`, `recipientName`, `recipientPhone`, etc.) en lugar de sustituirlo por un objeto parcial `{ id, status }`. Asimismo, se corrigió el valor de retorno público del hook para usar `query.data !== undefined ? query.data : (initialTrip ?? null)`, impidiendo que cuando la consulta viva resuelve a `null` se reviva erróneamente el snapshot de `initialTrip`.
  - PR82-H18: Eliminadas la totalidad de las 8 ocurrencias de aserción no nula (`!`) en código y pruebas de la tarea, cumpliendo estrictamente con la regla raíz de AGENTS.md §4 (`Prohibido: any, @ts-ignore, ! non-null`). En los hooks `useAvailableRequests`, `useTrip`, `useRealtimeInvalidation` y `useRequestOffers`, `fallbackClient` se inicializa incondicionalmente mediante `useState(() => new QueryClient())` permitiendo un coalescing seguro `contextClient ?? fallbackClient` sin operador `!`. En `use-realtime-invalidation.test.tsx`, se reemplazaron los operadores `!` por aserciones de existencia y optional chaining (`cb?.(...)`).
- **Evidencia Rojo / Verde (PR82-H15):**
  - PR82-H09 (hasMyOffer y sin fallback silencioso):
    - Rojo: `FAIL src/features/offers/hooks/use-available-requests.test.tsx > T-204 DoD: useAvailableRequests (Courier Feed TanStack Query & Realtime) > PR82-H09: el fetch por defecto consulta ofertas pendientes del courier y mapea hasMyOffer y myOfferAmountArs` -> `AssertionError: expected false to be true // Object.is equality`
    - Verde: `✓ src/features/offers/hooks/use-available-requests.test.tsx (7 tests) 351ms`
  - PR82-H10 (preservación de shape de T y no revivir initialTrip en null):
    - Rojo:
      - `FAIL src/features/trips/hooks/use-trip.test.tsx > T-204 DoD: useTrip (Active Trip TanStack Query & Realtime) > PR82-H10: preserva el shape completo de T al actualizar el status sin recortar campos de initialTrip` -> `AssertionError: expected undefined to be 'San Martín 123' // Object.is equality`
      - `FAIL src/features/trips/hooks/use-trip.test.tsx > T-204 DoD: useTrip (Active Trip TanStack Query & Realtime) > PR82-H10: no revive initialTrip cuando la consulta viva resuelve a null` -> `AssertionError: expected { id: 'trip-1', …(5) } to be null`
    - Verde: `✓ src/features/trips/hooks/use-trip.test.tsx (7 tests) 405ms`
  - PR82-H18 (eliminación de ! non-null assertions):
    - Barrido de código previo: 8 ocurrencias de `!` en hooks y tests (`use-available-requests.ts:54`, `use-trip.ts:40`, `use-realtime-invalidation.ts:40`, `use-request-offers.ts:97`, `use-realtime-invalidation.test.tsx:104,171,172,245`).
    - Barrido de código posterior: 0 ocurrencias de `!` non-null assertion en todos los archivos de la tarea.
- **Pruebas:**
  - `pnpm typecheck`: ✅ (exit code 0, 0 errores)
  - `pnpm lint`: ✅ `✔ No ESLint warnings or errors` (0 warnings, 0 errors)
  - `tools/verify-fichas.test.ts`: ✅ 6/6 tests pasados.
  - `vitest run --fileParallelism=false --testTimeout=15000`: ✅ 56/56 suites pasadas, 586/586 tests pasados.
  - `node --test .github/workflows/verify-workflows.test.mjs docs/adr/verify-adr.test.mjs`: ✅ 27/27 tests pasados.
- **Falta:** revisión de Ronda 4 por Lautaro073.
- **Bloqueos:** ninguno
- **Próximo paso:** Solicitar a Lautaro073 la revisión de Ronda 4 en PR #82.
- **Último commit:** bfdddc7 (fix(realtime): resolve PR 82 round 3 findings (hasMyOffer, trip shape, remove non-null assertions) [T-204])

## 2026-09-26 12:56 — Sesión Ronda 4 (Resolución D03 / 1-A: PR82-H19, PR82-H20, PR82-H21)
- **Persona:** Lautaro073 / agente
- **Qué se hizo:**
  - Sincronización obligatoria (Paso 0): merge de `origin/develop` (commit `305f8df`), incorporando CC-008 (`getTripDetailsServer`).
  - Actualización de la ficha `docs/tasks/T-204.md` con la decisión arquitectónica D03 / 1-A y la lista explícita de archivos permitidos.
  - Creación de contratos client-safe con Zod en `src/lib/live-contracts.ts` y su suite `src/lib/live-contracts.test.ts` (8/8 tests).
  - Creación de la frontera de servidor única en `src/server/live/t204.ts` (`import 'server-only'`) y su suite `src/server/live/t204.test.ts` (14/14 tests):
    - `getAvailableRequestsLiveServer`: autenticación obligatoria, proyección restringida sin datos privados ni coordenadas, computación de `hasMyOffer` y `myOfferAmountArs` consultando ofertas pendientes del courier autenticado.
    - `getRequestOffersLiveServer`: autenticación obligatoria, validación de pertenencia al comercio autenticado (`merchant_id = user.id`), retorno de ofertas ordenadas.
    - `getTripLiveStateServer`: consumo canónico de `getTripDetailsServer` (CC-008), descarte de toda PII, retorno exclusivo de `{ id, status }`.
  - Creación de los endpoints HTTP dinámicos en `src/app/api/live/available-requests/route.ts`, `src/app/api/live/requests/[requestId]/offers/route.ts` y `src/app/api/live/trips/[tripId]/route.ts` con sus pruebas unitarias (7/7 tests).
  - Refactorización de hooks cliente (`useAvailableRequests`, `useRequestOffers`, `useTrip`):
    - Cero consultas directas a Supabase (`createClient().from(...)`).
    - Consumo de `/api/live/*` vía same-origin `fetch` con validación Zod y `retry: false`.
    - Supabase Realtime exclusivamente para invalidar queries con debounce mediante `useRealtimeInvalidation`.
  - Manejo accesible de errores en UI (`CourierFeed` y `RequestOffersList`):
    - Mensajes y banners con `role="alert"` y botón de reintento (`refetch()`).
    - Preservación de solicitudes y ofertas previas en caso de error 500, evitando falsos estados vacíos.
  - Cláusula Anti-12px respetada (piso tipográfico 14px, `text-sm`, 0 `text-xs`) en todos los componentes.
  - Barrido de non-null assertions: exactamente 0 `!` en los 22 archivos tocados.
- **Decisiones tomadas y por qué:**
  - D03 / 1-A (Lautaro073): las lecturas y refetch de datos vivos se ejecutan en el servidor a través de API routes dedicadas para evitar exponer lógica sensible, validar autorización y no depender de RLS de cliente. Realtime cliente conserva su rol exclusivo de señal de invalidación con debounce.
- **Evidencia Rojo / Verde (Mutaciones obligatorias):**
  - Mutación H19 (reintroducción de `createClient` browser en `use-available-requests.ts`):
    - Rojo: `FAIL src/features/offers/hooks/use-available-requests.test.tsx > PR82-H19 (Control Estático): use-available-requests.ts no tiene imports de supabase/browser ni .from(` -> `AssertionError: expected sourceCode not to contain '@/lib/supabase/browser'`
    - Verde: `✓ src/features/offers/hooks/use-available-requests.test.tsx (8 tests) 331ms`
  - Mutación H20 (retornar `[]` en `!res.ok` sin lanzar error en `use-available-requests.ts`):
    - Rojo: `FAIL src/features/offers/hooks/use-available-requests.test.tsx > PR82-H20: cuando /api/live falla (HTTP 500), expone isError: true sin fallback silencioso` -> `AssertionError: expected false to be true`
    - Verde: `✓ src/features/offers/hooks/use-available-requests.test.tsx (8 tests) 336ms`
  - Mutación Offers (quitar chequeo `merchant_id !== user.id` en `src/server/live/t204.ts`):
    - Rojo: `FAIL src/server/live/t204.test.ts > 3.2 getRequestOffersLiveServer > retorna 404 si la solicitud no pertenece al merchant autenticado` -> `TypeError: supabase.from(...).select(...).eq(...).order is not a function`
    - Verde: `✓ src/server/live/t204.test.ts (14 tests) 9ms`
  - Mutación Trip (retornar `null` en `!res.ok` en lugar de lanzar error en `use-trip.ts`):
    - Rojo: `FAIL src/features/trips/hooks/use-trip.test.tsx > PR82-H20: cuando /api/live/trips/... responde con HTTP 500, expone isError: true` -> `AssertionError: expected false to be true`
    - Verde: `✓ src/features/trips/hooks/use-trip.test.tsx (9 tests) 472ms`
- **Pruebas:**
  - `pnpm typecheck`: ✅ (exit code 0, 0 errores)
  - `pnpm lint`: ✅ `✔ No ESLint warnings or errors` (0 warnings, 0 errors)
  - `tools/verify-fichas.test.ts`: ✅ 6/6 tests pasados.
  - `vitest run --fileParallelism=false --testTimeout=15000`: ✅ 64/64 suites pasadas, 669/669 tests pasados.
  - `node --test .github/workflows/verify-workflows.test.mjs docs/adr/verify-adr.test.mjs`: ✅ 27/27 tests pasados.
- **Falta:** Revisión de Ronda 5 por Lautaro073 en PR #82.
- **Bloqueos:** ninguno.
- **Próximo paso:** Presentar el trabajo completado para revisión en el PR #82.
- **Último commit:** 3467446 (fix(realtime): resolve PR 82 round 4 findings H19-H21 with server-only live endpoints [T-204])

## 2026-09-26 14:15 — Sesión Ronda 5 (Resolución PR82-H22 a H26 y Mejora H27)
- **Persona:** Lautaro073 / agente
- **Qué se hizo:**
  - Sincronización (Paso 0): verificación de rama y origin/develop (sin rebase, sin force-push, sin amend).
  - PR82-H22: Tipado canónico Next.js 15 en rutas dinámicas:
    - En `src/app/api/live/requests/[requestId]/offers/route.ts` y `src/app/api/live/trips/[tripId]/route.ts`: firma `{ params }: { params: Promise<{ ... }> }` con `await params`.
    - En sus suites de tests (`route.test.ts`): llamadas con `params: Promise.resolve({ ... })`.
  - PR82-H23: Desacoplamiento de tipado en `useTrip`:
    - Eliminado el genérico `<T extends TripDetailItem>` y el cast `as unknown as T` de `src/features/trips/hooks/use-trip.ts`.
    - Retorno directo de `parsed.data` (`LiveTripState`: `{ id, status }`) cuando no hay `initialTrip`.
    - Control estático y prueba unitaria explícita sin `initialTrip` agregados en `src/features/trips/hooks/use-trip.test.tsx`.
  - PR82-H24: Mock robusto de prueba de aislamiento de merchant:
    - Reescrito el test `retorna 404 si la solicitud no pertenece al merchant autenticado` en `src/server/live/t204.test.ts` mockeando selectivamente `delivery_requests` y `offers`.
    - Agregada la aserción de no-llamada `expect(mockFrom).not.toHaveBeenCalledWith('offers')`.
  - PR82-H25: Optimización de `getTripLiveStateServer` (evitar firma de avatar y lectura innecesaria):
    - Reemplazado `getTripDetailsServer` por `getTripDetailsRpc` desde `@/server/rpc/trips`, pasando el cliente de `createClient()`.
    - Mapeo explícito de `UNAUTHENTICATED` a `{ ok: false, error: 'UNAUTHENTICATED', status: 401 }`.
    - Pruebas unitarias y control estático en `src/server/live/t204.test.ts` validando la llamada a `getTripDetailsRpc` y la ausencia de `getTripDetailsServer`.
  - PR82-H26: Eliminación de residuos de manipulación manual de caché y `onRegisterRealtime`:
    - En `src/features/requests/hooks/use-request-offers.ts`: eliminados `localOffers`, `setLocalOffersState`, `setOffers` y toda invocación a `setQueryData`. La query TanStack Query gobierna el estado con invalidación por eventos Realtime y fallback a `initialOffers`.
    - En `src/features/requests/components/request-offers-list.tsx`: eliminados el prop `onRegisterRealtime` y su `useEffect` asociado.
    - En `src/features/requests/hooks/use-request-offers.test.tsx`: agregado control estático de ausencia de `setQueryData`, `setOffers` y `setLocalOffersState`.
  - PR82-H27: Desactivación de caché HTTP en lecturas vivas:
    - Agregado `{ cache: 'no-store' }` en todas las llamadas `fetch` de `useAvailableRequests`, `useRequestOffers` y `useTrip`.
    - Aserciones de `toHaveBeenCalledWith(url, { cache: 'no-store' })` en las tres suites de pruebas unitarias.
  - Verificación de calidad:
    - `0` non-null assertions en todos los archivos tocados.
    - `0` clases arbitrarias y respeto estricto del piso tipográfico 14px (`text-sm`).
    - `next build` exitoso (39/39 rutas compiladas, validación de tipos de rutas Next.js).
    - `pnpm typecheck` y `pnpm lint` limpios (0 warnings, 0 errores).
- **Decisiones tomadas y por qué:**
  - `getTripLiveStateServer` utiliza `getTripDetailsRpc` directamente con el cliente autenticado: el polling periódico del estado de viaje no necesita firmar URLs de avatar privado ni leer `courier_documents`, eliminando sobrecosto de I/O en cada consulta periódica.
  - Eliminación completa de `setOffers` y `setQueryData` en `useRequestOffers`: respeta el invariante de que Supabase Realtime únicamente invalida queries con debounce y TanStack Query maneja la revalidación.
- **Evidencia Rojo / Verde (Mutaciones obligatorias):**
  - Mutación H22 (reintroducción de `{ params: { requestId: string } }` objeto plano en `route.ts`):
    - Rojo: `pnpm build` falla: `.next/types/app/api/live/requests/[requestId]/offers/route.ts:49:7 Type error: Type '{ ... }' does not satisfy the constraint 'ParamCheck<RouteContext>'`.
    - Verde: `next build` exitoso con `{ params }: { params: Promise<{ ... }> }` y 39/39 rutas generadas.
  - Mutación H23 (reintroducción de `as unknown as T` en `use-trip.ts`):
    - Rojo: `FAIL src/features/trips/hooks/use-trip.test.tsx > PR82-H23 (Control Estático): use-trip.ts no hace type casting inseguro (as unknown as T)` -> `AssertionError: expected sourceCode not to contain 'as unknown as T'`
    - Verde: `✓ src/features/trips/hooks/use-trip.test.tsx (10 tests) 569ms`
  - Mutación H24 (cambio de guarda en `getRequestOffersLiveServer` a `if (!record)`):
    - Rojo: `FAIL src/server/live/t204.test.ts > 3.2 getRequestOffersLiveServer > retorna 404 si la solicitud no pertenece al merchant autenticado` -> `AssertionError: expected true to be false` (falla la aserción de `mockFrom.not.toHaveBeenCalledWith('offers')`, sin lanzar TypeErrors del mock).
    - Verde: `✓ src/server/live/t204.test.ts (17 tests) 16ms`
  - Mutación H25 (volver a llamar a `getTripDetailsServer` en `t204.ts`):
    - Rojo: `FAIL src/server/live/t204.test.ts > 3.3 getTripLiveStateServer > control estático: usa getTripDetailsRpc y no getTripDetailsServer` -> `AssertionError: expected sourceCode not to contain 'getTripDetailsServer'`
    - Verde: `✓ src/server/live/t204.test.ts (17 tests) 16ms`
  - Mutación H26 (reintroducción de `setQueryData` en `use-request-offers.ts`):
    - Rojo: `FAIL src/features/requests/hooks/use-request-offers.test.tsx > T-204 DoD: useRequestOffers con TanStack Query y Realtime vía /api/live > PR82-H26 (Control Estático): use-request-offers.ts no expone setOffers ni llama a setQueryData` -> `AssertionError: expected sourceCode not to contain 'setQueryData'`
    - Verde: `✓ src/features/requests/hooks/use-request-offers.test.tsx (9 tests) 429ms`
  - Mutación H27 (quitar `{ cache: 'no-store' }` en `use-available-requests.ts` y `use-request-offers.ts`):
    - Rojo (use-available-requests): `FAIL src/features/offers/hooks/use-available-requests.test.tsx > PR82-H09 / H19: el fetch consume /api/live/available-requests y mapea hasMyOffer y myOfferAmountArs` -> `AssertionError: expected "fetch" to be called with arguments: [ "/api/live/available-requests", { "cache": "no-store" } ]`
    - Rojo (use-request-offers): `FAIL src/features/requests/hooks/use-request-offers.test.tsx > PR82-H19: el fetch por defecto consume /api/live/requests/[requestId]/offers` -> `AssertionError: expected "fetch" to be called with arguments: [ "/api/live/requests/...", { "cache": "no-store" } ]`
    - Verde: ambos hooks pasando con `{ cache: 'no-store' }` verificado.
- **Pruebas:**
  - `pnpm typecheck`: ✅ (exit code 0, 0 errores)
  - `pnpm lint`: ✅ `✔ No ESLint warnings or errors` (0 warnings, 0 errors)
  - `pnpm build`: ✅ `Compiled successfully` (39/39 rutas estáticas y dinámicas)
  - `tools/verify-fichas.test.ts`: ✅ 6/6 tests pasados.
  - Suites T-204 (12 suites, 87 tests): ✅ 87/87 tests pasados.
  - `node --test .github/workflows/verify-workflows.test.mjs; node --test docs/adr/verify-adr.test.mjs`: ✅ 27/27 tests pasados.
- **Falta:** Aprobación final de Ronda 5 en PR #82.
- **Bloqueos:** ninguno.
- **Próximo paso:** Presentar el informe de Ronda 5 en PR #82.
- **Último commit:** 092edad (fix(realtime): resolve PR 82 round 5 findings H22-H27 [T-204])




