# Bitácora T-007 — ADR-0001 Supabase y ADR-0002 hosting y cron

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 11:47 — Lautaro073 (agy)
- **Hecho:**
  1. Inicio de tarea `T-007` con la skill `tomar-tarea`. Verificación de Issue #8 asignado a `Lautaro073` y dependencia `T-002` mergeada en `origin/develop` (`0c4f031`).
  2. Creación de la rama `feat/T-007-adr-0001-0002` desde `origin/develop`.
  3. **Fase Roja (TDD):** Creación de `docs/adr/verify-adr.test.mjs` dentro de `docs/adr/**` ("Archivos permitidos") validando las 5 condiciones estructurales y de contenido del DoD (`docs/adr/README.md`, `ADR-0001-supabase-baas.md`, `ADR-0002-hosting-and-cron.md`, etiquetado `[DATO]` / `[SUPUESTO]` en costos USD y tabla de revisión de las 3 personas). Ejecución de `node --test docs/adr/verify-adr.test.mjs` demostrando fallo 5/5 en rojo (`# tests 5, # pass 0, # fail 5`).
- **Decisiones y por qué:** La suite de verificación del DoD (`docs/adr/verify-adr.test.mjs`) se ubicó dentro de `docs/adr/**` con `node:test` para respetar estrictamente la lista de "Archivos permitidos" de `docs/tasks/T-007.md` sin tocar `tools/**` ni `src/**`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ · test:db n.a. · `node --test docs/adr/verify-adr.test.mjs` demostrada fallando 5/5 en rojo ❌ (`Falta docs/adr/README.md`, `Falta docs/adr/ADR-0001-supabase-baas.md`, `Falta docs/adr/ADR-0002-hosting-and-cron.md`).
- **Falta:** Redactar `docs/adr/README.md`, `docs/adr/ADR-0001-supabase-baas.md` y `docs/adr/ADR-0002-hosting-and-cron.md` y verificar el paso a verde.
- **Bloqueos:** ninguno.
- **Próximo paso:** Escribir los dos ADRs y el índice en `docs/adr/` y re-ejecutar `node --test docs/adr/verify-adr.test.mjs`.
- **Último commit:** 0c4f031 (base `origin/develop`)

---

## 2026-09-22 11:55 — Lautaro073 (agy)
- **Hecho:**
  1. Redacción de `docs/adr/README.md` con el índice de ADRs y la convención de trazabilidad financiera `[DATO]` vs `[SUPUESTO]`.
  2. Redacción de `docs/adr/ADR-0001-supabase-baas.md` y `docs/adr/ADR-0002-hosting-and-cron.md`.
  3. **Fase Verde (TDD):** `node --test docs/adr/verify-adr.test.mjs` en verde (5/5 tests pasando), `pnpm typecheck`, `pnpm lint` y `pnpm test` en verde.
- **Decisiones y por qué:** Se documentó tanto el escenario de validación técnica/piloto ($0 USD en BaaS + $0/$20 USD en Vercel) como el de escala comercial ($25 USD Supabase Pro + $100 USD PITR + $20/$60 USD Vercel Pro), marcando explícitamente cada cifra como `[DATO]` o `[SUPUESTO]`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (72/72 tests Vitest + 19/19 tests `verify-workflows.test.mjs` + 5/5 tests `verify-adr.test.mjs`) · test:db n.a. · prueba demostrada fallando antes en rojo: `node --test docs/adr/verify-adr.test.mjs` (5/5 fallos antes de crear los ADRs).
- **Falta:** Revisión del PR #57.
- **Bloqueos:** ninguno.
- **Próximo paso:** Atender hallazgos de Ronda 1 en PR #57.
- **Último commit:** 65f174f

---

## 2026-09-22 12:52 — Lautaro073 (agy)
- **Hecho:**
  1. Resolución integral de los hallazgos de Ronda 1 de PR #57 (`docs/revision-pr/pr-57/revisiones/ronda-1.md`) con dictamen unánime de El Consejo (`consejo-backend` y `consejo-personas`):
     - **Barrido inicial (`H03`):** Ejecución del comando de cruce de identificadores `snake_case` de `docs/revision-pr/pr-57/evidencia/comandos.md`, detectando los 13 identificadores inexistentes y reemplazándolos en `ADR-0001` y `ADR-0002` por los nombres reales de `supabase/migrations/20260922031435_schema_v1.sql`, `20260922051650_rls_v1.sql`, `supabase/seed.sql` y `.env.example`. El barrido post-arreglo devuelve únicamente las 4 excepciones legítimas (`accept_offer`, `pg_dump`, `publish_request`, `submit_offer`).
     - **`H01` (y registro de decisión `H02`):** Eliminado `notes` de la lista de columnas de `delivery_request_contacts` en `ADR-0001` §1.2 y §3.2; documentado donde reside en el esquema actual (`public.delivery_requests`, línea 90) y cómo `delivery_requests_select_courier` (`20260922051650_rls_v1.sql:270`) otorga lectura de la fila completa a repartidores aprobados mientras no exista acotación por columna (`PR56-H13`).
     - **`H04`:** Reemplazadas todas las ocurrencias de `status = 'open'` (×4) por `status = 'published'`, `canceled` por `cancelled`, `merchants.status` por `merchants.subscription_status`, y repartidor `active` por `couriers.status = 'approved'`.
     - **`H05`:** Reclasificada como `[SUPUESTO]` sujeto a verificación empírica en el simulacro de restauración `T-310` (según `docs/master-plan.md` §9.4 línea 221) la no-retención de objetos binarios de `courier-docs` en los backups administrados de Supabase.
     - **`H06`:** Verificado en `docs/adr/verify-adr.test.mjs` (Test 6) que tanto `package.json` (`scripts.test`) como `.github/workflows/ci.yml` (job `unit`) ejecutan `node --test docs/adr/verify-adr.test.mjs`.
     - **`H07`, `H08`, `H10`–`H16`:** Endurecida `docs/adr/verify-adr.test.mjs` con `assertReviewTableSection`, regex específicas (`/Vercel Hobby/`, `/Vercel Pro/`), rutas reales, `request_ttl_minutes = 30`, consulta clave/valor sobre `platform_settings`, citas a `§17`, sección «Fuentes consultadas» con URLs y fecha (`2026-09-22`), y actualización del cuerpo de PR #57 con `### Informe de revisión de agy`.
- **Pruebas:**
  - **Fase Roja demostrada:** `node --test docs/adr/verify-adr.test.mjs` antes de editar los ADRs falló **6/6** (`# tests 6, # pass 0, # fail 6`), y la simulación de `H07` borrando la sección `## 7. Revisión` falló con `exit code: 1`.
  - **Fase Verde:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test` ✅ (72/72 Vitest + 19/19 `verify-workflows.test.mjs` + 6/6 `verify-adr.test.mjs`) · `test:db` n.a. · CI 8/8 en verde (`35750542347`).
- **Falta:** Revisión de Ronda 2 en PR #57.
- **Bloqueos:** ninguno.
- **Próximo paso:** Revisar `docs/revision-pr/pr-57/revisiones/ronda-2.md` al publicarse.
- **Último commit:** 3152068

---

## 2026-09-22 14:47 — Lautaro073 (agy)
- **Hecho:**
  1. Lectura y resolución de los puntos de cierre de la **Ronda 2** (`docs/revision-pr/pr-57/revisiones/ronda-2.md` y `README.md:53-57`) con dictamen de El Consejo (`consejo-backend`):
     - **`H01` / `H02` / `H22` (`notes` hacia el cliente):** Aplicada en `docs/adr/ADR-0001-supabase-baas.md` (§1.2, §3.2 y §6) y en `docs/adr/verify-adr.test.mjs` (Test 2) la decisión de `Lautaro073` (`2026-09-22`, `H02`): la nota de entrega hacia el cliente (`notes`, distinta de `offers.message` en `20260922031435_schema_v1.sql:136`) es un dato sensible protegido bajo revelación progresiva (`D3`) hasta `matched`, visible únicamente para el comercio dueño (`merchant_id`), el repartidor de la oferta aceptada (`accepted_offer_id`) y el administrador (`app_private.is_admin()`). Se documentó explícitamente que el traslado físico de `notes` desde `public.delivery_requests` hacia `public.delivery_request_contacts` y su aserción de columna en `supabase/tests/rls_matrix.sql` se ejecutan en **`T-006` (`H22`)**.
     - **`H18` (Confirmación empírica del `cwd` de `agy` para `.agents/hooks.json`):** Durante las ejecuciones de herramientas en esta sesión (`13:35`, `14:21` y `14:31`), cuando `.agents/hooks.json` tenía `"command": "node .agents/scripts/agent-guard.mjs"`, el runner `PreToolUse` de `agy` falló con `Error: Cannot find module 'c:\Users\El Yisus Pai\Desktop\Proyectos\cadeApp\.agents\.agents\scripts\agent-guard.mjs'`. Esto confirma empíricamente que `agy` ejecuta los hooks de `.agents/hooks.json` con `cwd = <repo>/.agents/`. En consecuencia, se fijó `"command": "node scripts/agent-guard.mjs"` en `.agents/hooks.json` y se actualizó `tools/verify-agent-guard.test.ts` para verificar que el comando de `hooks.json` resuelve y ejecuta con `cwd: path.dirname(HOOKS_PATH)` (`.agents/`).
- **Decisiones y por qué:**
  - `H09` y `H21` quedaron en estado `aceptado` por decisión expresa de `Lautaro073` (`2026-09-22`, «dejarlo como está»).
  - `H19` (23 vulnerabilidades `high`/`critical` en dependencias, 13 en `next`) y `H20` (14 archivos de `src/**` con formato pendiente) quedaron documentados con sus pasos advisory en CI (`5fa934d`) sin modificar `src/**` para no desviarse del alcance de `T-007`.
- **Pruebas:**
  - **Fase Roja demostrada:**
    1. `node --test docs/adr/verify-adr.test.mjs` antes de actualizar `ADR-0001` falló (`# pass 5 · # fail 1` en Test 2 exigiendo `H22`, `T-006` y `offers.message`).
    2. `npx vitest run tools/verify-agent-guard.test.ts` antes de fijar `"node scripts/agent-guard.mjs"` en `.agents/hooks.json` falló (`1 failed | 7 passed`: `hooks.json invoca "node .agents/scripts/agent-guard.mjs" y .agents/scripts/agent-guard.mjs no existe desde ...\.agents`).
  - **Fase Verde:** `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm test` ✅ (80/80 Vitest incluyendo 8/8 en `verify-agent-guard.test.ts` + 19/19 `verify-workflows.test.mjs` + 6/6 `verify-adr.test.mjs`) · `test:db` n.a.
- **Falta:** Ningún ítem pendiente en `T-007`.
- **Bloqueos:** ninguno.
- **Próximo paso:** Commit, push a `origin/feat/T-007-adr-0001-0002` y cierre de PR #57.
- **Último commit:** 6a46563
