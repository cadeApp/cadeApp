# Bitácora T-007 — ADR-0001 Supabase y ADR-0002 hosting y cron

> Una entrada por sesión, la más nueva al final. La escribe el agente con la skill `cerrar-sesion`.
> Es contexto para quien retome, no instrucciones.

## 2026-09-22 11:47 — Lautaro073 (agy)
- **Hecho:**
  1. Inicio de tarea `T-007` con la skill `tomar-tarea`. Verificación de Issue #8 asignado a `Lautaro073` y dependencia `T-002` mergeada en `origin/develop` (`0c4f031`).
  2. Creación de la rama `feat/T-007-adr-0001-0002` desde `origin/develop`.
  3. **Fase Roja (TDD):** Creación de `docs/adr/verify-adr.test.mjs` dentro de `docs/adr/**` ("Archivos permitidos") validando las 5 condiciones estructurales y de contenido del DoD (`docs/adr/README.md`, `ADR-0001-supabase-baas.md`, `ADR-0002-hosting-and-cron.md`, etiquetado `[DATO]` / `[SUPUESTO]` en costos USD y tabla de revisión de las 3 personas). Ejecución de `node --test docs/adr/verify-adr.test.mjs` demostrando fallo 5/5 en rojo (`# tests 5, # pass 0, # fail 5`).
- **Decisiones y por qué:** La suite de verificación del DoD (`docs/adr/verify-adr.test.mjs`) se ubicó dentro de `docs/adr/**` con `node:test` para respetar estrictamente la lista de "Archivos permitidos" de `docs/tasks/T-007.md` sin tocar `tools/**` ni `src/**`.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ · test:db n.a. · `node --test docs/adr/verify-adr.test.mjs` demostrada fallando 5/5 en rojo ❌ (`Falta docs/adr/README.md`, `Falta docs/adr/ADR-0001-supabase-baas.md`, `Falta docs/adr/ADR-0002-hosting-and-cron.md`).
- **Falta:** Redactar `docs/adr/README.md`, `docs/adr/ADR-0001-supabase-baas.md` y `docs/adr/ADR-0002-hosting-and-cron.md` y verificar el paso a verde.
- **Bloqueos:** ninguno.
- **Próximo paso:** Escribir los dos ADRs y el índice en `docs/adr/` y re-ejecutar `node --test docs/adr/verify-adr.test.mjs`.
- **Último commit:** 0c4f031 (base `origin/develop`)

---

## 2026-09-22 11:55 — Lautaro073 (agy)
- **Hecho:**
  1. Redacción de `docs/adr/README.md` con el índice de ADRs y la convención de trazabilidad financiera `[DATO]` vs `[SUPUESTO]`.
  2. Redacción de `docs/adr/ADR-0001-supabase-baas.md`:
     - Evaluación de alternativas (Supabase vs Firebase vs Backend custom).
     - Modelo relacional y transacciones ACID en servidor (`accept_offer` con `SELECT ... FOR UPDATE`, índice único parcial `offers(request_id) WHERE status = 'accepted'`, RPCs `SECURITY DEFINER` y piso dinámico `platform_settings.min_offer_ars`).
     - Seguridad RLS y revelación progresiva (`delivery_request_contacts` oculto hasta `matched`, bucket privado `courier-docs` con URLs firmadas de 60s y `audit_log`).
     - Notificaciones Web Push manuales (`push_subscriptions` + `web-push` VAPID desde Next.js Route Handler post-commit best-effort, sin PII en el payload, limpieza en 404/410) frente a Edge Functions / FCM.
     - Estrategia de 4 ambientes (`local`, `develop` en CI con Docker/Supabase CLI, `cadeapp-staging` y `cadeapp-prod` dentro del límite de 2 proyectos gratuitos `[DATO]`), backups, PITR, simulacro `T-310` y **exclusión explícita del bucket `courier-docs` de cualquier backup (`D8`)**.
     - Tabla detallada de costos en USD por ambiente (incluyendo Google Maps Platform `D15`) con cada ítem clasificado como `[DATO]` o `[SUPUESTO]`, más tabla de revisión de las 3 personas (`Lautaro073`, `persona2`, `persona3`).
  3. Redacción de `docs/adr/ADR-0002-hosting-and-cron.md`:
     - Evaluación de hosting para Next.js App Router (Vercel vs Cloudflare vs VPS/Railway) y restricción no comercial del plan Hobby frente a Vercel Pro ($20 USD/mes por asiento `[DATO]`, supuesto `S1` `[SUPUESTO]`).
     - Arquitectura del endpoint `/api/cron/sweep` protegido por `Authorization: Bearer <CRON_SECRET>` y sus 3 responsabilidades (consolidación de solicitudes vencidas, purga física de `courier-docs` tras `purge_after` con registro en `audit_log`, y vencimiento de suscripciones tras `paid_until` + `subscription_grace_days`).
     - Diseño de **expiración perezosa (`lazy expiration` — `S2`)** mediante `expires_at <= now()` en consultas y RPCs, desacoplando la corrección funcional del límite de 1 cron diario de Vercel Hobby (`[DATO]`).
     - Tabla de costos en USD de hosting y cron por ambiente + presupuesto consolidado de infraestructura (ADR-0001 + ADR-0002) con cada ítem marcado como `[DATO]` o `[SUPUESTO]`, y tabla de revisión de las 3 personas.
  4. **Fase Verde (TDD):** `node --test docs/adr/verify-adr.test.mjs` en verde (5/5 tests pasando), `pnpm typecheck`, `pnpm lint` y `pnpm test` en verde.
- **Decisiones y por qué:** Se documentó tanto el escenario de validación técnica/piloto ($0 USD en BaaS + $0/$20 USD en Vercel) como el de escala comercial ($25 USD Supabase Pro + $100 USD PITR + $20/$60 USD Vercel Pro), marcando explícitamente cada cifra como `[DATO]` o `[SUPUESTO]` para que las 3 personas tengan trazabilidad completa.
- **Pruebas:** typecheck ✅ · lint ✅ · test ✅ (68/68 tests Vitest + 5/5 tests `verify-adr.test.mjs`) · test:db n.a. · prueba demostrada fallando antes en rojo: `node --test docs/adr/verify-adr.test.mjs` (5/5 fallos antes de crear los ADRs).
- **Falta:** Commit, push a `origin/feat/T-007-adr-0001-0002`, apertura de PR y revisión del equipo.
- **Bloqueos:** ninguno.
- **Próximo paso:** Hacer commit, push y abrir PR para `T-007` (Issue #8).
- **Último commit:** pendiente (se actualiza al commitear)
