# T-116 — Componente de mapa `src/ui/map.tsx` con Google Maps Platform

- **Zona / dueño:** P2 · **Issue:** #22 · **Fase:** Fase 1 (Núcleo transaccional y flujos)
- **Dependencias:** T-008, T-111, T-112 y T-106 (T-106 debe estar mergeada antes de tomar esta tarea)
- **Contratos y referencias a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`, `docs/design/stitch/exports/C00/README.md`, `docs/design/stitch/exports/T00/README.md` y `docs/master-plan.md` §§4–6

## Objetivo
Componente de mapa `src/ui/map.tsx` (Google Maps Platform con `@vis.gl/react-google-maps` en import dinámico con `Skeleton`), selector interactivo de pin (crosshair central fijo desplazable, botón "Usar mi ubicación", botones de ajuste fino) para alta de comercio (T-111) y creación de solicitud (T-112), validación Zod de bounding box de Aguilares, y fallback graceful a formulario de texto si la API no carga o está offline

## Archivos permitidos
- `src/ui/map.tsx`
- `src/ui/map.test.tsx`
- `src/features/merchants/**`
- `src/features/requests/**`
- `src/app/(merchant)/**`
- `package.json`
- `pnpm-lock.yaml`
- `docs/tasks/T-116.md`
- `docs/tasks/log/T-116.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- `@vis.gl/react-google-maps`, únicamente mediante `contract-change` aprobado y carga diferida obligatoria. El contrato de `src/ui/map.tsx`, el paquete y el lockfile no se modifican hasta que ese cambio separado esté aprobado.

## DoD
- [ ] Pruebas en rojo antes de implementar y commit separado: fallback sin API key o con Google caído, selección de pin, botón de ubicación, ajuste fino y límites de Aguilares.
- [ ] Se aplica `implementar-diseno` sobre C01, C03 y la variante T03-mapa-sin-conexión. La carga usa `Skeleton`, el mapa se importa dinámicamente y el formulario de texto sigue operativo sin permiso de GPS o sin red.
- [ ] Tests unitarios de `src/ui/map.test.tsx`; selección de pin actualiza coordenadas y dirección; Zod rechaza coordenadas fuera de Aguilares; axe AA sin violaciones; first-load bundle JS < 180 KB respetado mediante import dinámico.
- [ ] Verificación en navegador y capturas de C01/C03 a 390 px y 360 px, con mapa disponible, mapa caído y modo offline; el feed del repartidor no monta este componente.
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-116.md` al día y PR con evidencia


- Leer y aplicar [`docs/design/visual-task-directive.md`](../design/visual-task-directive.md) y usar el prompt visual de [`docs/guia-prompts.md`](../guia-prompts.md) antes de implementar.
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
