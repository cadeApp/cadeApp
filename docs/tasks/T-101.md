# T-101 — `submit_offer`, `withdraw_offer`, `set_availability`, `rate_limits` at

- **Zona / dueño:** P1 · **Issue:** #11 · **Fase:** Fase 1 (Núcleo transaccional y flujos)
- **Dependencias (mergeadas en develop):** contracts-v1
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
`submit_offer`, `withdraw_offer`, `set_availability`, `rate_limits` atómico

## Archivos permitidos
- `supabase/migrations/**`
- `supabase/tests/rpc_offers.sql`
- `src/server/rpc/offers.ts`
- `src/server/rpc/offers.test.ts` (autorizado por Lautaro073)
- `src/types/database.types.ts` (autorizado por Lautaro073)
- `public/brand/logo.svg` (hotfix aria-label="cadeApp" tras fix(svg) de 164d368 en develop)
- `docs/tasks/T-101.md`
- `docs/tasks/log/T-101.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`

## Dependencias nuevas permitidas
- ninguna

## DoD
- [x] 999, 1000 y 1001 con el piso cambiado a 1500; pending, rejected y suspended rechazados; oferta activa duplicada; rate limit con llamadas concurrentes; test de contrato contra `rpc-contracts.ts` y el fake
- [x] `pnpm typecheck && pnpm lint && pnpm test`
- [x] Sin cambios fuera de "Archivos permitidos"
- [x] Bitácora `docs/tasks/log/T-101.md` al día y PR con evidencia

## Notas para quien retome
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- **Desde T-005 el cliente no mueve estados.** La RLS de `20260922051650_rls_v1.sql` congela `offers.status`, `offers.decided_at`, `delivery_requests.status`, `accepted_offer_id` y los cinco *timestamps* de ciclo de vida, tanto en `update` como en `insert`: una oferta nace `pending`, una solicitud nace `draft`, un documento nace `submitted` y un incidente nace `open`. O sea que **estas RPC no son una comodidad, son el único camino**, y tienen que ser `security definer`. Si una de ellas intenta la transición con el rol del invocador, va a recibir `42501`. Salió de la revisión de la PR #56 (`PR56-H06`, `PR56-H11`, `PR56-H12`, `PR56-H15` a `PR56-H19`); el detalle está en `docs/revision-pr/pr-56/revisiones/ronda-4.md`.
- `withdraw_offer` es el **único** camino para retirar una oferta: la policy `offers_update_courier` solo deja editarla mientras está en `pending` y no deja tocar `status`, así que el valor `withdrawn` del enum hoy solo lo puede escribir un admin o una RPC.
