# T-103 — Ciclo de solicitud (publish, cancel, picked_up, delivered, no_show, co

- **Zona / dueño:** P1 · **Issue:** #13 · **Fase:** Fase 1 (Núcleo transaccional y flujos)
- **Dependencias (mergeadas en develop):** contracts-v1
- **Contratos a leer:** `src/domain/**`, `AGENTS.md`, `.agents/rules/**`

## Objetivo
Ciclo de solicitud (publish, cancel, picked_up, delivered, no_show, courier_cancel, republish, report_incident), expiración perezosa, distancia por zonas

## Archivos permitidos
- `supabase/migrations/**`
- `supabase/tests/rpc_requests.sql`
- `src/server/rpc/requests.ts`
- `src/server/rpc/requests.test.ts`
- `src/types/database.types.ts`
- `docs/tasks/T-103.md`
- `docs/tasks/log/T-103.md`
- `docs/revision-pr/**`

## Fuera de alcance
- Modificación de archivos no autorizados en la lista anterior
- Cambios de contratos sin procedimiento `contract-change`
- Edición manual de `src/types/database.types.ts`: solo se permite regenerarlo con `pnpm db:types --local` al agregar las RPC de esta tarea.

## Dependencias nuevas permitidas
- ninguna

## DoD
- [ ] Cada transición de §5.1 del master plan, válida e inválida por actor; una solicitud vencida rechaza ofertas; suscripción vencida bloquea publicar; test de contrato
- [ ] `pnpm typecheck && pnpm lint && pnpm test`
- [ ] Sin cambios fuera de "Archivos permitidos"
- [ ] Bitácora `docs/tasks/log/T-103.md` al día y PR con evidencia

## Notas para quien retome
- **CC-002 (#65):** ampliación de alcance autorizada por Lautaro073 el 2026-09-23 para el test del wrapper/contrato y los tipos generados. T-103 permanece bloqueada hasta que CC-002 esté mergeado en develop. Los cambios de `src/domain/**` se resuelven en CC-002; no se autorizan dentro de T-103.
- Ficha oficial generada en T-001 sincronizada con docs/implementation-plan.md §8.
- **Desde T-005 el cliente no mueve estados.** La RLS de `20260922051650_rls_v1.sql` congela `offers.status`, `offers.decided_at`, `delivery_requests.status`, `accepted_offer_id` y los cinco *timestamps* de ciclo de vida, tanto en `update` como en `insert`: una oferta nace `pending`, una solicitud nace `draft`, un documento nace `submitted` y un incidente nace `open`. O sea que **estas RPC no son una comodidad, son el único camino**, y tienen que ser `security definer`. Si una de ellas intenta la transición con el rol del invocador, va a recibir `42501`. Salió de la revisión de la PR #56 (`PR56-H06`, `PR56-H11`, `PR56-H12`, `PR56-H15` a `PR56-H19`); el detalle está en `docs/revision-pr/pr-56/revisiones/ronda-4.md`.
- `publish`, `cancel`, `picked_up`, `delivered`, `no_show`, `courier_cancel` y `republish` son las únicas formas de mover `delivery_requests.status`, y `report_incident` la única de crear un incidente que no nazca `open` sin `resolution`. Los cinco *timestamps* de ciclo de vida también los escribe la RPC: la policy del comercio los congela y exige `created_at = now()` al insertar.
